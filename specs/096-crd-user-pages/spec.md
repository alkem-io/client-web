# Feature Specification: CRD Public Profile Pages (User, Organization, Virtual Contributor)

**Feature Branch**: `096-crd-user-pages`
**Created**: 2026-04-29 (expanded 2026-04-30 to add Organization + VC profile pages)
**Status**: Draft
**Input**: Migrate the public **profile pages** for the three actor types — **User** (`/user/:userSlug`), **Organization** (`/organization/:orgSlug`), and **Virtual Contributor** (`/vc/:vcSlug`) — from the current MUI implementation to the CRD design system (shadcn/ui + Tailwind).

**Per-actor design source** (this is the canonical rule for every section / clarification below):

- **User profile** — **redesign per the prototype at `prototype/src/app/pages/UserProfilePage.tsx`**. The prototype introduces a 5-tab resource strip, drops the MUI's flat list of sections, and omits the MUI's innovation-packs / innovation-hubs sub-sections. This page is **not** a parity restyle of the current MUI; where prototype and MUI disagree, the prototype wins.
- **Organization profile** — **parity restyle of the current MUI** (`OrganizationPageView` + `AssociatesView`). No prototype exists; no design changes vs. today's MUI behavior.
- **VC profile** — **parity restyle of the current MUI** (`VCProfilePageView` + `VCProfileContentView`). No prototype exists; no design changes vs. today's MUI behavior.

Follow the migration pattern established by spec `045-crd-space-settings` and the architectural rules in `docs/crd/migration-guide.md`. The seven **User Settings** tabs (`/user/:userSlug/settings/*`) are owned by sibling spec `097-crd-user-settings`. Both specs share the same `useCrdEnabled` toggle and the shared layout shell `CrdLayoutWrapper`. Organization and VC settings/admin pages are **not** in scope here — they remain future specs.

## Clarifications

### Session 2026-04-29 (User profile)

- Q: Online-presence dot on the public User profile hero — derive from `lastActiveDate`, drop entirely, or wire a real presence channel? → A: **Drop entirely.** The dot is removed from the public profile hero. This keeps the migration a parity restyle (current MUI does not surface presence) and avoids implying real-time online status without an actual presence channel. `User.lastActiveDate` exists on the schema but stays unused by CRD in this iteration.
- Q: Does the public User profile Message button stay in the CRD hero? → A: **Yes — keep it.** The CRD hero exposes a Message button for any signed-in non-owner viewer. The button reuses the existing `useSendMessageToUsersMutation` via a callback prop, mirroring the current MUI `UserPageBanner.handleSendMessage` flow exactly: clicking the button opens an in-hero compose surface (or a small dialog), the user types a message, the mutation fires with `{ message, receiverIds: [userId] }`, and on success the surface closes. No new GraphQL mutation is introduced.
- Q: Public-profile resource sections — pagination strategy? → A: **Per-actor rule (FR-016).** **User profile** follows the prototype (`prototype/src/app/pages/UserProfilePage.tsx`): every resource section renders every item — no cap, no "Show more" affordance, no Next/Previous controls, no infinite scroll. **Organization profile** preserves current-MUI behavior exactly: `AccountResourcesView` keeps its `VISIBLE_SPACE_LIMIT = 6` cap on hosted spaces with the existing "Show all" button (existing MUI logic ported as-is to CRD), while `TilesContributionsView`-equivalent membership sections (Lead Spaces, All Memberships) render all items with no cap. **VC profile** has no list sections that need a cap. Empty sections are omitted per FR-015.
- Q: Settings (gear) icon on the public-profile hero — owner-only, or also visible to platform admins on other users' profiles? → A: **Owner OR platform admin.** The Settings icon mirrors the same `canEditSettings = (currentUser.id === profileUser.id) || currentUserHasPlatformAdminPrivilege` predicate used by the sibling settings spec (`097-crd-user-settings` FR-008a) — visible for the profile owner AND for any platform-admin viewer of someone else's profile. Same icon, same destination (`/user/:userSlug/settings/profile`); for an admin, it links to the target user's settings, not their own. The Message button stays visible for the admin too, since admins can also message other users — so an admin viewing a non-self public profile sees BOTH the Settings icon and the Message button in the hero.
- Q: Tab strip overflow on small viewports — how does the 5-tab public-profile resource strip behave below `md`? → A: **Horizontal scroll.** For viewports below the `md` breakpoint, every tab remains rendered inline; the strip itself overflows and is horizontally scrollable (touch swipe, wheel, or trackpad gesture) using `overflow-x-auto no-scrollbar` (matching the prototype's existing approach on the resource strip). The active tab MUST be auto-scrolled into view on mount and on tab change so it never starts off-screen. No dropdown / hamburger / two-row wrap variant is introduced — this matches the responsive behavior of the 7-tab settings strip in sibling spec `097-crd-user-settings` so the visual identity stays consistent across the user vertical.

### Session 2026-04-30 (Organization + VC profiles, expansion)

- Q: Should the Settings (gear) icon on the **Organization** and **VC** heroes also follow the owner-OR-admin predicate, or is it gated differently today? → A: **Use the entity's existing predicate, ported as-is — no broader gating introduced.** The Organization hero shows the Settings icon when the viewer has `canEdit` per `useOrganizationProvider().permissions` (today's MUI predicate); the VC hero shows the Settings icon when `vc.authorization.myPrivileges` includes `AuthorizationPrivilege.Update` (today's MUI predicate in `VCPageBanner`). Both are equivalent in spirit to the User canEditSettings predicate (owner OR admin) but resolve through the entity's own authorization data. The destination URL stays the same as today: `buildSettingsUrl(profile.url)` — for orgs that's `/organization/<slug>/settings/...`, for VCs it's `/vc/<slug>/settings/...`. **The destination route itself is rendered in MUI** since Organization and VC settings/admin pages are **not** in scope for this spec; clicking the icon lands the user on the existing MUI admin shell (consistent with the Account-tab navigation pattern in sibling spec 097).
- Q: Does the Organization hero keep the Message button, and does the VC hero get one? → A: **Organization: yes, VC: no.** The current MUI `OrganizationPageBanner` exposes `onSendMessage` (parity), so the CRD Org hero keeps it for any signed-in viewer. The current MUI `VCPageBanner` does **not** expose a Message button (VCs are AI personas; messaging them through the platform is not a current product feature), so the CRD VC hero **omits** the Message button entirely. No new GraphQL mutation is introduced for either — the Org Message button reuses the existing send-message-to-users mutation against the org's contributor IDs (the existing `OrganizationPageBanner` flow).
- Q: VC profile's "Body of Knowledge" section — restyle or rebuild? → A: **Restyle the existing structure exactly.** The current MUI VC profile renders a Body of Knowledge section with three variants: (a) Space-backed (renders a `SpaceCardHorizontal` linking to the backing space, with the space's about description), (b) Knowledge-Base-backed (renders a description blurb plus a "Visit" button — gated by `hasReadAccess`, disabled with a tooltip when the user lacks privilege), (c) External (renders a copy line indicating the engine type — Assistant or other). CRD ports all three variants verbatim, restyled with CRD primitives. The "Visit" button on the Knowledge-Base variant uses the existing `KNOWLEDGE_BASE_PATH` URL builder. **No design refresh** of the BoK section is introduced.
- Q: Empty-states / 404 behavior — same as User? → A: **Yes.** All three actor profile pages share the same parity rules: (i) empty resource sections are omitted (FR-015) or rendered as a CRD empty-state line, depending on parity with current MUI; (ii) when the entity does not exist (404 on the underlying query — e.g., `isApolloNotFoundError` on the VC profile), the page renders the existing `Error404` component inside the CRD layout (parity with current MUI `VCProfilePage` 404 handling); (iii) when the viewer lacks the required privilege (e.g., `useRestrictedRedirect` on the VC profile), the existing redirect logic runs unchanged.
- Q: User profile tab strip — how does each of the 5 tabs map to the 3 underlying resource sections (Resources Hosted [hosted spaces + hosted VCs], Spaces Leading, Member Of)? → A: **One tab → one view; section headers hidden on single-slice tabs.** The mapping is: **All Resources** renders all 3 sections **with** their section headers (Resources Hosted / Spaces Leading / Member Of); **Hosted Spaces** renders only the hosted-spaces slice of Resources Hosted (the VCs sub-list is hidden, and Spaces Leading + Member Of are hidden); **Virtual Contributors** renders only the hosted-VCs slice of Resources Hosted (spaces sub-list hidden, other sections hidden); **Leading** renders only Spaces Leading; **Member Of** renders only Member Of. When a single-slice tab is active, the section header is hidden — just the card grid is rendered, since the tab label itself acts as the heading. Empty-state and section-omission rules (FR-015) still apply per slice within whichever tab is active.
- Q: User profile tab strip — URL-driven (deep-linkable) or local React state? → A: **Local React state only, parity with current MUI.** The active tab is held in `useState('all')` inside the CRD page integration; it does **not** sync to a URL search param or path segment. Direct entry (e.g., reload, back/forward, shared link) always lands on **All Resources**. Rationale: the migration is a parity restyle — current MUI does not expose tab state in the URL, and adding deep-linking expands scope beyond presentation. Deep-linking can be added later as an additive enhancement (FR amendment) without changing this spec.
- Q: Organization Associates sidebar — render every associate, or a capped sample with "View all" link? → A: **Match current MUI exactly** (`AssociatesView`): render up to `ASSOCIATE_CARDS_COUNT = 12` associates by default, then a "Show more (N) / Show less" link that toggles the remaining set in-place. When the viewer lacks `permissions.canReadUsers`, the section header is still rendered but the body shows the existing sign-in CTA copy (i18n key `associates-view.sign-in`) instead of the avatar grid — section is **not** hidden. This is a parity port of the MUI `AssociatesView` component; no behavior change vs. today.
- Q: Loading states during initial data fetch on each profile page — Skeleton placeholders, empty shell, or nothing? → A: **Skeleton placeholders, using the existing CRD `Skeleton` primitive.** While the underlying queries are in flight (`useUserProvider`, `useOrganizationProvider` + `useOrganizationAccountQuery`, `useVirtualContributorProfileWithModelCardQuery`), each CRD page MUST render Skeleton placeholders sized to the eventual content: hero region (banner rectangle, circular avatar, name line, location line), sidebar sections (3–4 stacked rectangles per section), and main column (2–3 card-shaped placeholders). Skeletons MUST be replaced by real content as each query resolves (the page does not block on all queries — sections render as their data lands). This is the same pattern used by other CRD pages (sibling spec 097 settings, Explore Spaces). Rationale: prevents layout shift, gives immediate visual confirmation that the page is loading, and the `Skeleton` primitive already exists under `src/crd/primitives/skeleton`.
- Q: VC profile social-references brand icons — existing source, new CRD primitive, or copy SVG assets? → A: **Use `lucide-react` brand icons (no new primitive).** The CRD `VCContentView` MUST map social-network IDs from `isSocialNetworkSupported` to the matching `lucide-react` component (`Linkedin`, `Twitter`, `Github`, `Youtube`, etc.), with `Globe` as the fallback for unrecognized social platforms. `lucide-react` is the standard CRD icon library per `src/crd/CLAUDE.md`; every supported platform has a matching icon. No new CRD primitive is introduced, no SVG assets are copied. This is a small, intentional brand-fidelity tradeoff — `lucide-react` icons are stylized approximations, not exact brand marks; exact-fidelity brand SVGs can be added later as a separate primitive if/when product asks.

## User Scenarios & Testing

The three public profile pages are the entry points of their respective verticals — User, Organization, and Virtual Contributor. They share an architectural pattern (CRD shell + integration layer + presentational components) with each other and with the User Settings shell (sibling spec `097-crd-user-settings`). All ship together when CRD is enabled: when the toggle is on, every URL above renders in CRD; when the toggle is off, every URL renders in the existing MUI files. There is no half-CRD / half-MUI mix.

### User Story 1 - Public User Profile Page (Priority: P1)

Anyone with access to the platform opens `/user/:userSlug` and sees a public profile of that user, replacing the current MUI `UserProfilePage` with a CRD shell. The page presents:

- A **profile hero**: a wide banner image (using `pickColorFromId` for a deterministic gradient fallback when no banner is set), the user's avatar overlaid on the banner, the user's display name, a short location line (city + country), and — when the viewer satisfies `canEditSettings` (the profile owner OR a platform admin) — a **Settings** icon button that links to `/user/:userSlug/settings/profile` for the profile being viewed. There is no online-presence dot in this iteration (the prototype's mock dot is dropped); presence remains a future enhancement.
- A **left sidebar** (`col-span-4` on `lg+`, stacked above the right column on smaller viewports — single-column layout, sidebar is **not** hidden — per the prototype's `grid-cols-1 lg:grid-cols-12` layout) showing two sections: an **About** block rendering the user's bio (`profile.description`, markdown) and an **Organizations** block listing the user's associated organizations as compact `OrganizationCard` rows (logo, name, role, member count) — same data the current MUI `AssociatedOrganizationsLazilyFetched` consumes.
- A **right column** (`col-span-8` on `lg+`) with a sticky **resource tab strip** (sticky-positioned per the prototype's `sticky top-16` wrapper): All Resources, Hosted Spaces, Virtual Contributors, Leading, Member of. The default active tab is "All Resources". Each tab filters the visible resource sections. On viewports below `md` the strip is horizontally scrollable (`overflow-x-auto no-scrollbar`); all five tabs stay inline and the active tab auto-scrolls into view.
- Below the tab strip the page renders, depending on the active tab:
  - **Resources Hosted** — a section combining the user's hosted spaces (rendered as the same reusable CRD space-card the Explore Spaces feature consumes) and the user's hosted virtual contributors (rendered as `Sparkles`-iconed cards with name, description, type badge). **Innovation packs and innovation hubs are intentionally omitted** (per prototype, see Out of Scope).
  - **Spaces Leading** — spaces where the user holds either the `Lead` or `Admin` role, filtered via `useFilteredMemberships(contributions, [RoleType.Lead, RoleType.Admin])` exactly as the current MUI `UserProfilePageView` does, rendered as space-cards.
  - **Member of** — remaining memberships, rendered as space-cards.

If the user has no hosted resources / no leading spaces / no memberships, the corresponding section either renders an empty-state message ("No memberships yet") or is omitted, mirroring the current MUI behavior.

The page is **read-only**: there is no editing capability anywhere on the public profile. Editing happens exclusively in `/user/:userSlug/settings/*`, which is owned by sibling spec `097-crd-user-settings`.

The page also includes a **Message** button in the hero. When the viewer is signed in and is **not** the profile owner, clicking Message opens an in-hero compose surface (a small popover or inline textarea); submitting fires the existing `useSendMessageToUsersMutation` against this user as recipient — same handler shape as the current MUI `UserPageBanner.handleSendMessage`. The Message button is hidden on the user's own profile.

**Independent Test**: Toggle CRD on. Open `/user/jeroen`. Verify the hero, the bio, the organizations list, the sticky tab strip with All Resources active, and the rendered space cards. Click each tab — only the matching sections render. Confirm the Settings icon button is visible when viewing your own profile (links to `/user/<self>/settings/profile`) AND when a platform admin views another user's profile (links to `/user/<otherUser>/settings/profile`); for non-admin viewers on someone else's profile, the Settings icon is hidden. Confirm the Message button is visible on other users' profiles (for any signed-in viewer, including admins) and hidden on your own.

**Acceptance Scenarios**:

1. **Given** any signed-in user opens `/user/<otherUser>`, **When** the page renders, **Then** the CRD hero, bio, organizations sidebar, sticky tab strip (All Resources active), and the user's hosted/leading/member space sections are visible.
2. **Given** the viewer is on their own profile, **When** the hero renders, **Then** a Settings icon button is visible and a Message button is **not** visible.
3. **Given** a non-admin viewer is on someone else's profile, **When** the hero renders, **Then** the Message button is visible and the Settings icon button is **not** visible.
4. **Given** a platform-admin viewer is on someone else's profile, **When** the hero renders, **Then** **both** the Message button **and** the Settings icon button are visible; clicking the Settings icon navigates to `/user/<otherUser>/settings/profile` (the target user's settings, not the admin's own).
5. **Given** the user has no hosted resources, **When** the All Resources tab is active, **Then** the Resources Hosted section is omitted (empty section is not rendered).
6. **Given** the user has no leading spaces, **When** the Leading tab is active, **Then** an empty-state message is rendered.
7. **Given** any user opens the public profile, **When** the hero renders, **Then** **no** online-presence dot is shown (parity with current MUI).
8. **Given** CRD is disabled via the localStorage toggle, **When** the page is opened, **Then** the existing MUI `UserProfilePage` renders unchanged.

---

### User Story 2 - Public Organization Profile Page (Priority: P1)

Anyone with access to the platform opens `/organization/:orgSlug` and sees a public profile of that organization, replacing the current MUI `OrganizationPage` with a CRD shell. The page presents:

- A **profile hero**: a wide banner image (gradient fallback via `pickColorFromId(organization.id)` when no banner is set), the organization's avatar/logo overlaid on the banner, the organization's display name, a short location line (city + country), a **Verified** badge when `organization.verification.status === VerifiedManualAttestation` (parity with the current MUI badge), and — when the viewer has `permissions.canEdit` per the existing `useOrganizationProvider` (owner / admin) — a **Settings** icon button that links to the existing `buildSettingsUrl(profile.url)` destination (which currently resolves to the MUI `/organization/<slug>/settings/...` admin shell; the CRD migration of that admin shell is out of scope for this spec).
- A **Message** button in the hero, visible to any signed-in viewer (parity with the current MUI `OrganizationPageBanner.onSendMessage` callback). Clicking opens an in-hero compose surface; submitting fires the existing send-message mutation against the organization as recipient — same handler shape as today. The Message button is hidden for anonymous viewers.
- A **left sidebar** (`col-span-3` on `md+`, single column on smaller viewports) showing four sections (parity with the current MUI `OrganizationProfileView` + `AssociatesView`):
  - **Bio** — `organization.profile.description` rendered as markdown.
  - **Tagsets** — Keywords + Capabilities rendered as compact tag pills (current MUI tagsets).
  - **References** — non-social links from `organization.profile.references[]` rendered as labeled URL chips. Social references (LinkedIn / GitHub / X / Bluesky / etc., as identified by the existing `isSocialNetworkSupported` predicate) are NOT shown here; they appear in their own **Social** sub-block below References (parity with current MUI `OrganizationProfileView`'s `SocialLinks`).
  - **Associates** — port of the current MUI `AssociatesView`: avatar grid capped at 12 with a "Show more (N) / Show less" toggle, with the associate `totalCount` from `metrics[Associate]` shown in the section header. When the viewer has `permissions.canReadUsers`, the avatar grid renders; when they lack it, the section header still renders and the body shows the existing sign-in CTA copy (`associates-view.sign-in`) instead of the grid (parity with MUI — section is **not** hidden).
- A **right column** (`col-span-9` on `md+`) with three sections (parity with the current MUI):
  - **Account Resources** — when the organization has any account-owned resources (`accountResources.spaces` / `innovationPacks` / `innovationHubs` non-empty), render them as a single titled section combining all three lists, exactly like the current MUI `AccountResourcesView`. The section is omitted when the org has no account resources (matches current MUI logic).
  - **Lead Spaces** — spaces where the organization holds the `lead` role (filtered via `useFilteredMemberships(contributions, [RoleType.Lead])`), rendered as `TilesContributionsView`-equivalent CRD space cards. Section omitted when empty.
  - **All Memberships** — remaining memberships (also via `useFilteredMemberships`), rendered as space cards. When empty, render an empty-state line ("No memberships yet" — same key the current MUI uses) inside a titled card so the section header is still visible.

The page is **read-only**: there is no editing capability anywhere on the public Organization profile. Editing happens exclusively in `/organization/:orgSlug/settings/...`, which today is rendered in MUI (and remains in MUI under this spec — its CRD migration is out of scope).

**Independent Test**: Toggle CRD on. Open `/organization/<some-org>`. Verify the hero, sidebar (Bio + Tagsets + References + Associates), and right column (Account Resources + Lead Spaces + All Memberships) render. Click the Settings icon as an admin viewer — navigates to `/organization/<slug>/settings/...` (MUI admin shell — out of scope, but the link must work). Click the Message button — compose surface opens. Toggle CRD off — the existing MUI `OrganizationPage` renders unchanged.

**Acceptance Scenarios**:

1. **Given** any signed-in user opens `/organization/<orgSlug>`, **When** the page renders, **Then** the CRD hero (banner + avatar + name + location + Verified badge if applicable), sidebar, and right column with Account Resources / Lead Spaces / All Memberships are visible.
2. **Given** the viewer has `canEdit` on the organization, **When** the hero renders, **Then** the Settings icon button is visible and clicking it navigates to the organization's settings/admin URL.
3. **Given** the viewer does not have `canEdit`, **When** the hero renders, **Then** the Settings icon button is hidden; the Message button is still visible (parity with current MUI).
4. **Given** the organization is verified, **When** the hero renders, **Then** the Verified badge is shown.
5. **Given** the organization has no account resources, **When** the right column renders, **Then** the Account Resources section is omitted (empty section is not rendered — parity with current MUI's `hasAccountResources` check).
6. **Given** the organization has no Lead Spaces, **When** the right column renders, **Then** the Lead Spaces section is omitted.
7. **Given** the organization has no memberships, **When** the right column renders, **Then** the All Memberships section renders with the empty-state caption "No memberships yet".
8. **Given** the viewer lacks `canReadUsers`, **When** the sidebar renders, **Then** the Associates section header is still visible and the section body shows the existing sign-in CTA copy (`associates-view.sign-in`) instead of the avatar grid (parity with current MUI — the section is **not** hidden).
9. **Given** an anonymous viewer, **When** the hero renders, **Then** the Message button is hidden.
10. **Given** CRD is disabled via the localStorage toggle, **When** the page is opened, **Then** the existing MUI `OrganizationPage` renders unchanged.

---

### User Story 3 - Public Virtual Contributor Profile Page (Priority: P1)

Anyone with access to the platform opens `/vc/:vcSlug` and sees a public profile of that virtual contributor, replacing the current MUI `VCProfilePage` with a CRD shell. The page presents:

- A **profile hero**: a wide banner image (gradient fallback via `pickColorFromId(vc.id)` when no banner is set), the VC's avatar/icon overlaid on the banner, the VC's display name, and — when the viewer has `vc.authorization.myPrivileges` containing `AuthorizationPrivilege.Update` (owner / admin) — a **Settings** icon button that links to `buildSettingsUrl(profile.url)` (the existing `/vc/<slug>/settings/...` MUI admin shell). **No Message button** (parity with current MUI; VCs are AI personas, not message recipients).
- A **left sidebar** (`col-span-3` on `md+`, single column on smaller viewports) showing four sections (parity with the current MUI `VCProfilePageView` left column):
  - **Description** — `vc.profile.description` rendered as markdown.
  - **Host** — a compact card showing the VC's provider (`vc.provider.profile`) — display name + avatar + link to the provider's profile page (User or Organization). Same shape as the current MUI `ContributorCardHorizontal seamless`.
  - **References** — non-social references from `vc.profile.references[]` (filtered via `isSocialNetworkSupported` — only the "other" group renders here; social references go in the right column). Empty-state line "No references" when the list is empty.
  - **Body of Knowledge** — varies by `vc.bodyOfKnowledgeType`:
    - `AlkemioSpace` — descriptive text + a `SpaceCardHorizontal`-equivalent CRD card linking to the backing space (gated by the viewer's `ReadAbout` privilege on that space — when no privilege, the card shows "Private space" placeholder, parity with current MUI).
    - `AlkemioKnowledgeBase` — descriptive text from `useKnowledgeBase().knowledgeBaseDescription` + a **Visit** button linking to `${profile.url}/${KNOWLEDGE_BASE_PATH}`. The button is **disabled with a tooltip** ("Body of knowledge is private") when the viewer lacks `hasReadAccess` (parity with current MUI).
    - **External** (no body of knowledge) — copy describing the engine type (Assistant vs. other), parity with current MUI `externalVCDescription`.
- A **right column** (`col-span-9` on `md+`) — the **VC Content View**: model card details (the existing `VirtualContributorModelCardModel` data — `aiEngine`, `prompts`, `dataPrivacy`, etc.), social links (the "social" group of references rendered with their brand icons, exact parity with current MUI `VCProfileContentView`).

The page is **read-only**: there is no editing capability anywhere on the public VC profile. Editing happens exclusively in the VC's settings/admin shell, which today is rendered in MUI and remains in MUI under this spec.

**Independent Test**: Toggle CRD on. Open `/vc/<some-vc>`. Verify the hero (no Message button), sidebar (Description + Host + References + BoK section), and right column (model card + social links) render. For a VC with `AlkemioSpace` BoK, verify the SpaceCardHorizontal links to the backing space (or "Private space" placeholder when the viewer lacks read privilege). For a VC with `AlkemioKnowledgeBase` BoK, click Visit — navigates to the knowledge base; without read access, Visit is disabled with the tooltip. Click the Settings icon as an owner viewer — navigates to the VC's settings/admin URL. Toggle CRD off — the existing MUI `VCProfilePage` renders unchanged.

**Acceptance Scenarios**:

1. **Given** any signed-in user opens `/vc/<vcSlug>`, **When** the page renders, **Then** the CRD hero (banner + avatar + name + Settings icon when permitted, **no Message button**), sidebar (Description + Host + References + BoK), and right-column content view (model card + social links) are visible.
2. **Given** the viewer has the `Update` privilege on the VC, **When** the hero renders, **Then** the Settings icon button is visible and clicking it navigates to the VC's settings/admin URL.
3. **Given** the viewer does not have `Update`, **When** the hero renders, **Then** the Settings icon button is hidden.
4. **Given** the VC's BoK type is `AlkemioSpace`, **When** the BoK section renders, **Then** a SpaceCardHorizontal-equivalent CRD card links to the backing space; when the viewer lacks the space's `ReadAbout` privilege, the card shows the "Private space" placeholder.
5. **Given** the VC's BoK type is `AlkemioKnowledgeBase`, **When** the BoK section renders, **Then** the description and a Visit button are shown; when the viewer lacks `hasReadAccess`, Visit is disabled with the "Body of knowledge is private" tooltip.
6. **Given** the VC is `External` (no BoK), **When** the BoK section renders, **Then** the engine-type description is shown (parity with current MUI copy).
7. **Given** a VC has no `bodyOfKnowledgeType` set AND `modelCard.aiEngine.isExternal === false`, **When** the sidebar renders, **Then** the Body of Knowledge section is omitted entirely (no header, no empty-state caption — parity with current MUI which silently renders nothing in this case).
8. **Given** the VC profile query returns a 404 (entity not found), **When** the page resolves, **Then** the existing `Error404` component is rendered inside the CRD layout (parity with current MUI).
9. **Given** the viewer lacks the `Read` privilege on the VC, **When** the page resolves, **Then** the existing `useRestrictedRedirect` redirect runs unchanged.
10. **Given** CRD is disabled via the localStorage toggle, **When** the page is opened, **Then** the existing MUI `VCProfilePage` renders unchanged.

---

### Edge Cases

- **Unauthenticated viewer on any public profile** (per research §1, verified against `TopLevelRoutes.tsx`): `/user/*` is wrapped in `<NoIdentityRedirect>` — anonymous viewers are redirected to login before the User profile ever renders. `/organization/*` has no auth wrapper and `/vc/*` is wrapped in `<NonIdentity>`; both let anonymous viewers load the page. On the Organization profile, the Message button is hidden (no signed-in identity to send from) and the Settings icon is hidden (no `canEdit`). On the VC profile, the Settings icon is hidden, and `useRestrictedRedirect` enforces the `Read` privilege downstream.
- **Public profile URL with stale `nameID`**: the existing redirect logic (`useUserProvider` for users, `useOrganizationProvider` for orgs, `useUrlResolver` for VCs) resolves the canonical URL; CRD reuses the same hooks.
- **Viewing your own User profile via `/me` shorthand**: `/user/me` resolves to the current user's profile page (existing `UserMeRoute` behavior), then renders the public profile. The Settings icon links to `/user/me/settings/profile`. No analogous shorthand exists for Organization or VC — those always use the slug.
- **Long resource lists** (50+ hosted spaces, 50+ memberships): pagination behavior is per-actor (FR-016). User profile renders every item (per prototype). Organization profile preserves the current MUI 6-cap + "Show all" affordance on Account Resources spaces, and renders all items for Lead Spaces / All Memberships. Sticky tab strips (User profile only — Org/VC don't have a tab strip) stay anchored as the user scrolls.
- **Message send failure** (User + Organization): the existing mutation error path surfaces a CRD `Toast` with the error message; the compose surface stays open so the user can retry without retyping.
- **CRD toggled off mid-session**: the design-system toggle on the User Settings tab (sibling spec) reloads the page; the user lands on the equivalent MUI public profile page (User / Organization / VC). URL stays the same; routing matches the MUI handler.
- **VC with private Body of Knowledge**: the Visit button is disabled with a tooltip explaining the privacy; the BoK description still renders (parity with current MUI).
- **Organization with no associates / no resources / no memberships**: each section follows its own omission rule — Account Resources is omitted when empty; Lead Spaces is omitted when empty; All Memberships is rendered with an empty-state caption (parity with the current MUI's three different patterns).
- **VC profile rendered for a VC that returns 404 from `useVirtualContributorProfileWithModelCardQuery`**: the existing `isApolloNotFoundError` check renders `Error404` inside the CRD layout — parity with current MUI.

## Requirements

### Functional Requirements

#### Migration scope and routing

- **FR-001**: System MUST render the public **User Profile** page (`/user/:userSlug`), the public **Organization Profile** page (`/organization/:orgSlug`), and the public **Virtual Contributor Profile** page (`/vc/:vcSlug`) in CRD when `localStorage('alkemio-crd-enabled')` is `'true'`.
- **FR-002**: System MUST render the existing MUI `UserProfilePage`, `OrganizationPage`, and `VCProfilePage` unchanged when `localStorage('alkemio-crd-enabled')` is unset or any other value (default OFF).
- **FR-003**: System MUST gate the CRD vs. MUI choice via `useCrdEnabled` and add conditional blocks in `TopLevelRoutes.tsx` exactly as documented in `docs/crd/migration-guide.md` (lazy-loaded chunks for both versions; only the active chunk is fetched). Each of the three actor routes (`/user/*`, `/organization/*`, `/vc/*`) gets its own conditional block.
- **FR-004**: System MUST keep the existing MUI page files in place (`src/domain/community/user/userProfilePage/`, `src/domain/community/organization/pages/`, `src/domain/community/virtualContributor/vcProfilePage/`) and the existing entry wirings (`UserRoute.tsx`, `OrganizationRoute`, `VCRoute.tsx`); the CRD pages live under three sibling subfolders of `src/main/crdPages/topLevelPages/`:
  - User profile: `src/main/crdPages/topLevelPages/userPages/publicProfile/`
  - Organization profile: `src/main/crdPages/topLevelPages/organizationPages/publicProfile/`
  - VC profile: `src/main/crdPages/topLevelPages/vcPages/publicProfile/`
- **FR-005**: CRD page components MUST NOT import from `@mui/*` or `@emotion/*` and MUST NOT import GraphQL-generated types directly into views; data mappers in each page's `dataMapper.ts` are the only place GraphQL types meet CRD prop types.
- **FR-006**: CRD components in `src/crd/components/user/`, `src/crd/components/organization/`, and `src/crd/components/virtualContributor/` (heroes, sidebars, sections, etc.) MUST be presentational only (zero `@mui/*` imports, zero GraphQL-type imports, all behavior received as `on*` callback props per the CRD architectural rules in `src/crd/CLAUDE.md`).
- **FR-007**: System MUST resolve `/user/me` to the current user's profile exactly as the current MUI `UserMeRoute` does. There is no `/me` shorthand for Organization or VC — this is parity with current routing.
- **FR-008**: Each public profile page MUST be reachable under the same authentication conditions as today (parity with current MUI route wrappers — User route is wrapped by `NoIdentityRedirect`, Organization and VC routes follow their existing wrappers). Authentication-gated affordances inside each hero (Settings icon, Message button) follow their own gating predicates (FR-011, FR-012, FR-021, FR-031).
- **FR-009**: While the underlying data queries are in flight — for **User**: `useUserProvider`, `useUserAccountQuery`, `useUserContributions`, `useUserOrganizationIds`; for **Organization**: `useOrganizationProvider`, `useOrganizationAccountQuery`, `useAccountResources`; for **VC**: `useVirtualContributorProfileWithModelCardQuery` plus the BoK auxiliary queries (`useSpaceBodyOfKnowledgeAuthorizationPrivilegesQuery`, `useSpaceBodyOfKnowledgeAboutQuery`) for space-backed BoK and `useKnowledgeBase` for knowledge-base-backed BoK — each CRD profile page MUST render `Skeleton` placeholders (from `src/crd/primitives/skeleton`) sized to the eventual content. The hero skeleton MUST follow the actor's hero schema: User and Organization heroes show banner rectangle + circular avatar + name line + **location line**; the VC hero shows banner rectangle + circular avatar + name line and **omits the location placeholder** (parity with FR-030, which spells out no location field on the VC hero). Each sidebar section MUST render 3–4 stacked rectangles, and the main column 2–3 card-shaped placeholders. Skeletons MUST be replaced by real content **per region as each query resolves** (the page does not block on all queries before rendering). The query → region map is documented per actor in `data-model.md` ("Query → region"), and each `*PublicProfileViewProps` carries a `loading: { … }` shape with one boolean per region; the mapper produces it from the underlying Apollo `loading` flags. Rationale: prevents layout shift and gives immediate visual confirmation of load progress, consistent with sibling CRD pages.

#### User profile page (User Story 1)

- **FR-010**: The User profile hero MUST render the user's banner (or a deterministic gradient fallback computed via `pickColorFromId` from `@/crd/lib/pickColorFromId`), avatar, display name, and location line. The hero MUST NOT render an online-presence indicator (parity with current MUI; the prototype's mock dot is intentionally dropped).
- **FR-011**: The User profile hero MUST show a Settings icon button linking to `/user/:userSlug/settings/profile` (the profile being viewed) when the viewer satisfies `canEditSettings = currentUser.id === profileUser.id || hasPlatformPrivilege(AuthorizationPrivilege.PlatformAdmin)` — i.e., the profile owner OR a platform admin. The icon MUST be hidden for every other viewer (non-owner non-admin signed-in viewers, anonymous viewers). For admin viewers on another user's profile, the icon MUST coexist with the Message button (FR-012) since admins can both edit and message. The destination route is owned by sibling spec `097-crd-user-settings` (which evaluates the same predicate at the settings-shell route boundary).
- **FR-012**: The User profile hero MUST show a Message button only when the viewer is signed in and is **not** the profile owner. Clicking the button opens an in-hero compose surface; submitting fires the existing `useSendMessageToUsersMutation` via a callback prop with `{ message, receiverIds: [userId] }` — exact parity with the current MUI `UserPageBanner.handleSendMessage`. The mutation lives in the integration layer (`src/main/crdPages/topLevelPages/userPages/publicProfile/`); the CRD hero component receives `onSendMessage(messageText: string): Promise<void>` as a prop and knows nothing about GraphQL.
- **FR-013**: The User profile MUST render a sticky 5-tab resource strip (All Resources, Hosted Spaces, Virtual Contributors, Leading, Member of) with **All Resources** active by default. Each tab maps to exactly one view of the underlying 3 resource sections (Resources Hosted [hosted spaces + hosted VCs], Spaces Leading, Member of) per the following table:
  - **All Resources** — render all 3 sections **with** their section headers in the documented order.
  - **Hosted Spaces** — render only the hosted-spaces slice of Resources Hosted; hide the VCs sub-list and the other two sections; section header hidden (the tab label is the heading).
  - **Virtual Contributors** — render only the hosted-VCs slice of Resources Hosted; hide the spaces sub-list and the other two sections; section header hidden.
  - **Leading** — render only Spaces Leading; section header hidden.
  - **Member of** — render only Member of; section header hidden.
  The "Spaces Leading" set is computed from `useFilteredMemberships(contributions, [RoleType.Lead, RoleType.Admin])` — the same filter input the current MUI `UserProfilePageView` uses today (a contribution counts as "leading" if the user holds Lead OR Admin on it). Empty-state / section-omission rules (FR-015) apply per slice within whichever tab is active. The active tab is held in **local React state** (`useState('allResources')`) — it MUST NOT be synced to a URL search param or path segment in this iteration. Direct page entry (reload, back/forward, shared link) always lands on All Resources. On viewports below the `md` breakpoint the strip MUST horizontally scroll (`overflow-x-auto no-scrollbar`); all five tabs MUST remain rendered inline (no dropdown / hamburger / two-row wrap variant) and the active tab MUST be auto-scrolled into view on mount and on every tab change.
- **FR-014**: The User profile MUST reuse the same CRD space-card primitive that `043-crd-search-dialog` and the planned Explore Spaces page consume. No new space-card variant is introduced.
- **FR-015**: When a section has no items, the section MUST be either omitted or replaced by a CRD empty-state message — parity with current MUI behavior. The exact rule (omit vs. show empty caption) is documented per section in the page's mapper.
- **FR-016**: Pagination behavior is **per-actor**, matching each page's design source:
  - **User profile** — every section (Resources Hosted, Spaces Leading, Member of) MUST render every item the data hooks return: no pagination cap, no "Show more" affordance, no Next/Previous controls, no infinite scroll. The grid container handles overflow via standard page scrolling. This matches the prototype (`prototype/src/app/pages/UserProfilePage.tsx`) — the prototype renders every item of every list. NOTE: this is a deliberate deviation from current MUI `AccountResourcesView`, which paginates user-hosted spaces at `VISIBLE_SPACE_LIMIT = 6` with a "Show all" affordance. The prototype removes that cap; CRD follows the prototype.
  - **Organization profile — Account Resources** MUST preserve current MUI `AccountResourcesView` exactly: hosted spaces are shown up to `VISIBLE_SPACE_LIMIT = 6` with a "Show all" button that, when clicked, expands to the full list (no second collapse). Innovation packs and innovation hubs render every item (no cap — parity with current MUI).
  - **Organization profile — Lead Spaces and All Memberships** MUST render every item (no cap), matching current MUI `TilesContributionsView`.
  - **VC profile** has no list sections subject to this rule.

#### Organization profile page (User Story 2)

- **FR-020**: The Organization profile hero MUST render the organization's banner (or a deterministic gradient fallback via `pickColorFromId(organization.id)`), avatar/logo, display name, location line, and a **Verified** badge when `organization.verification.status === VerifiedManualAttestation` (parity with current MUI).
- **FR-021**: The Organization profile hero MUST show a Settings icon button linking to `buildSettingsUrl(organization.profile.url)` (the existing `/organization/<slug>/settings/...` admin URL) when the viewer satisfies `canEdit` per the existing `useOrganizationProvider().permissions.canEdit`. The icon MUST be hidden for viewers without `canEdit`. The destination route is **out of scope** for CRD migration in this spec — clicking it lands the user on the existing MUI admin shell.
- **FR-022**: The Organization profile hero MUST show a Message button for any signed-in viewer (parity with current MUI `OrganizationPageBanner.onSendMessage`). Clicking the button opens an in-hero compose surface; submitting fires the existing **`useSendMessageToOrganizationMutation`** with input `{ message, organizationId }` (parity with current MUI `useOrganizationProvider().handleSendMessage`). The mutation lives in the integration layer; the CRD hero component receives `onSendMessage(messageText: string): Promise<void>` as a prop and is recipient-agnostic — only the integration helper knows which mutation to call. The button is hidden for anonymous viewers. **Note (correction vs. earlier drafts)**: an earlier draft of this spec assumed User and Organization shared `useSendMessageToUsersMutation` with `{ receiverIds: [orgId] }` — that was incorrect. The Organization vertical uses its own dedicated mutation with input shape `{ message, organizationId }`. The implementation provides two integration helpers — `useSendMessageToUserHandler` and `useSendMessageToOrganizationHandler` — both exposing the same `(text: string) => Promise<void>` API so the shared `MessagePopover` primitive stays recipient-agnostic.
- **FR-023**: The Organization profile sidebar (`col-span-3` on `md+`, single column below) MUST render five sections in this order, all parity ports of the current MUI:
  - **Bio** — markdown rendering of `profile.description`.
  - **Tagsets** — Keywords + Capabilities as compact tag pills.
  - **References** — non-social links from `profile.references[]` as labeled URL chips (filtered via `isSocialNetworkSupported`).
  - **Social** — social-network references (the "social" group from `isSocialNetworkSupported`) rendered with brand-style icons. **Note**: `lucide-react` no longer ships brand icons (LinkedIn / GitHub / Twitter / YouTube were removed in recent versions due to trademark considerations); the implementation falls back to a generic `Link2` glyph with the platform name conveyed via `aria-label` and the link target. A future enhancement may introduce a dedicated CRD primitive with exact-fidelity brand SVGs if product asks. Section is hidden when there are no social references.
  - **Associates** — port of the current MUI `AssociatesView` exactly: shows up to `ASSOCIATE_CARDS_COUNT = 12` associate avatars by default, with a "Show more (N) / Show less" link that toggles the remaining set in-place. The section header always renders (with associate `totalCount` from `metrics[Associate]`). When the viewer has `permissions.canReadUsers`, the avatar grid is shown. When the viewer lacks `canReadUsers`, the body shows the existing sign-in CTA copy (i18n key `associates-view.sign-in`) instead of the avatar grid — the section is **not** hidden. This is parity with current MUI; no behavior change.
- **FR-024**: The Organization profile right column (`col-span-9` on `md+`) MUST render three sections in this order, parity with current MUI `OrganizationPageView`:
  - **Account Resources** — combined hosted spaces + innovation packs + innovation hubs. Section omitted entirely when all three lists are empty (matches current MUI's `hasAccountResources` check). Hosted-spaces pagination follows FR-016 (Org rule: 6-cap + "Show all" parity).
  - **Lead Spaces** — filtered via `useFilteredMemberships(contributions, [RoleType.Lead])`; the Org-profile filter input is `[Lead]` only (current MUI parity — `OrganizationPageView` passes only `RoleType.Lead`, not Admin). Section omitted when empty.
  - **All Memberships** — remaining memberships. When empty, render a titled card with the empty-state caption "No memberships yet" (reuse the existing `pages.user-profile.communities.noMembership` translation key per FR-102).
- **FR-025**: The Organization profile MUST be read-only — no inline editing, no add/remove operations on associates or resources from the page. All editing flows live in the existing MUI `/organization/<slug>/settings/...` admin shell (out of scope for this spec).

#### Virtual Contributor profile page (User Story 3)

- **FR-030**: The VC profile hero MUST render the VC's banner (or a deterministic gradient fallback via `pickColorFromId(vc.id)`), avatar/icon, and display name. The hero MUST NOT render a Message button (parity with current MUI `VCPageBanner`).
- **FR-031**: The VC profile hero MUST show a Settings icon button linking to `buildSettingsUrl(vc.profile.url)` (the existing `/vc/<slug>/settings/...` admin URL) when `vc.authorization.myPrivileges` includes `AuthorizationPrivilege.Update`. The icon MUST be hidden for viewers without the privilege. The destination route is **out of scope** for CRD migration in this spec — clicking it lands the user on the existing MUI admin shell.
- **FR-032**: The VC profile sidebar (`col-span-3` on `md+`, single column below) MUST render four sections in this order: **Description** (markdown rendering of `vc.profile.description`), **Host** (compact card showing `vc.provider.profile` — display name + avatar + link to the provider's profile, parity with current MUI `ContributorCardHorizontal seamless`), **References** (only the non-social references — filter via `isSocialNetworkSupported`; empty-state line when the list is empty), **Body of Knowledge** (variants per FR-033).
- **FR-033**: The VC profile **Body of Knowledge** section MUST render one of three variants based on `vc.bodyOfKnowledgeType`:
  - `AlkemioSpace` — descriptive text from `vc.bodyOfKnowledgeDescription` (rendered above the card regardless of read access) + a `SpaceCardHorizontal`-equivalent CRD card linking to the backing space. When the viewer lacks the space's `ReadAbout` privilege (verified via `useSpaceBodyOfKnowledgeAuthorizationPrivilegesQuery`), the card MUST show a "Private space" placeholder (parity with current MUI default profile fallback — `displayName` set to the i18n key `components.card.privacy.private` interpolated with `entity: 'space'`, empty URL).
  - `AlkemioKnowledgeBase` — descriptive text + a **Visit** button linking to `${vc.profile.url}/${KNOWLEDGE_BASE_PATH}`. The descriptive text is `useKnowledgeBase().knowledgeBaseDescription` when populated; **when empty, fall back to the placeholder copy `t('virtualContributorSpaceSettings.placeholder')`** — exact parity with current MUI `VCProfilePageView` (`knowledgeBaseDescription || t('virtualContributorSpaceSettings.placeholder')`). When the viewer lacks `hasReadAccess`, the Visit button MUST be disabled with the tooltip from the existing translation key `components.profile.fields.bodyOfKnowledge.privateBokTooltip` (parity reuse per FR-102; no new CRD-namespace key introduced for this string).
  - **External** (no BoK) — copy describing the engine type ("Assistant" vs. other) per `vc.aiPersona.engine`; reuses the existing `components.profile.fields.engines.externalVCDescription` interpolation key with `engineName` from `components.profile.fields.engines.externalAssistant` or `components.profile.fields.engines.external` (FR-102 parity reuse — no new CRD-namespace keys for these three strings).
  - **No BoK at all** — when `vc.bodyOfKnowledgeType` is unset and `modelCard.aiEngine.isExternal === false` (an unusual data state where none of the three variants apply), the BoK section MUST be omitted entirely. The mapper produces `bodyOfKnowledge: null` and the view skips the section — no header, no empty-state caption. This matches the current MUI `VCProfilePageView` behavior, which renders the BoK section only when one of `hasSpaceKnowledge` / `hasKnowledgeBase` / `isExternal` is true.
- **FR-034**: The VC profile right column (`col-span-9` on `md+`) MUST render the **VC content view**. **Important deviation from "exact MUI parity"** (corrected vs. earlier drafts): the current MUI `VCProfileContentView` renders three sections (`functionality`, `aiEngine`, `monitoring`) using **hard-coded data** sourced from `useTemporaryHardCodedVCProfilePageData(modelCard)` — there is a `// REMOVE when data is fetched from server and use Trans!` TODO comment in the MUI source confirming this is a placeholder. Additionally, the MUI VCProfileContentView does NOT render social references at all (the social group is computed in the sidebar view but never displayed). The CRD content view therefore **modernizes** rather than parity-restyles: it (a) renders the **real** `modelCard.aiEngine` data from the GraphQL `VirtualContributorModelCard` (engine name + isExternal + hostingLocation + additional technical details), and (b) adds a Social Links sub-section that surfaces the social-group references (from `isSocialNetworkSupported`) as link rows with a generic icon — same `lucide-react` brand-icon fallback as FR-023's Organization Social sub-block (lucide brand icons no longer ship; a generic `Link2` is used with platform identity in `aria-label`). The MUI hard-coded `functionality` and `monitoring` blocks are out of scope and not ported; they are TODO content in MUI and re-implementing them as hard-coded blocks in CRD would entrench tech debt.
- **FR-035**: The VC profile MUST honor the existing `useRestrictedRedirect` (with `requiredPrivilege: AuthorizationPrivilege.Read`) — viewers without the Read privilege are redirected exactly as today.
- **FR-036**: When `useVirtualContributorProfileWithModelCardQuery` returns a not-found error (`isApolloNotFoundError`), the page MUST render the existing `Error404` component inside the CRD layout (parity with current MUI).
- **FR-037**: The VC profile MUST be read-only — no inline editing. All editing flows live in the existing MUI `/vc/<slug>/settings/...` admin shell (out of scope for this spec).

#### Internationalization

- **FR-100**: All user-visible strings on the CRD profile pages MUST live in `src/crd/i18n/profilePages/profilePages.<lang>.json`. The folder name is `profilePages/`; the i18n **namespace key** registered in `src/core/i18n/config.ts` and `@types/i18next.d.ts` is `crd-profilePages` (CRD-namespace prefix per project convention — same pattern as `crd-exploreSpaces`, `crd-userSettings`). One combined namespace covers all three actor profiles, per `src/crd/CLAUDE.md`.
- **FR-101**: All six supported languages (en, nl, es, bg, de, fr) MUST be created and edited in the same PR that introduces or removes a key, per the CRD i18n manual workflow (no Crowdin).
- **FR-102**: Where current MUI uses a generic translation key already defined in `src/core/i18n/en/translation.en.json` (e.g., `pages.user-profile.communities.noMembership`, `components.contributions.allMembershipsTitle`, `components.profile.fields.bodyOfKnowledge.title`), the CRD page MAY reuse that existing key via the `translation` namespace rather than duplicating it under `crd-profilePages`. The decision per key is recorded in the page's data-mapper.

#### Accessibility

- **FR-110**: Every interactive element on every CRD profile page MUST meet WCAG 2.1 AA — semantic HTML, visible focus indicators, accessible names on icon-only buttons (Settings, Message, tab strip controls, BoK Visit button), keyboard reachability, and `aria-busy` on async-pending buttons.
- **FR-111**: The User profile resource tab strip MUST be navigable by keyboard (Tab into the strip, Left/Right arrows between tabs, Enter to activate) — same pattern the existing CRD tabs primitive provides. Organization and VC profiles do not have a tab strip — they render their sections statically in the documented order.

### Key Entities

- **User** — the User profile being viewed. Sourced from `useUserProvider` (which wraps `useUserModelFullQuery`). Key attributes consumed by CRD: `id`, `nameID`, top-level identity (`firstName`, `lastName`), `profile.{id, displayName, tagline, description, location.{city, country}, avatar, banner, references}`.
- **Organization** — the Organization profile being viewed. Sourced from `useOrganizationProvider` + `useOrganizationAccountQuery`. Key attributes: `id`, `nameID`, `profile.{id, displayName, description, location, avatar, banner, references, url}`, `verification.status`, `metrics`, `contributions` (memberships), `permissions.{canEdit, canReadUsers}`, `capabilities`, `keywords`, `associates`.
- **Virtual Contributor** — the VC profile being viewed. Sourced from `useVirtualContributorProfileWithModelCardQuery` + the BoK auxiliary queries. Key attributes: `id`, `nameID`, `profile.{id, displayName, description, avatar, banner, references, url}`, `provider.profile`, `bodyOfKnowledgeType`, `bodyOfKnowledgeID`, `bodyOfKnowledgeDescription`, `aiPersona.engine`, `modelCard`, `authorization.myPrivileges`.
- **Membership** — an entry in the contributor's membership list (User: `rolesUser.spaces`; Organization: `useOrganizationProvider().contributions`). Attributes used by CRD: `id`, `displayName`, `level`, role flags. Mapped to `SpaceHostedItem` for the existing tile views.
- **AssociatedOrganization** (User profile only) — an organization the user is a member of. Sourced via `useUserOrganizationIds` + lazy fetch. Attributes: `id`, `profile.{displayName, description, location, avatar, url}`, `verified`, `associatesCount`, role.
- **HostedResource** — a hosted space, virtual contributor, innovation pack, or innovation hub the contributor owns. Listed in the Resources Hosted (User) or Account Resources (Organization) section.
- **Associate** (Organization profile only) — a member of the organization. Attributes: `id`, `profile.{displayName, avatar, url}`. Sourced via the existing organization provider.
- **BodyOfKnowledge** (VC profile only) — describes a VC's knowledge source. Discriminated union: `{ kind: 'space', spaceId, profile, hasReadAccess }`, `{ kind: 'knowledgeBase', description, hasReadAccess, visitUrl }`, or `{ kind: 'external', engineLabel }`.

## Success Criteria

### Measurable Outcomes

- **SC-001**: Any signed-in viewer can open `/user/<other>`, `/organization/<some-org>`, and `/vc/<some-vc>` and scan the hero + sidebar + main sections of each in under 5 seconds with no console errors and no failed network calls.
- **SC-002**: 100% of the URLs that resolve to a public User, Organization, or VC profile in MUI continue to resolve to the equivalent page in CRD with no broken links — verified by an automated route smoke test covering all three actor types.
- **SC-003**: The Message and Settings affordances on each hero are visible exactly per the per-actor matrix specified by FR-011/FR-012 (User), FR-021/FR-022 (Organization), FR-031 (VC) — verified by automated tests covering each viewer category (owner / admin / non-admin / anonymous) for all three actor types.
- **SC-004**: The lighthouse / axe accessibility audit reports zero critical or serious violations on each of the three CRD profile pages.
- **SC-005**: Bundle size delta from this migration MUST NOT exceed +35 KB gzipped across the three new lazy-loaded chunks combined (User profile + Organization profile + VC profile) over the prior build (verified via `pnpm analyze`).
- **SC-006**: Public-profile resource sections render correctly for the parity matrix per actor type — User: 0 / 1 / 50+ memberships across L0 / L1 / L2 spaces; Organization: 0 / 1 / 50+ memberships, account resources present and absent, verified and unverified; VC: each of the three BoK variants (`AlkemioSpace`, `AlkemioKnowledgeBase`, `External`) — verified manually with seeded test fixtures.
- **SC-007**: When CRD is disabled via the localStorage toggle, every URL above renders the existing MUI page unchanged — verified by an automated CRD-off smoke test.

## Assumptions

- The CRD shell (`CrdLayoutWrapper`, header, footer, dialogs) and the design-system toggle (`useCrdEnabled`) are already in place and reused without modification — established by spec `041-crd-dashboard-page` and reused by spec `045-crd-space-settings`.
- The reusable CRD space-card primitive (used on the User profile's resource sections and the Organization profile's account resources / leading / membership sections) already exists or will be reused from `043-crd-search-dialog` / the in-flight Explore Spaces page; this spec does not introduce a new variant.
- A reusable CRD `ContributorCardHorizontal`-equivalent primitive is needed for the VC profile's Host section. It does not yet exist as a CRD primitive; this spec introduces a small `CompactContributorCard` under `src/crd/components/common/` that the VC page consumes. It is also reused by the User profile's Organizations sidebar (`caption=role`, `secondaryCaption=member-count`). NOTE: the Organization profile's Associates section does **not** use `CompactContributorCard` — it renders an avatar grid (parity port of MUI `ContributorCardSquare`-shaped data via `AssociatesView`).
- A reusable recipient-agnostic in-hero compose surface is needed for the User and Organization profile heroes' Message buttons. This spec introduces a small `MessagePopover` component under `src/crd/components/common/` (placed in `common/` from day one rather than the User vertical so the Organization hero does not cross-import). Both heroes consume it via a uniform `onSendMessage(text: string): Promise<void>` callback prop; the popover itself is recipient-agnostic. The VC profile does not have a Message button (FR-030), so does not consume `MessagePopover`.
- The seven-tab User Settings shell that the User profile's Settings (gear) icon links into is owned by sibling spec `097-crd-user-settings`. Both specs ship together as a single user-vertical release; the gating predicate `useCrdEnabled` is the same for both.
- Organization and VC settings/admin shells are **not** migrated in this spec. The Settings (gear) icons on the Org and VC heroes link to the existing MUI admin shells (`/organization/<slug>/settings/...` and `/vc/<slug>/settings/...`). When the user clicks through, they land on a MUI page; this is consistent with the sibling-spec 097's Account-tab navigation pattern (CRD page → MUI admin route for heavy flows).
- The existing data hooks (`useUserProvider`, `useUserContributions`, `useUserOrganizationIds`, `useSendMessageToUsersMutation`, `useOrganizationProvider`, `useOrganizationAccountQuery`, `useAccountResources`, `useFilteredMemberships`, `useVirtualContributorProfileWithModelCardQuery`, `useSpaceBodyOfKnowledgeAuthorizationPrivilegesQuery`, `useSpaceBodyOfKnowledgeAboutQuery`, `useKnowledgeBase`, `useUrlResolver`, `useRestrictedRedirect`) are sufficient — **no GraphQL schema change is required**.
- **The User profile prototype is treated as a visual reference, not an authoritative spec.** Where the prototype invents a capability that does not exist on the backend (online-presence dot), CRD drops it unless explicitly clarified otherwise. **There is no prototype for Organization or VC profiles** — those CRD pages adopt the same architectural pattern (CRD shell + integration layer + presentational components) as the User profile, with parity to the current MUI for content and data; the visual treatment is consistent with the User profile's CRD design language (banner + avatar overlay, sidebar + main column, card-based sections).
- Translations are managed manually (AI-assisted) for all six languages in the same PR; no Crowdin involvement (per `src/crd/CLAUDE.md`).
- The Message button on the User and Organization heroes invokes the existing send-message mutation via a callback prop on the CRD hero component (parity with the current MUI flows). It does **not** route through the global Messages dialog — it is a one-shot send-message-to-this-recipient surface.

## Out of Scope

- **The seven User Settings tabs** (`/user/:userSlug/settings/{profile,account,membership,organizations,notifications,settings,security}`) are owned by sibling spec `097-crd-user-settings`, not this spec.
- **The Organization settings/admin shell** (`/organization/:orgSlug/settings/...`) — including the Organization Profile, Associates Management, Verification, Notifications, etc. tabs. The existing MUI admin pages stay in MUI under this spec; the Settings (gear) icon on the CRD Org hero links to the existing MUI admin URL. Migration of those tabs to CRD is a future spec.
- **The Virtual Contributor settings/admin shell** (`/vc/:vcSlug/settings/...`) — including model card editing, BoK configuration, etc. Same arrangement as Organization: the icon links to MUI; migration is a future spec.
- **No new backend capabilities.** This migration is a presentation-layer port. No new GraphQL types, no new mutations, no new role gating, no new permission semantics.
- **No "follow" / "connect" social affordances** on any public profile page. The User profile's prototype hints at social-network features (online dot) that do not exist; no friend / connection system is introduced for any actor type.
- **No deletion of MUI files.** The current MUI pages stay in place until the CRD toggle is removed globally — parity with the rule established in `docs/crd/migration-guide.md`.
- **No public profile editing.** All edits happen exclusively in the actor's settings/admin shell (out of scope per the bullets above).
- **No mobile-app-specific layouts.** All three pages are responsive (single-column on `< md`) but no native-app shell is targeted.
- **No restyle of Kratos-rendered form fields, BoK markdown rendering, or model-card details beyond the surrounding card shell.** The CRD migration restyles the outer page composition; nested rendered content keeps its current presentation unless explicitly called out in the FRs.
- **No design refresh of the VC model card or social links presentation.** The CRD VC content view is a parity restyle of the current MUI `VCProfileContentView` — every element is preserved.
- **User profile — innovation packs and innovation hubs are NOT rendered.** The current MUI `AccountResourcesView` shows hosted innovation packs and innovation hubs alongside hosted spaces and hosted virtual contributors when the user owns any. The prototype `prototype/src/app/pages/UserProfilePage.tsx` deliberately omits both — the Resources Hosted section in the prototype contains only hosted spaces and hosted virtual contributors. CRD follows the prototype: users who hosted innovation packs or hubs in MUI will not see those sections on their CRD User profile. Re-introducing them is a future enhancement; if/when product asks for it, the existing `useAccountResources` already returns the data so a follow-up spec can surface them without backend changes.
- **Standalone preview demo pages.** Four routes are wired into the standalone CRD preview app (`src/crd/app/`, served by `pnpm crd:dev`) so designers can iterate without the full backend: `/user/me` (self-view of the current user), `/user/alex-rivera` (viewing another user — Alex Rivera mock data borrowed from the prototype), `/organization/alkemio` (Org profile with Verified badge + 14 associates + Account Resources), `/vc/datasynth-bot` (VC profile with the `space` BoK variant). All four are powered by plain TypeScript mock data in `src/crd/app/data/profiles.ts`; no Apollo / GraphQL / integration layer is involved. These demos are not part of the production app — they are a design-iteration tool.

- **VC profile — `${KNOWLEDGE_BASE_PATH}/*` subroute stays in MUI.** The existing MUI `VCRoute` mounts `<VCKnowledgeBaseRoute />` at `/vc/:vcSlug/knowledge-base/*`. The CRD migration covers only the `/vc/:vcSlug` profile index. The CRD `CrdVCRoutes` MUST nest the existing MUI `VCKnowledgeBaseRoute` at the same path so the URL keeps working when CRD is on; migration of the knowledge-base sub-pages to CRD is a future spec.
