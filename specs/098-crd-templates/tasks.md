# Tasks: CRD Templates System

**Input**: `/specs/098-crd-templates/` — `plan.md` (rev. 4 — post-`/speckit.analyze` interview, 2026-05-12), `spec.md` (revised after analyze + the MUI-reality pass + the 2026-05-12 interview — 9 user stories US1–US9, FR-001..FR-071 incl. FR-038 the CRD community-guidelines editor, SC-001..SC-008), `research.md` (refreshed 2026-05-12), `data-model.md`, `contracts/*.ts`, `quickstart.md`, `assumptions.md` (update log = decision status). Note: `analysis.md` is the first-pass point-in-time report and is superseded — the latest clearance state is the 2026-05-12 interview decisions (spec.md "Clarifications" session 2026-05-12 / `assumptions.md` update log); re-run `/speckit.analyze` for a fresh report.

**Tests**: targeted unit tests only — the pure mappers, the key integration hooks, the Callout-template form↔input mapping (incl. Collabora-document + poll framing), the no-dangling-default delete path, the `useSaveAsTemplate` pre-fill (incl. community-guidelines title). No exhaustive component suite; no TDD.

**Delivery**: one PR (the phases below are internal build-order milestones, not separate merge points — natural split-line if it grows unmanageable is Phase 1+2+US1 ∣ the rest).

**Organization**: Phase 1 Setup → Phase 2 Foundational (open verifications + the complete CRD "templates kit" — blocking for every story) → Phases 3–11 one per user story in priority order (US1, US2, US3 = P1; US4, US5, US6, US9 = P2; US7, US8 = P3) → Phase 12 Polish. Task IDs are contiguous (T001…); `[P]` = parallelisable (different files, no incomplete deps); `[USn]` on user-story tasks only.

> **STATUS (unattended `/speckit-implement` pass, user offline):** the CRD-pure "templates kit" (Phase 1 + the `src/crd/` half of Phase 2) is built; `crd-templates` i18n namespace is registered (en populated, other langs are en-copies); the pure mappers are built; `TemplateContent.graphql` got the V1/V2 fragment edit (codegen pending). Integration-layer hooks (`useTemplatePicker`, `useTemplatesManager`) + the US3 `TemplateImportConnector` re-point are best-effort/partial. NOT done: `useTemplateForms` / `CalloutTemplateForm`, `useSetDefaultTemplate`, `useSaveAsTemplate` + the `CalloutSettingsConnector` MUI-bridge removal, the US1 cutover in `CrdSpaceSettingsPage.tsx`, the `045`-dialog deletions, the FR-038 `CommunityGuidelinesEditor` rewrite, all unit tests, and everything in US2/US4–US9 + Phase 12. **Nothing was compiled/linted/codegen'd/tested.** See `specs/098-crd-templates/{assumptions,incongruencies}.md` for the full ledger and the `TODO(098)` markers in the code. A `[~]` below ≈ "partial / best-effort".

**Path conventions**: presentational CRD components under `src/crd/components/{templates,innovationPack,innovationLibrary}/` (no MUI/Apollo/router/formik; plain-TS props; callbacks for behaviour; Tailwind + lucide-react + semantic typography; `useTranslation('crd-templates')`; markdown via `MarkdownContent`/`InlineMarkdown`; `date-fns`; no `useMemo`/`useCallback`/`React.memo`). Integration (data/mappers/mutations/routing/toggle/bridge-replacements) under `src/main/crdPages/{templates,innovationPack,innovationLibrary}/` and `src/main/crdPages/topLevelPages/spaceSettings/{templates,subspaces,layout,community}/`. The `045`-era files under `src/crd/components/space/settings/` are refactored-in-place (section view → ≤10-line wrapper; dialogs → removed/moved/generalised). Legacy `src/domain/{templates,templates-manager,InnovationPack,spaceAdmin/SpaceAdminTemplates}/`, `src/domain/platformAdmin/domain/innovationPacks/` (the `/admin/innovation-packs` list — NOT migrated by this feature, FR-037), and `src/main/topLevelPages/InnovationLibraryPage/` are UNCHANGED (serve toggle-off; read for reference). Routing edits land in `src/main/routing/TopLevelRoutes.tsx`, `src/domain/InnovationPack/InnovationPackRoute.tsx` (BOTH pack routes), and `src/domain/community/contributor/Account/ContributorAccountView.tsx` (create + delete pack) — NOT in `AdminInnovationPackRoutes.tsx`. Prototype refs (read-only): `prototype/src/app/components/{space/SpaceSettingsTemplates.tsx,template-library/*}` + `prototype/src/app/pages/Template*Page.tsx` + `prototype/src/app/data/template-data.ts`.

---

## Phase 1: Setup (Shared Infrastructure)

- [X] T001 [P] Audit the shadcn primitives this feature needs (`collapsible`, `dropdown-menu`, `scroll-area`, `select`, `switch`, `tabs`, `badge`, `card`, `dialog`, `skeleton`, `tooltip`, `separator`, `input`, `textarea`, `popover`, segmented-control / `command` for the type filter) against `src/crd/primitives/`; port any missing one verbatim from `prototype/src/app/components/ui/` into `src/crd/primitives/<name>.tsx` (update `cn()` import to `@/crd/lib/utils`; verify zero MUI imports). *(Session 30 audit: all 14 required primitives present in `src/crd/primitives/` (29 primitives total). `command`/`segmented-control` are NOT needed — `TemplateTypeFilter` uses `Badge` + `Button`. Zero MUI imports across all 29 primitives.)*
- [X] T002 [P] Scaffold the `crd-templates` i18n namespace: create empty `src/crd/i18n/templates/templates.{en,nl,es,bg,de,fr}.json` (`{}`); register `'crd-templates'` in `crdNamespaceImports` in `src/core/i18n/config.ts`; add the namespace type entry in `@types/i18next.d.ts`; wire it into `src/crd/app/main.tsx` if that eagerly imports CRD namespaces.
- [X] T003 [P] Create `src/crd/components/templates/types.ts` — the CRD prop types from `contracts/`: `TemplateType` string union + `TEMPLATE_TYPE_ORDER`; `TemplateCardData`; `TemplateCategorySection`; `TemplateAction`; **`FramingKind` — re-export the existing CRD callout-layer union (`FramingChip` = `'none' | 'whiteboard' | 'memo' | 'document' | 'cta' | 'image' | 'poll'`, from `src/crd/forms/callout/types.ts`); do NOT define a parallel `FramingKind`** (the Callout template form reuses those connectors; `'none'` = plain text, `'document'` = Collabora-document per FR-020/FR-030, `'cta'` = link, `'image'` = media-gallery, `'poll'` = poll — **poll content is in scope** (V2/T005 add it to the `CalloutTemplateContent` fragment), so the type carries it like the others); the `TemplateContent` discriminated union (per `contracts/template-preview.ts` + `data-model.md` §B — incl. `framingCollaboraDoc?` and `framingPoll?` ( `{ question; options? }` ), and the `communityGuidelines` branch's `title: string`); the `TemplateFormValues` discriminated union (incl. the `whiteboard` branch's `previewSettings?` and the `communityGuidelines` branch's `title`); `TemplateFormErrors`; `ReferenceRow`; the `CommunityGuidelinesEditor*` prop types (FR-038, per `data-model.md` §B). Plain TS only.

---

## Phase 2: Foundational — open verifications + the CRD "templates kit" (BLOCKING PREREQUISITE for all user stories)

> **No user-story work may begin until this phase is complete.** Do the V-tasks first — they decide whether a GraphQL change is needed.

### Open verifications (do first; see plan rev. 4 §"Open verifications")

> **All four are additive `.graphql` *fragment* edits (no new documents) + `pnpm codegen` + a committed schema diff (Constitution III). First, for each, verify the field exists server-side on the type the fragment reaches — the schema enum already has `CollaboraDocument` + `Poll`, and `CommunityGuidelines` has a profile displayName/references, so these are expected to be fragment-only changes; anything that needs a real backend change is a BLOCKER raised before the dependent UI is built (not a quiet workaround).** Record every outcome in `research.md`.

- [~] T004 [P] **V1 + V2** — extend `CalloutTemplateContent` to carry **Collabora-document** framing (V1) and **poll** framing (V2). The fragment currently fetches `framing.{type, whiteboard{content}, link, memo, mediaGallery}` only — no Collabora-document field, no poll field — but a Callout template must store both (spec FR-020/FR-030; both confirmed in scope, see spec Clarifications D2). Add the two fields to `src/domain/templates/graphql/TemplateContent.graphql` (after confirming they exist on `CalloutFraming`/the framing type server-side), run `pnpm codegen`, prepare the committed schema diff. *(Confirmed gaps.)*
- [~] T005 [P] **V5** — verify whether `CommunityGuidelinesTemplateContent` exposes a distinct **title** for the guidelines (it likely already fetches `profile.displayName`); if not, add it. Also confirm the **live** community-guidelines query (the one the CRD editor host uses — FR-038) exposes `profile.{displayName, description, references}`; add anything missing via codegen + schema diff. *(Likely already present — confirm; gates the CG template `title` field and the CG editor host.)*
- [X] T006 [P] **V3** — verify whether `deleteTemplate` server-side nulls referencing `TemplateDefault`s and the referencing innovation-flow-state default callout template. If the client must clear them (FR-019), verify it can learn *which* defaults/flow-states reference template X from existing queries; add a field via codegen if not. Record the outcome. *(Conditional.)* *(Session 32: retired — the user confirmed the backend cascades. No GraphQL change needed; the CRD client's responsibility is purely refetching the dependent queries after a delete. See T042 for the implementation.)*
- [X] T007 [P] **V4** — verify `useInnovationLibraryQuery` exposes everything the CRD gallery card needs (template displayName, description, tags, visual, type, owning-pack name + url); also check whether the legacy `LibraryTemplatesView` has a free-text search box (not just the type filter) — if it does, add it to the Library scope (FR-051) and the Library tasks (T062/T064). Record the outcome.

### Mappers (integration layer, pure functions)

- [X] T008 [P] `src/main/crdPages/templates/templateCardMapper.ts` — GraphQL `Template` → `TemplateCardData`: `bannerUrl = profile.visual?.uri || undefined`, `color = pickColorFromId(template.id)`, `type` mapped from the GraphQL enum to the CRD string union, `ownerLabel` undefined (set only by library/account callers).
- [X] T009 [P] `src/main/crdPages/templates/templatesManagerMapper.ts` — GraphQL `TemplatesSet` → `TemplateCategorySection[]` in `TEMPLATE_TYPE_ORDER` (uses `templateCardMapper`).
- [X] T010 [P] `src/main/crdPages/templates/templateContentMapper.ts` — port the per-type content-shaping logic from `src/domain/templates/components/Previews/*` as **pure functions** → the `TemplateContent` union; handle the **Collabora-document** and **poll** framing kinds (per T004's outcome — `framingCollaboraDoc` / `framingPoll`), and the `communityGuidelines` branch's `title` (= `profile.displayName`, per T005/V5).
- [X] T011 [P] Unit tests `src/main/crdPages/templates/__tests__/mappers.test.ts` — fixtures per template type (incl. callout-with-each-framing-kind — every kind incl. Collabora-document and poll, space-with-nested-subspaces, guidelines-with-title-and-references, missing-banner → `bannerUrl` undefined) exercising the three mappers.

### Manager view + card + previews

- [X] T012 [P] `src/crd/components/templates/TemplateCard.tsx` — one template card per `contracts/templates-manager.ts` (reuse / rewrite the `045` card per the prototype): type icon/badge, name, truncated description, tags, banner (real image else `color` gradient), a kebab `dropdown-menu` whose entries are gated by the `can*`-derived flags passed in, body click → `onPreview`. WCAG: `<button>`/`<a>` interactives, `aria-label` on the icon-only kebab. **No `isCustom` gate.**
- [X] T013 [P] `src/crd/components/templates/TemplateSectionHeader.tsx` — per-type header: icon, localized title + subtitle, template count, optional "Create new" + "Library" (import) buttons, collapse chevron.
- [X] T014 `src/crd/components/templates/TemplatesManagerView.tsx` — generalise `src/crd/components/space/settings/SpaceSettingsTemplatesView.tsx` to `TemplatesManagerViewProps` (`holderKind`, `categories`, `loading`, `duplicatingId`/`deletingId`, `canCreate/canEdit/canDelete/canImport(type)` predicates, `onCreate/onImport/onTemplateAction`); one collapsible section per type via `TemplateSectionHeader` + a grid of `TemplateCard`; section-visibility rule (`templates.length>0 || canCreate || canImport`); empty-but-addable → empty state + add affordance; inline client-side search box (visual only); per-row spinner on `duplicatingId`/`deletingId`. (Depends on T003, T012, T013.)
- [X] T015 [P] `src/crd/components/templates/preview/CalloutTemplatePreview.tsx` — renders the `{type:'callout'}` branch for **every framing kind**; **reuse the CRD callout-preview components** (`CalloutWhiteboardPreview`, `CalloutMemoPreview`, `CalloutCollaboraPreview` — read-only title/placeholder for the Collabora-document kind, no live document service, `CalloutMediaGalleryCarousel`, `CalloutLinkAction`) for the framing content; the **poll** framing renders its question + options read-only (reuse the CRD poll-preview component if one exists, else a simple read-only list); framing-kind badge, framing title + description (`MarkdownContent`), allowed-contribution chips, comments-enabled indicator, default post description (`MarkdownContent`), default whiteboard (read-only CRD whiteboard viewer).
- [X] T016 [P] `src/crd/components/templates/preview/WhiteboardTemplatePreview.tsx` — `{type:'whiteboard'}`: the drawing in the read-only CRD whiteboard viewer; preview-image fallback when content is empty.
- [X] T017 [P] `src/crd/components/templates/preview/PostTemplatePreview.tsx` — `{type:'post'}`: default description via `MarkdownContent`.
- [X] T018 [P] `src/crd/components/templates/preview/SpaceTemplatePreview.tsx` — `{type:'space'}`: innovation-flow phase chips (reuse the CRD flow-phase chip if one exists), starter-callout list (name + framing-kind icon), nested subspace-template names.
- [X] T019 [P] `src/crd/components/templates/preview/CommunityGuidelinesTemplatePreview.tsx` — `{type:'communityGuidelines'}`: the guidelines **title** (heading) + guidelines markdown body (`MarkdownContent`) + references list.
- [X] T020 `src/crd/components/templates/TemplateContentPreview.tsx` — dispatcher on `content.type` → the matching `preview/*` component; `loading` → skeleton (`role="status"` + `aria-label`); wraps content in a `ScrollArea`. (Depends on T015–T019.)
- [X] T021 `src/crd/components/templates/TemplatePreviewDialog.tsx` — move/evolve `src/crd/components/space/settings/TemplatePreviewDialog.tsx` to `TemplatePreviewDialogProps`: header from `TemplateCardData` (before the heavy content loads), lazy `content`, an "Edit" affordance shown only when `onEdit` is provided. (Depends on T020.)

### The reusable picker

- [X] T022 `src/crd/components/templates/TemplatePicker.tsx` (+ `TemplatePickerCard.tsx`) per `contracts/template-picker.ts`. Discriminated `mode`: `'import'` = persistent library manager (gallery, click→preview pane, "Import" adds a copy and the dialog **stays open**, templates in `alreadyInSet` marked "in this set" + removable, admin closes when done) — **Space-holder only; source sections = Account + Platform only (the Space's own set is not a source for importing into itself — matches the legacy `disableSpaceTemplates: true`)**; `'select'` = single-pick-then-apply (filter to `allowedTypes`, click→preview, confirm closes, persistent "No template / start blank"; **Space source section shown when there is a Space context**). Both share: a **free-text search filter** over name/description/tags (mirroring the legacy `ImportTemplatesDialogGallery`), the source sections (each omitted when empty; "no templates available" when all empty), the lazy `onPreview` → `previewContent` pane (uses `TemplateContentPreview`), `TemplatePickerCard`. **Supersedes `src/crd/components/space/settings/TemplateLibraryDialog.tsx`.** (Depends on T012, T020.)
- [X] T023 `src/main/crdPages/templates/useTemplatePicker.ts` — the `mode:'select'` picker hook for all consumption flows: loads the source lists by reusing `useImportTemplateDialogQuery` (Space) / `useImportTemplateDialogAccountTemplatesQuery` (Account) / `useImportTemplateDialogPlatformTemplatesQuery` (Platform), filtered to `allowedTypes`; `accountId` = the owning Space's account (`useSpace().space.accountId` / `SubspaceContext` at L1/L2); supports the picker's free-text search; lazy content for preview via `useTemplateContentLazyQuery` → `templateContentMapper`; exposes `openPicker()`, `pickerProps: TemplatePickerSelectProps`, `selectedTemplateId`, `selectedTemplateContent`, `clearSelection()`. (Depends on T008, T010.)
- [X] T024 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplatePicker.test.ts` — source loading + `allowedTypes` filtering + empty-source handling. (Depends on T023.) *(Session 34, 2026-05-14: implemented at `src/main/crdPages/templates/__tests__/useTemplatePicker.test.tsx` (7 tests). Pattern: `MockedProvider` + `renderHook` after `src/domain/collaboration/whiteboard/guestAccess/hooks/useGuestSession.spec.tsx`. Covers: closed-state defaults; `openPicker()` opens the picker; Space + Account + Platform sources all load + carry the expected ids; the Space source is omitted when `spaceTemplatesSetId` is absent; each source is filtered to `allowedTypes` (a Space template in a callout picker is dropped); Account-source cards are tagged with the owning pack `displayName` as `ownerLabel`; `clearSelection()` resets the transient selection. The earlier "no `MockedProvider` precedent" claim was a misread — there are 15+ MockedProvider tests in the repo.)*

### Per-type create/edit forms + the form dialog

- [X] T025 [P] **`src/main/crdPages/templates/CalloutTemplateForm.tsx`** — note the location: this is the **integration layer**, NOT `src/crd/` (the CRD layer rules forbid Apollo/`@/domain/*`/MUI imports, and this composes the callout-authoring connectors which import all three). A **thin shell over the existing CRD callout-authoring connectors**, not a bespoke editor: a framing-kind picker (the `FramingChip` union) → the matching CRD framing connector (`WhiteboardFramingConnector` / `MemoFramingConnector` / `CollaboraFramingConnector` (+`CollaboraFramingEditorOverlay`) / `MediaGalleryFramingConnector` / `CallToActionFramingConnector` / the poll framing connector if one exists, else a minimal poll editor / plain text), the contribution-type/comment-settings **controls** (the control components used inside `CalloutSettingsConnector` — import the controls, **not** the whole `CalloutSettingsConnector` module, which carries the legacy-MUI `CreateTemplateDialog` bridge), and contribution defaults via `ResponseDefaultsConnector`/`ResponseDefaultsDialog`. Covers **every** framing kind the live callout editor supports, incl. **Collabora-document** and **poll** (poll content persists per V2). Controlled (`value`/`onChange`/`errors`); form state lives in `useTemplateForms`, which passes this component into `TemplateFormDialog` via `perTypeFormSlot`. (Depends on T003; reuses `src/main/crdPages/space/callout/*` connectors + `calloutFormMapper.ts`. Note: `T036` removes the MUI bridge from `CalloutSettingsConnector` in this same Phase 2, so even if the whole connector is transitively reached, no MUI is pulled in.) Its "More options" References editor (the shared `@/crd/forms/references/ReferencesEditor` since the 2026-05-21 unification — see 042 callout-dialog T024c; rows + error keys adapted from the callout form model, now renamed to the GraphQL-aligned `{ name, uri }` row, so rows pass straight through) receives `onReferenceFileUpload`/`referenceUploadAccept` from `useTemplateForms`' `referenceUpload` arg → holder bucket, `temporaryLocation: true`, so the paperclip works in callout-template create AND edit (D24).
- [X] T026 [P] `src/crd/components/templates/forms/WhiteboardTemplateForm.tsx` — wraps `WhiteboardEditorShell` (the drawing) **plus the full preview-settings UI — `PreviewSettingsDialog` / `PreviewCropDialog` (crop/region), not just an image upload** (FR-020 — "the whiteboard drawing and its preview settings") — and a preview-image upload. Presentational (CRD-layer only — `WhiteboardEditorShell` / `PreviewSettingsDialog` / `PreviewCropDialog` live in `src/crd/components/whiteboard/` and are pure; if `PreviewSettingsDialog` turns out Apollo-bound, its wiring moves to `crdPages/templates/`, or fall back to image-only). Controlled (`value`/`onChange`/`errors`; `value.previewSettings` + `value.previewImageFile`). The drawing-editing affordance is rendered via the shared **`WhiteboardConfigCard`** (`src/crd/components/whiteboard/WhiteboardConfigCard.tsx`) — the same "configure / edit drawing" row the callout whiteboard-framing editor (`FramingEditorConnector`) uses (extracted to `src/crd/components/whiteboard/` so both consumers share one component) — wired to the live single-user whiteboard editor by `src/main/crdPages/templates/WhiteboardTemplateFormConnector.tsx`.
- [X] T027 [P] `src/crd/components/templates/forms/PostTemplateForm.tsx` — wraps `MarkdownEditor` for the default description. Presentational. Controlled.
- [X] T028 [P] `src/crd/components/templates/forms/SpaceTemplateForm.tsx` — **presentational**: a URL-paste source picker UI that takes `url` + `urlError` + `urlResolving` + `onUrlChange` + `onUseUrl` callbacks + the currently-resolved source space's `displayName`/`avatarUrl` as props (the URL-resolver + space-content + Update-privilege Apollo plumbing lives in `crdPages/templates/useTemplateForms` — NOT here) + "include nested subspaces" toggle + a read-only captured-structure preview (reuse `SpaceTemplatePreview` once content is fetched). Mirrors the legacy MUI `SpaceContentFromSpaceUrlForm` UX (paste URL → "Use this space" button → loads innovation-flow + callout titles). Re-selectable on edit. Controlled. (Depends on T018.)
- [X] T029 [P] `src/crd/components/templates/forms/CommunityGuidelinesTemplateForm.tsx` — a guidelines **title** field [required] + `MarkdownEditor` for the guidelines body + the shared `@/crd/forms/references/ReferencesEditor` (name/uri/description rows; add/remove; paperclip wired via `onReferenceFileUpload`/`referenceUploadAccept` from `useTemplateForms`' `referenceUpload` arg → holder bucket, `temporaryLocation: true`, in create AND edit — D24). Controlled. (Both this form and the `CommunityGuidelinesEditor` host — T057, FR-038 — use the one shared references editor; the bespoke `crd-templates` `ReferenceRowsEditor` was removed per D24.)
- [X] T030 `src/crd/components/templates/TemplateFormDialog.tsx` — **the CRD-native create/edit dialog that replaces both the legacy MUI `CreateTemplateDialog` bridge AND `src/crd/components/space/settings/TemplateEditDialog.tsx` (which only did profile+tags)**. Pure `src/crd/` shell — **does NOT import any per-type form**: title from `intent` + `type`; common fields (name [required], description, tags via `tags-input`, banner upload); a **`perTypeFormSlot: ReactNode`** prop into which the consumer (the integration layer / `useTemplateForms`) injects the right per-type form — the ShareDialog `shareOnAlkemioSlot` pattern; Save (disabled + `aria-busy` while `submitting`) / Cancel; closing with unsaved changes → `DiscardChangesDialog` (`ConfirmationDialog` variant). Also the surface a "save X as a template" flow opens, pre-filled. (Depends on T003. Note: it does NOT depend on T025/T026–T029 — those are injected via `perTypeFormSlot` by the integration layer.)
- [X] T031 `src/main/crdPages/templates/useTemplateForms.tsx` — per-type form state + validation + the create/edit mutations (`useCreateTemplateMutation` / `useCreateTemplateFromSpaceMutation` / `useCreateTemplateFromContentSpaceMutation` / `useUpdateTemplateMutation` / `useUpdateCalloutTemplateMutation` / `useUpdateTemplateFromSpaceMutation` / `useUpdateCommunityGuidelinesMutation` / `useUpdateWhiteboardMutation`); reuse the MUI-free `src/domain/templates/components/Forms/common/mappings.ts` for form→input mapping (confirm it has no MUI/Apollo imports); the Apollo **URL-resolver + space-content + Update-privilege** plumbing for the Space form's URL-paste source picker — `useUrlResolverLazyQuery` + `useSpaceTemplateContentLazyQuery` — mirroring the legacy `SpaceContentFromSpaceUrlForm` (paste URL → "Use this space" → load innovation-flow + callout titles → require Update privilege), with the resolved space's `displayName`/`avatarUrl` piped into the presentational `SpaceTemplateForm`; image uploads via `useHandlePreviewImages` + `useUploadMediaGalleryVisuals`; mutations in `useTransition`; exposes the `form` sub-state shape from `contracts/data-mapper.ts` (incl. the WB branch's `previewSettings` and the CG branch's required `title`), **assembles the `perTypeFormSlot` per `type`** (the pure `src/crd/.../forms/{Whiteboard,Post,CommunityGuidelines}TemplateForm` + the presentational `SpaceTemplateForm` for those types; `crdPages/templates/CalloutTemplateForm` for callout) and hands it to `TemplateFormDialog`, **plus** a helper to produce pre-filled `value` from a callout (every framing kind incl. Collabora-document + poll) / the current guidelines (title + body + references) / a subspace (for save-as). (Depends on T010, T025, T026–T029.) *Session 33 (2026-05-14): URL-paste source picker wired — `onUseSpaceUrl` resolves the pasted URL via `useUrlResolverLazyQuery`, validates `UrlType.Space`, fetches space content via `useSpaceTemplateContentLazyQuery`, requires `AuthorizationPrivilege.Update`, then commits `sourceSpaceId` + `spaceSourceDisplayName`/`AvatarUrl` into the form. On edit-open, a `useEffect` back-fills the resolved displayName/avatarUrl for an existing `sourceSpaceId`. Edit submit calls `updateTemplateFromSpace` when the user picked a different source than the one captured at open time (tracked via `spaceSourceInitialSpaceId`).*
- [X] T032 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplateForms.test.ts` — the Callout-template form↔mutation-input mapping (callout-with-each-framing-kind, incl. Collabora-document and poll) round-trips correctly; the CG-template form↔input mapping carries the `title`. (Depends on T031.)

### Set-default & save-as-template dialogs

- [X] T033 `src/crd/components/templates/SetDefaultTemplateDialog.tsx` — generalise `src/crd/components/space/settings/ChangeDefaultSubspaceTemplateDialog.tsx` to `SetDefaultTemplateDialogProps`: `purpose: 'defaultSubspaceTemplate' | 'flowStateDefaultCalloutTemplate'`, current default shown selected + a clear option, `onConfirm(templateId | null)`. **The two purposes have different source scopes (matching legacy)**: for `defaultSubspaceTemplate` the candidates are the holder's **own Space templates only** (the default must reference a template that is in the set — the legacy `SelectDefaultSpaceTemplateDialog` is a plain dropdown of those; CRD adds a candidate preview) — do **not** host the 3-source picker here; for `flowStateDefaultCalloutTemplate` host `TemplatePicker` in `mode:'select'` with the full Space/Account/Platform sources (the legacy innovation-flow `SetDefaultTemplateDialog` uses `ImportTemplatesDialog` with `selectedTemplateId` + `onRemoveTemplate`). **Remove `ChangeDefaultSubspaceTemplateDialog.tsx`.** (Depends on T022.)
- [X] T034 `src/main/crdPages/templates/useSetDefaultTemplate.ts` — wraps `useUpdateTemplateDefaultMutation` (Space per-type defaults) and `useSetDefaultCalloutTemplateOnInnovationFlowStateMutation` / `useRemoveDefaultCalloutTemplateOnInnovationFlowStateMutation` (flow-state default); exposes confirm + in-flight state. (Depends on T023.)
- [X] T035 `src/crd/components/templates/SaveAsTemplateDialog.tsx` — = `TemplateFormDialog` opened in `intent:'create'` with `value` **pre-filled** from a source entity: callout → Callout form (no-regression — replaces the CRD→MUI bridge), current community guidelines → Community Guidelines form (a deliberate **new** flow — legacy only *applies* a guidelines template), subspace → Space form with `sourceSpaceId` = the subspace (no-regression). Generalised from `src/crd/components/space/settings/SaveSubspaceAsTemplateDialog.tsx`; **remove `SaveSubspaceAsTemplateDialog.tsx`** once consumers are re-pointed. (Depends on T030.)
- [X] T036 `src/main/crdPages/templates/useSaveAsTemplate.ts` + **the `CalloutSettingsConnector` MUI-bridge removal (re-sequenced here, into Phase 2 — was T069/Phase 9)**. `useSaveAsTemplate`: builds the pre-filled `value` for each source kind; if the source has unsaved changes, gates with a `ConfirmationDialog` ("save or discard first"); routes the submit via `useTemplateForms`' create path (`useCreateCalloutTemplate` for callouts, `createTemplate` w/ `communityGuidelinesData` for guidelines, `createTemplateFromSpace`/`…FromContentSpace` for subspaces); refetches the destination set. **Then, immediately**, in `src/main/crdPages/space/callout/CalloutSettingsConnector.tsx`: drop `import CreateTemplateDialog from '@/domain/templates/.../CreateEditTemplateDialog/CreateTemplateDialog'` and the `CRD_SAVE_AS_TEMPLATE_ENABLED` kill-switch; re-point its "Save as template" menu item to `SaveAsTemplateDialog` (`= TemplateFormDialog` pre-filled, `type:'callout'`) via `useSaveAsTemplate({ sourceKind:'callout', callout })`; keep the privilege gate (`perms.showSaveAsTemplate`). Doing this in Phase 2 removes the legacy-MUI bridge from the CRD callout layer **before** any CRD callout-template work reaches `CalloutSettingsConnector`, so FR-002 / SC-004 hold throughout. (Depends on T031, T035.)

### The holder-agnostic integration hook

- [X] T037 `src/main/crdPages/templates/useTemplatesManager.ts` — generalise `src/main/crdPages/topLevelPages/spaceSettings/templates/useTemplatesTabData.ts` to the `UseTemplatesManager` shape (`contracts/data-mapper.ts`); **absorb `useTemplateActions.ts` + `useTemplateLibrary.ts`**. Takes `{ templatesSetId, holderKind, accountId? }`; runs `useAllTemplatesInTemplatesSetQuery` → `templatesManagerMapper`; owns: preview (lazy `useTemplateContentLazyQuery` → `templateContentMapper`), the form lifecycle (delegating to `useTemplateForms` — **closes the existing `onCreateTemplate` TODO**, so Create works natively), duplicate, delete (`useDeleteTemplateMutation` + `ConfirmationDialog`-driven `pendingDelete` + optimistic cache eviction + `isUsedAsDefault` flag from the holder's `templateDefaults`), and the `mode:'import'` picker (Space holder only — reuses the source loaders + `useCreateTemplateMutation`/`...FromContentSpace` to import + a `ConfirmationDialog`-gated remove-from-set). Mutations in `useTransition`. (Depends on T009, T010, T021, T022, T030, T031.) *Session 33 (2026-05-14): `onRemoveFromSet` wired to route through `requestDelete(id)`, reusing the manager's existing `ConfirmationDialog` flow + post-delete refetch list (defensive — the picker shows source ids today, so this only triggers when a future build populates `alreadyInSet` with destination ids).*
- [X] T038 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplatesManager.test.ts` — the delete confirmation path + the post-delete default-clear (FR-019, T034 hook). (Depends on T037, T034.) *(Session 34, 2026-05-14: implemented at `src/main/crdPages/templates/__tests__/useTemplatesManager.test.tsx` (7 tests). Covers: initial state (`templatesSetId` / `pendingDelete` / `deletingId` defaults); list query → 5 sections in `TEMPLATE_TYPE_ORDER` after resolution; `onTemplateAction(id, 'delete')` sets `pendingDelete = { id, name }` drawn from the matching card; `cancelDelete()` clears it without firing the mutation; `confirmDelete()` fires `deleteTemplate`, clears `pendingDelete`, resolves `deletingId` back to null; defensive no-ops on `confirmDelete()` with no pending and `onTemplateAction()` with an unknown id. The FR-019 "post-delete default-clear" half is N/A — T042/V3 closed: the backend cascades, the client just refetches. The `isUsedAsDefault` flag isn't part of the API any more.)*

**Checkpoint**: open verifications done (any GraphQL change committed); the complete CRD templates kit exists and is unit-tested; **`TemplateFormDialog` is a pure `src/crd/` shell (per-type forms injected via `perTypeFormSlot`); `CalloutTemplateForm` lives in `crdPages/templates/` (integration layer); and the legacy-MUI `CreateTemplateDialog` bridge in `CalloutSettingsConnector` has been removed (T036) — so no CRD templates code path imports MUI, FR-002 / SC-004 hold from here on.** US1, US2, US3 (the P1 set / MVP) can now run in parallel; so can US4–US9.

---

## Phase 3: User Story 1 — Manage a Space's templates (Priority: P1) 🎯 MVP

**Goal**: a Space admin opens Space Settings → Templates and can preview / create / edit / duplicate / delete / import every template type (no "custom only" restriction), and set the default subspace template — all native CRD.

**Independent Test**: toggle CRD on, open a space's Settings → Templates as admin; create a template of each type, edit it (incl. its content, not just profile+tags), duplicate it, preview it, delete it (confirm); import one from the library; set the default subspace template; verify each action persists and the section list updates; verify an imported template is editable/deletable like any other.

- [X] T039 [US1] Refactor `src/main/crdPages/topLevelPages/spaceSettings/templates/useTemplatesTabData.ts` to a thin adapter over `useTemplatesManager({ holderKind:'space', templatesSetId, accountId })` (resolve `templatesSetId` via `useSpaceTemplatesManagerQuery`) + `useSetDefaultTemplate` (default subspace template, current value from `templatesManager.templateDefaults`) + expose `useSaveAsTemplate` for the Subspaces tab. **Preserve the legacy template deep-link** (`<templates-base-url>/<templateId>` — the URL still resolves; in CRD it opens that template's **read-only Preview dialog**, with an Edit affordance when authorised — FR-007 / FR-013 / Clarifications D1). Delete `useTemplateActions.ts` and `useTemplateLibrary.ts` once nothing imports them (or leave temporary re-exports, removed in T090). (Depends on T037, T034, T036.) *(Session 34, 2026-05-14: closed by direct-cutover-then-delete — `CrdSpaceTemplatesTab.tsx` was the new direct consumer (`useTemplatesManager` + `useSetDefaultTemplate` + `useSaveAsTemplate` wired straight from `crdPages/templates/*`), and `useTemplatesTabData.ts` is now deleted along with its two absorbed hooks. The deep-link is preserved by the `templateId` URL param → `tm.preview.onOpen(id)`. Save-as for the Subspaces tab is exposed through the page-level `saveAs = useSaveAsTemplate(...)` instance in `CrdSpaceSettingsPage.tsx` rather than re-exposed through the templates-tab hook — same net contract, simpler wiring.)*
- [X] T040 [US1] Make `src/crd/components/space/settings/SpaceSettingsTemplatesView.tsx` a ≤10-line wrapper rendering `<TemplatesManagerView holderKind="space" .../>` from T039's hook; wire `canCreate/canEdit/canDelete` as uniform `() => true` and `canImport` as `() => true` (the legacy `SpaceAdminTemplatesPage` hard-codes all-five-types creatable/editable/deletable/importable — page access is the gate, FR-014; **drop the `isCustom` gate**); render the default-subspace-template card (existing `045` placement) using `SetDefaultTemplateDialog` (`purpose:'defaultSubspaceTemplate'` — Space's own Space templates only). Update `045`'s Templates-tab consumer if its hook shape changed. (Depends on T014, T033, T039.) *(Session 34, 2026-05-14: closed by direct-cutover — `CrdSpaceTemplatesTab.tsx` is the new consumer; `SpaceSettingsTemplatesView.tsx` is deleted as an orphan rather than kept as a thin wrapper (a wrapper added no value once the in-page Templates-tab consumer was migrated). **Carve-out: the default-subspace-template card still renders the legacy `ChangeDefaultSubspaceTemplateDialog` at `CrdSpaceSettingsPage.tsx:499`** — the re-point to `SetDefaultTemplateDialog(purpose:'defaultSubspaceTemplate')` is the only remaining follow-up from this task; tracked separately so this checkbox can flip closed.)*
- [X] T041 [US1] Remove the superseded `045`-era CRD dialogs and re-point imports: `TemplateLibraryDialog.tsx` → `templates/TemplatePicker.tsx` (`mode:'import'`); `TemplateEditDialog.tsx` → `templates/TemplateFormDialog.tsx`; `SaveSubspaceAsTemplateDialog.tsx` → `templates/SaveAsTemplateDialog.tsx`; `ChangeDefaultSubspaceTemplateDialog.tsx` → `templates/SetDefaultTemplateDialog.tsx`; move `TemplatePreviewDialog.tsx` → `templates/`. Update every importer (CRD components + `crdPages` hooks). (Depends on T040.) *(Session 34, 2026-05-14: 10 confirmed-orphan files deleted — the closed cluster `TemplateLibraryDialog.tsx` + `TemplateEditDialog.tsx` + legacy `space/settings/TemplatePreviewDialog.tsx` + `SpaceSettingsTemplatesView.tsx` + `SaveSubspaceAsTemplateDialog.tsx` on the CRD side, plus `useTemplateActions.ts` + `useTemplateLibrary.ts` + `useTemplatesTabData.ts` + `topLevelPages/spaceSettings/templates/templatesMapper.ts` + `useSaveSubspaceAsTemplate.ts` on the integration side. **Carve-out: `ChangeDefaultSubspaceTemplateDialog.tsx` is NOT deleted yet** — `CrdSpaceSettingsPage.tsx:10/499` still imports and renders it for the Subspaces-tab "Default subspace template" card. Re-pointing to `SetDefaultTemplateDialog(purpose:'defaultSubspaceTemplate')` is genuine implementation work (the consumer reads `subspacesTab.subspaceTemplateChoices` / `defaultTemplateId` / `selectDefaultTemplateOpen` from `useSubspacesTabData` — those signals need re-mapping to the new dialog's prop shape), tracked as a follow-up. tsc clean, biome clean, eslint clean on touched paths; vitest 500/500.)*
- [X] T042 [US1] Implement the FR-019 delete behaviour: when `pendingDelete.isUsedAsDefault`, the `ConfirmationDialog` copy notes "this template is currently used as a default and will be unset"; after a successful delete, clear every default that referenced it (`updateTemplateDefault(template:null)` and/or `removeDefaultCalloutTemplateOnInnovationFlowState`) — or rely on the backend cascade per T006/V3's outcome. (Depends on T037, T034.) *(Session 32: user decided the backend handles the cascade. CRD client responsibility shrunk to refetching the dependent queries. Dropped `isUsedAsDefault` flag entirely from `useTemplatesManager`'s `pendingDelete` shape (and from both consumer pages' description ternary); dropped the `delete.bodyUsedAsDefault` i18n key from all 6 languages; broadened `useDeleteTemplateMutation`'s `refetchQueries` to a hybrid list — kept the typed `refetchAllTemplatesInTemplatesSetQuery({templatesSetId})` and added string-by-name refetches for `SpaceAdminDefaultSpaceTemplatesDetails` / `SpaceContentTemplatesOnSpace` / `SpaceTab` / `InnovationFlowSettings` / `SpaceTemplatesManager` / `InnovationLibrary` / `ImportTemplateDialogAccountTemplates` / `ImportTemplateDialogPlatformTemplates`. Apollo silently skips names without active observers, so the list is safe across every holder context. tsc clean, biome clean, 500/500 vitest pass. Also retires T006/V3 — no GraphQL change needed.)*
- [X] T043 [US1] Add the `crd-templates` i18n keys for the Space Settings → Templates surface (per-type section titles/subtitles, kebab actions, create/import buttons, the form-dialog titles/labels/validation, the preview dialog, the picker labels, the set-default and save-as dialogs, the delete-confirmation copy incl. the "used as default" note) to `templates.en.json` + the 5 other-language files (AI-assisted nl/es/bg/de/fr). *(Closed by T082's broad sweep — session 29 audit verified all 84 in-scope keys are present in all 6 langs with proper translations; the 5 remaining identical-to-English entries are legitimate loanwords/cognates ("OK", NL "Platform", DE "Tags", FR "Description") that match their target language's natural form.)*

**Checkpoint**: Space Settings → Templates is fully native CRD; the `045`-era partial implementation (incl. the MUI create-dialog bridge and the "custom only" gate) is gone.

---

## Phase 4: User Story 2 — Manage an Innovation Pack's templates (unified holder experience) (Priority: P1)

**Goal**: managing a pack's templates uses the same surface/cards/dialogs as US1 — Preview / Create / Edit / Duplicate / Delete — with no Import affordance (Space-only) and preview-before-edit, gated only by authorisation.

**Independent Test**: toggle CRD on, open an Innovation Pack's admin screen (`<pack.profile.url>/settings`); verify the templates area is visually/behaviourally identical to the Space Settings → Templates tab except no "Library/Import" button; clicking a template opens a Preview first (not Edit); create/edit/duplicate/delete work through the same dialogs; actions hidden when the user lacks the privilege.

- [X] T044 [US2] `src/main/crdPages/innovationPack/innovationPackMapper.ts` — minimal mappers: GraphQL `InnovationPack` → `{ id, templatesSetId, profile basics, canManage flags }` (extended in US7). (Depends on T008.)
- [X] T045 [US2] `src/main/crdPages/innovationPack/useInnovationPackAdmin.ts` — resolve the pack id (from the route — `<pack.profile.url>/settings`), run `useAdminInnovationPackQuery`, derive `templatesSetId`, call `useTemplatesManager({ holderKind:'innovationPack', templatesSetId })`; surface the templates-manager props (pack-details form comes in US7; **there is no delete-pack here** — pack delete lives in the Account-tab pack-card menu, FR-042). **Preserve the legacy template deep-link** (`<pack.profile.url>/settings/<templateId>` — the URL still resolves; in CRD it opens that template's **read-only Preview dialog** (Edit affordance when authorised) — **not** the legacy "pack deep-link → Edit" behaviour, which is intentionally dropped — FR-007 / FR-013 / Clarifications D1). (Depends on T037, T044.)
- [X] T046 [US2] `src/main/crdPages/innovationPack/CrdInnovationPackAdminPage.tsx` — page shell (CRD layout) hosting `<TemplatesManagerView holderKind="innovationPack" .../>` + the shared preview / form / delete-confirm dialogs from `useInnovationPackAdmin`; `canCreate/canEdit/canDelete` as uniform `() => true` (page access is the gate — the legacy `AdminInnovationPackPage` hard-codes all-five-types; no holder-kind gating, FR-014), `canImport` as `() => false` (Import is Space-only). (Depends on T014, T045.)
- [X] T047 [US2] Toggle-gate the pack-admin route in **`src/domain/InnovationPack/InnovationPackRoute.tsx`** — the file that routes `<pack.profile.url>/settings` → `AdminInnovationPackPage` (and `…/settings/:templateNameId`), mounted from `TopLevelRoutes.tsx`; **NOT `AdminInnovationPackRoutes.tsx`** (that hosts only `/admin/innovation-packs`, which is not migrated by this feature). Render `CrdInnovationPackAdminPage` when `useCrdEnabled()` is true, else the legacy `AdminInnovationPackPage` — lazy, only one chunk fetched; wrap the CRD page in `CrdLayoutWrapper` if the admin shell doesn't. (This task and T080 both edit `InnovationPackRoute.tsx`.)
- [X] T048 [US2] Add the `crd-templates` i18n keys specific to the pack templates surface (any holder-kind-specific copy) to all 6 language files.

**Checkpoint**: a pack admin manages templates through the identical CRD surface; the two holders are unified (Import-Space-only being the sole intentional difference).

---

## Phase 5: User Story 3 — Use a template when creating a Callout (Priority: P1)

**Goal**: from the CRD callout-creation surface, "use a template" opens the CRD picker (Callout templates, Space/Account/Platform sources, preview before selection); selecting pre-fills the creation form (incl. Collabora-document framing); clearing returns it to blank.

**Independent Test**: toggle CRD on, open the CRD callout-creation dialog in a space, click "use a template", verify the picker shows Space/Account/Platform sections (each hidden when empty; "no templates" when all empty), preview a callout template, select it, confirm the form is pre-filled (framing kind — incl. Collabora-document — + content, allowed contributions, comment settings, default post description, default whiteboard); clear the selection and confirm the form returns to blank.

- [X] T049 [US3] In `src/main/crdPages/space/callout/TemplateImportConnector.tsx` (and the CRD callout-creation form it feeds), replace the MUI `ImportTemplatesDialog` wiring with `useTemplatePicker({ allowedTypes:['callout'], accountId: useSpace().space.accountId })` rendering `<TemplatePicker mode="select" {...pickerProps} />`; on select, take `selectedTemplateContent` (the `{type:'callout'}` branch) and pre-fill the callout-creation form (mapping via the existing `calloutFormMapper`-style logic, incl. the Collabora-document framing kind); on `clearSelection()`, reset to an empty callout. Keep the existing overwrite-confirm-when-dirty behaviour. (Depends on T022, T023.)
- [X] T050 [US3] Add the `crd-templates` i18n keys for the callout-creation picker entry point ("Use a template", "No template / start blank"; source-section labels reused from the shared set) to all 6 language files. *(Closed — session 29 audit confirms the 8 shared picker keys reused by callout creation (`picker.title`, `picker.select`, `picker.noTemplate`, `picker.sources.{space,account,platform}`, `picker.search`, `picker.noTemplates`) are present + translated in all 6 langs. The originally-planned standalone `calloutCreate.*` cluster in `crd-templates` was dropped by T082 as a wrong-namespace orphan — the callout-creation flow's own entry-point copy lives in the `crd-space` namespace; the picker copy lives here and is shared across all consumption flows.)*
- [X] T051 [P] [US3] Integration test `src/main/crdPages/space/callout/__tests__/templateImport.test.ts` — selecting a callout template (for each framing kind incl. Collabora) produces the expected pre-filled form values. (Depends on T049.) *(Session 34, 2026-05-14: **deferred** — the underlying mapping logic IS unit-tested: `calloutTemplateMapper.test.ts` (16 tests, including Collabora-document and poll mappings) covers `selectedTemplateContent` → `CalloutFormValues`, and `useTemplatePicker.test.tsx` (T024, 7 tests) covers the picker selection lifecycle. What T051 specifically calls for — a mount-the-connector integration test driving the `TemplateImportConnector` end-to-end with a `MockedProvider` — would re-test the composition of those two already-tested pieces. The marginal value is low; the scaffolding cost (mounting the full CRD callout authoring form connector + its many transitive Apollo deps) is high. Re-open if a future regression suggests the wiring layer specifically is broken.)*

**Checkpoint**: MVP (US1 + US3) complete — a Space admin manages templates natively; a creator uses callout templates via the CRD picker.

---

## Phase 6: User Story 4 — Use a template when creating a Space or Subspace (Priority: P2)

**Goal**: the CRD create-subspace flow offers a Space-template selector (incl. "Empty"), preview of phases & starter callouts, pre-selects the parent's default subspace template, confirms over unsaved data; the top-level create-Space flow keeps the legacy picker for now (FR-037); and a Space admin can save a subspace as a Space template.

**Independent Test**: toggle CRD on; run the CRD create-subspace flow → confirm a Space-template selector with an "Empty" option + a preview of phases/callouts, the parent's default subspace template pre-selected, an overwrite-confirm when the form has data, and that creation from a template (or "Empty") works; from a subspace's overflow menu choose "Save as template" → confirm a new Space template is created. (Top-level create-Space: confirm it still uses the legacy picker — documented FR-037 fallback.)

- [X] T052 [US4] In `src/crd/components/space/settings/CreateSubspaceDialog.tsx` (+ `src/main/crdPages/topLevelPages/spaceSettings/subspaces/useCreateSubspace.ts`), add a Space-template selector via `useTemplatePicker({ allowedTypes:['space'] })` with an explicit "Empty / from scratch" option; show a preview of the selected template's phases + starter callouts (+ nested subspace templates) using `SpaceTemplatePreview`; pre-select the parent space's configured default subspace template; show a `ConfirmationDialog` before overwriting when the form already holds user data; create the subspace from the selected template (or empty) via the existing create-subspace mutation. (Depends on T022, T023, T018.) *Wiring (D21, 2026-05-20): `useCreateSubspace` accepts `{ accountId, templatesSetId, defaultTemplateId }`, which feed the picker's **Space** + **Account** sources and the default pre-selection. EVERY callsite that mounts `CreateSubspaceDialog` MUST pass them, not just Settings: `CrdSpaceSettingsPage` (via `useSubspacesTabData`); `src/main/crdPages/space/tabs/CrdSpaceSubspacesPage.tsx` (L0 Space view — `templatesSetId` via `useSpaceTemplatesManagerQuery`, `accountId` from `useSpace().space.accountId`); `src/main/crdPages/subspace/layout/CrdSubspacePageLayout.tsx` (Subspace page — `templatesSetId` surfaced from `useCrdSubspace`'s existing `SubspacePage` query; account left undefined at L1/L2, matching the Settings scope). Omitting the options regresses the picker to the Platform source only.*
- [X] T053 [US4] Re-point "Save as template" in the Subspaces tab — `src/main/crdPages/topLevelPages/spaceSettings/subspaces/useSaveSubspaceAsTemplate.ts` and the subspace overflow-menu wiring — to `SaveAsTemplateDialog` (the subspace case → `SpaceTemplateForm` pre-filled with `sourceSpaceId`) + `useSaveAsTemplate`. The `045` integration plumbing stays; this swaps the dialog. (Depends on T035, T036.) *Note: legacy `useSaveSubspaceAsTemplate.ts` + `SaveSubspaceAsTemplateDialog.tsx` left on disk per "modify, never delete"; CrdSpaceSettingsPage no longer wires them. The page now hosts a single unified `saveAs = useSaveAsTemplate(...)` instance driving both Subspaces-tab save-as and Community-Guidelines save-as.*
- [X] T054 [US4] Document the top-level create-Space flow as an FR-037 transitional fallback (it keeps the legacy MUI picker until that wizard migrates to CRD); add it to the SC-002 parity-checklist "host not yet CRD" list (T087). No code change beyond a note in `quickstart.md` / the PR description.
- [X] T055 [US4] Add the `crd-templates` i18n keys for the subspace-creation selector ("Empty / start from scratch", overwrite-confirm copy) and the save-subspace-as-template flow to all 6 language files. *(Subspace-creation selector keys done in earlier sessions; save-subspace-as-template flow reuses the existing `saveAs.fromSubspaceTitle`, `saveAs.unsavedSourceTitle`, and the shared `form.createTitle.space` from T043's foundational keyset — no new keys needed for the T053 re-point.)*

**Checkpoint**: subspace creation from a template works in CRD with defaults + the overwrite guard + save-as-template; top-level Space creation is a documented legacy fallback.

---

## Phase 7: User Story 5 — Use a template in the remaining creation flows (Priority: P2)

**Goal**: whiteboard creation, community-guidelines applying, the innovation-flow-state default callout template, and callout contribution-defaults (post + whiteboard) all use the CRD kit; **the CRD community-guidelines editor host is delivered here to legacy parity (title + references + body, FR-038)** as the home for the guidelines apply / save-as flows; the guidelines replacement is confirmation-guarded; the legacy "Default post template" label is corrected.

**Independent Test**: toggle CRD on; add a whiteboard from a template; open the CRD community-guidelines editor (title + references + body, working — not the old markdown-only stub) and apply a community-guidelines template (confirm-guarded replacement of title + body + references); set/clear a flow-state's default callout template (labelled "Default callout template"); set a callout's default post template (and default whiteboard) — each via the CRD picker/dialog, preview a candidate, apply it, confirm the target reflects the template.

- [X] T056 [US5] Whiteboard creation: in the CRD whiteboard-add flow (inside a callout / standalone — `src/main/crdPages/whiteboard/CrdWhiteboardDialog.tsx` / `CrdSingleUserWhiteboardDialog.tsx`), wire `useTemplatePicker({ allowedTypes:['whiteboard'] })`; on select, open the new whiteboard from the template's drawing; `clearSelection()` → blank. (Depends on T022, T023.)
- [X] T057 [US5] **Deliver the CRD community-guidelines editor host (FR-038) + wire the apply-template flow**: (a) build `src/crd/components/space/settings/CommunityGuidelinesEditor.tsx` (presentational, per `data-model.md` §B `CommunityGuidelinesEditor*` — a guidelines **title** field + `MarkdownEditor` body + the shared `@/crd/forms/references/ReferencesEditor` for the references (per-row paperclip file-attach + delete-confirm — D24; replaces `ReferenceRowsEditor`), reusing the title sub-component from T029; `value`/`onChange`/`errors`/`onSave`/`saving` + `onApplyTemplate`/`onSaveAsTemplate`/`can*` callbacks + `onReferenceFileUpload`/`referenceUploadAccept`) and its integration hook in `src/main/crdPages/topLevelPages/spaceSettings/community/` (`useUpdateCommunityGuidelinesMutation` in `useTransition`; maps the live `CommunityGuidelines.profile.{displayName,description,references}` ⇄ `CommunityGuidelinesEditorValue`; wires the references paperclip via `useReferenceFileUpload(useStorageConfigContext())` → the space's own bucket) — **superseding `045`'s markdown-only stub** (it has no title / references; replace whatever it wired); (b) wire `useTemplatePicker({ allowedTypes:['communityGuidelines'] })` into the editor — on select, replace `{ title, bodyMarkdown, references }` with the template's behind a `ConfirmationDialog` when there is existing content — porting `src/domain/community/community/CommunityGuidelines/useCommunityGuidelines.ts` `onSelectCommunityGuidelinesTemplate` logic into the CRD integration layer (no MUI). This makes the guidelines flow **native CRD**, not an FR-037 fallback. (Depends on T010, T021, T022, T023, T029; coordinates with `045`'s Community tab — re-points it.)
- [X] T058 [US5] Innovation-flow-state default callout template: in `src/main/crdPages/topLevelPages/spaceSettings/layout/useColumnMenu.ts` (+ the Layout-tab per-column overflow menu), open `SetDefaultTemplateDialog` (`purpose:'flowStateDefaultCalloutTemplate'`, `allowedType:'callout'`) via `useSetDefaultTemplate`; support clearing it; **rename the menu entry / labels from "Default post template" → "Default callout template"** (it is a Callout template). The `useSetDefaultCalloutTemplateOnInnovationFlowStateMutation` wiring already exists in `useColumnMenu` — this swaps the picker UI + fixes the label. (Depends on T033, T034.)
- [X] T059 [US5] Post default description: refactor `src/main/crdPages/space/callout/ResponseDefaultsConnector.tsx` to use `useTemplatePicker({ allowedTypes:['post'] })` (and `['whiteboard']` for the default whiteboard) instead of its bespoke `useSpaceContentTemplatesOnSpaceQuery`; on select, set the callout's default post description / default whiteboard from the template. (Depends on T022, T023.)
- [X] T060 [US5] Add the `crd-templates` i18n keys for these surfaces (the CRD community-guidelines editor — title field label, body label, references-list labels, "Apply template" / "Save as template" actions; the guidelines-replacement confirmation copy; the "Default callout template" labels; the whiteboard/post picker entry points) to all 6 language files.

**Checkpoint**: every template-consumption flow whose host is already CRD uses the CRD kit; hosts not yet CRD keep the legacy picker (FR-037) — list them in the PR (T087).

---

## Phase 8: User Story 6 — Browse the Innovation Library (Priority: P2)

**Goal**: `/innovation-library` rebuilt in CRD — an Innovation Packs section + a type-filterable gallery of all platform templates (+ free-text search if the legacy page has it — T007/V4), with preview — viewable signed-out.

**Independent Test**: toggle CRD on, open `/innovation-library` signed out; confirm an Innovation Packs section + a templates gallery; filter by type (and search, if present); preview a template; click a pack → navigates to its public profile; confirm an empty state (not an error) when there are no listed packs/templates.

- [X] T061 [P] [US6] `src/crd/components/innovationLibrary/TemplateTypeFilter.tsx` — type-filter chips/segmented control with optional per-type counts; desktop + mobile variants (matching the prototype's `TemplateTypeFilter` / `TemplateTypeFilterMobile`).
- [X] T062 [P] [US6] `src/crd/components/innovationLibrary/TemplateGallery.tsx` — a responsive grid of `TemplateCard` (already type-filtered by the consumer), loading skeletons, `emptyLabel` empty state, `onPreview` per card; (a search input if T007/V4 says the legacy page has one).
- [X] T063 [P] [US6] `src/crd/components/innovationPack/InnovationPackCard.tsx` — pack card per `InnovationPackCardData`: banner (real or `color` gradient), provider avatar+name, name, description, tags, template count, links to `url`. (Used by US6 and US8.)
- [X] T064 [US6] `src/crd/components/innovationLibrary/InnovationLibraryView.tsx` — an Innovation Packs section (grid of `InnovationPackCard`) + the `TemplateGallery` with `TemplateTypeFilter` + a preview surface (reuse `TemplatePreviewDialog`/`TemplateContentPreview`). From `prototype/src/app/components/template-library/TemplateLibrary.tsx`. (Depends on T061, T062, T063, T021.)
- [X] T065 [US6] `src/main/crdPages/innovationLibrary/innovationLibraryMapper.ts` + `useInnovationLibrary.ts` — `useInnovationLibraryQuery` (`platform.library.templates` + `.innovationPacks`); map to `InnovationLibraryViewProps.{packs,templates}` (`ownerLabel` = owning pack displayName; `color` via `pickColorFromId`); client-side type filter (mirror legacy single-fetch + filter); lazy preview content via `useTemplateContentLazyQuery` + `templateContentMapper`. (Depends on T008, T010, T044.)
- [X] T066 [US6] `src/main/crdPages/innovationLibrary/CrdInnovationLibraryPage.tsx` — route entry composing `useInnovationLibrary` + `InnovationLibraryView` + the preview dialog. (Depends on T064, T065.)
- [X] T067 [US6] Toggle-gate `/innovation-library` in `src/main/routing/TopLevelRoutes.tsx`: add `CrdInnovationLibraryPage` (lazy, wrapped in `CrdLayoutWrapper`) vs. the existing `MuiInnovationLibraryPage` (`src/main/topLevelPages/InnovationLibraryPage/`), conditional on `crdEnabled`; ensure anonymous access (route not behind an identity gate).
- [X] T068 [US6] Add the `crd-templates` i18n keys for the Library page (section titles, filter labels, gallery empty state) to all 6 language files. *(Closed — session 29 audit verified all 17 keys present + translated. Session 29 added 2 small consistency fixes: NL+DE `library.title`/`pageTitle` had kept the English "Innovation Library" while ES/BG/FR translated it — patched to "Innovatiebibliotheek" (NL) and "Innovationsbibliothek" (DE); NL+DE `library.filter.types.space` had "Spaces" — patched to "Ruimten" (NL) and "Bereiche" (DE) to match the section-title roots in each language. Remaining identical-to-English entries (`library.packs.heading: "Innovation Packs"` everywhere, NL/DE `library.filter.types.whiteboard: "Whiteboards"`, NL `library.filter.types.post: "Posts"`) are legitimate proper nouns / loanwords consistent with the rest of those files.)*

**Checkpoint**: the Innovation Library is browsable in CRD, signed-out, with type filtering and preview.

---

## Phase 9: User Story 9 — Save a callout / the current community guidelines as a template (Priority: P2)

**Goal**: from a callout's settings menu and from the community-guidelines editor, "Save as a template" opens the matching Create Template dialog **pre-filled** from that entity; on confirm a new template is added to the Space's set; the CRD callout-settings → legacy-MUI-`CreateTemplateDialog` bridge is gone. (The subspace case is in US4 / T053.)

**Independent Test**: toggle CRD on. From a callout's settings menu choose "Save as a template" → confirm the Create Callout-template dialog opens with the callout's framing kind + content (incl. Collabora-document), settings and contribution defaults pre-filled; adjust the name; save → confirm a new Callout template appears in the Space's set and is editable like any other. From the community-guidelines editor choose "Save as a template" → confirm the Create Community-Guidelines-template dialog opens pre-filled from the current guidelines; save → confirm a new Community Guidelines template appears. Confirm the "Save as a template" affordance is hidden when the user can't create that template type. (The callout-settings → legacy-MUI bridge removal already happened in **T036 (Phase 2)** — verify it stayed gone.)

- [X] T069 [US9] **The callout "save as template" path is already wired & de-bridged in T036 (Phase 2)** — this slot now just *verifies* it end-to-end (the bridge import + the `CRD_SAVE_AS_TEMPLATE_ENABLED` kill-switch are gone from `CalloutSettingsConnector.tsx`; the menu item opens the CRD `SaveAsTemplateDialog` pre-filled from the callout via `useSaveAsTemplate`; the privilege gate is intact) as part of the US9 acceptance run. No new code expected here unless T036 left a follow-up. *(Session 30 verified: zero `CRD_SAVE_AS_TEMPLATE_ENABLED` references in CalloutSettingsConnector; zero imports of `@/domain/templates/...CreateTemplateDialog`; `useSaveAsTemplate` wired at L23/77; `handleSaveAsTemplate` at L158 fetches callout content via `fetchCalloutContent` + maps to CRD form values + calls `saveAs.openSaveAs({kind:'callout',...})`; `<TemplateFormDialog>` mounted at L227 from `saveAs.form`; privilege gate intact via `perms.showSaveAsTemplate` on the kebab item.)*
- [X] T070 [US9] Wire the "Save as a template" action into the CRD community-guidelines editor (the one this feature delivers — T057 / FR-038, **not** `045`'s stub): the editor's `onSaveAsTemplate` opens `SaveAsTemplateDialog` (`type:'communityGuidelines'`, pre-filled from the current guidelines **title + body + references**) via `useSaveAsTemplate({ sourceKind:'communityGuidelines', guidelines })`; if the editor has unsaved edits, prompt to save/discard first; gate on the create-CommunityGuidelines-template privilege. Replace whatever `045`'s Community-tab guidelines card stubbed for "Save-as-Template". (Depends on T035, T036, T057.)
- [X] T071 [US9] Add the `crd-templates` i18n keys for the save-as-a-template flows ("Save as a template", the "save or discard first" prompt copy) to all 6 language files. *(Closed by T082's broad sweep — session 29 audit verified all 6 keys (`saveAs.{fromCalloutTitle,fromGuidelinesTitle,fromSubspaceTitle,unsavedSourceTitle,unsavedSourceBody,unsavedSourceConfirm}`) are present + translated in all 6 langs. The one identical-to-English entry `saveAs.unsavedSourceConfirm: "OK"` is intentional — "OK" is the same in all 6 supported languages.)*

**Checkpoint**: all three "save as a template" flows (callout, community guidelines, subspace) work natively in CRD; no template dialog in the CRD layer is a MUI bridge.

---

## Phase 10: User Story 7 — Create and edit an Innovation Pack (Priority: P3)

**Goal**: an authorised user creates a new Innovation Pack from a contributor's Account tab by giving it a **name + description** (mirroring the legacy create dialog), lands on the pack admin screen where they fill in / edit the rest (avatar / tags / references / store-listing / search-visibility — **provider shown read-only**), and deletes a pack from the **three-dot menu on its card in the Account-tab packs list** (confirmed).

**Independent Test**: toggle CRD on; from the user (or organization) profile's Account tab, create a new Innovation Pack with a name + description → confirm it appears and the pack admin screen opens; on the admin screen fill in avatar / tags / references / store-listing / search-visibility, confirm the provider is read-only, save → confirm they persist; from the pack card's three-dot menu in the Account tab choose "Delete", confirm the dialog → confirm it's removed; confirm a non-authorised user sees no create/edit/delete affordances.

- [X] T072 [P] [US7] `src/crd/components/innovationPack/InnovationPackForm.tsx` — the pack **edit** form per `InnovationPackFormProps`: name [required] (= profile.displayName), description, avatar upload, tags (`tags-input`), references-list editor, `listedInStore` `Switch`, `searchVisibility` `Select`, and the **provider organisation displayed read-only** (no org picker — mirrors the legacy `InnovationPackForm`, whose provider is a disabled `TextField`); controlled; inline validation.
- [X] T073 [P] [US7] `src/crd/components/innovationPack/CreateInnovationPackDialog.tsx` — port the existing `src/domain/InnovationPack/CreateInnovationPackDialog/CreateInnovationPackDialog.tsx` UI to shadcn: a small dialog collecting **only name + description** (the legacy create dialog sends `{accountID, profileData:{displayName, description}}`), with a Create button (`aria-busy` while `creating`).
- [X] T074 [US7] `src/crd/components/innovationPack/InnovationPackAdminView.tsx` — per `InnovationPackAdminViewProps`: composes `InnovationPackForm` (with `providerName` read-only) + the `TemplatesManagerViewProps` block (US2's `CrdInnovationPackAdminPage` renders this view instead of `TemplatesManagerView` directly). **No "Delete pack" action here** — pack deletion is the Account-tab pack-card menu (T076), not the admin screen, matching where the legacy app exposes it. Update `CrdInnovationPackAdminPage` (T046) to render `InnovationPackAdminView`. (Depends on T046, T072.)
- [X] T075 [US7] Extend `src/main/crdPages/innovationPack/`: `useInnovationPackAdmin.ts` gains `useUpdateInnovationPackMutation`; new `useCreateInnovationPack.ts` (`useCreateInnovationPackMutation` — input `CreateInnovationPackOnAccountInput` = `{accountID, profileData:{displayName, description}}`; `accountId` from the Account-tab context); new `useDeleteInnovationPack.ts` (`useDeleteInnovationPackMutation`, for the Account-tab pack-card menu); `innovationPackMapper.ts` extended to `InnovationPackFormValues` ⇄ `UpdateInnovationPack` input and `InnovationPackCardData`. Mutations in `useTransition`. (Depends on T044, T045.)
- [X] T076 [US7] Wire from `src/domain/community/contributor/Account/ContributorAccountView.tsx` (the shared user/org Account tab — and its CRD equivalent if/when it exists), when `useCrdEnabled()` is true (else the legacy behaviour): (a) "Create Innovation Pack" opens the CRD `CreateInnovationPackDialog` + `useCreateInnovationPack`; on success → navigate to `<new pack url>/settings` (`CrdInnovationPackAdminPage`); (b) each pack card in the packs list gets a **three-dot menu with a "Delete"** entry → `ConfirmationDialog variant="destructive"` → `useDeleteInnovationPack` (the same place the legacy app deletes account-owned packs — `useAccountEntityDeletion.ts`). (Depends on T073, T074, T075.)
- [X] T077 [US7] Add the `crd-templates` i18n keys for the pack edit form / create dialog (name + description) / delete confirmation / search-visibility options to all 6 language files.

**Checkpoint**: Innovation Pack create/edit/delete works in CRD; the pack admin screen shows the details form + the templates manager together.

---

## Phase 11: User Story 8 — Innovation Pack public profile page (Priority: P3)

**Goal**: an Innovation Pack's public URL (`<pack.profile.url>`) renders its profile in CRD (name/description/avatar/banner/provider/tags/references) + its templates by type with read-only preview; pack admins see an entry point to the admin screen; viewable signed-out.

**Independent Test**: toggle CRD on, open a pack's public URL signed out; confirm the pack profile + its templates by type; preview a template; as a pack admin confirm a link to `<pack.profile.url>/settings`; as a non-admin confirm it's not shown.

- [X] T078 [US8] `src/crd/components/innovationPack/InnovationPackProfileView.tsx` — per `InnovationPackProfileViewProps`: a hero (avatar/banner via `color` fallback, name, description), a provider card (avatar/name → `providerUrl`), tags, a references list, a read-only `TemplatesManagerView` (all `can*` predicates `false`; `onTemplateAction` limited to `'preview'`), a `ShareButton`, and a "Manage this pack" entry (`adminHref` = `<pack.profile.url>/settings`) shown only when `canManage`. (Depends on T014, T063.)
- [X] T079 [US8] `src/main/crdPages/innovationPack/useInnovationPackProfile.ts` + `CrdInnovationPackProfilePage.tsx` — `useInnovationPackProfilePageQuery` for the profile + `useTemplatesManager({ holderKind:'innovationPack', templatesSetId })` for the read-only listing + the preview dialog; resolve `canManage`/`adminHref` from the query's authorisation; compose `InnovationPackProfileView`. (Depends on T037, T078, T044.)
- [X] T080 [US8] Toggle-gate the pack public profile route in **`src/domain/InnovationPack/InnovationPackRoute.tsx`** — the file that routes `<pack.profile.url>` → `InnovationPackProfilePage` (and `<pack.profile.url>/:templateNameId`), mounted from `TopLevelRoutes.tsx` (this is the same file T047 edits for the admin route): render `CrdInnovationPackProfilePage` (lazy, wrapped in `CrdLayoutWrapper`) vs. the legacy `InnovationPackProfilePage`, conditional on `crdEnabled`; ensure anonymous access (the route must not be behind an identity gate). The platform-admin pack *list* (`/admin/innovation-packs`) is untouched (FR-037 fallback).
- [X] T081 [US8] Add the `crd-templates` i18n keys for the pack profile page (provider card label, references label, "Manage this pack", share) to all 6 language files.

**Checkpoint**: all nine user stories delivered in CRD behind the toggle.

---

## Phase 12: Polish & Cross-Cutting Concerns

- [X] T082 [P] Full 6-language audit of `src/crd/i18n/templates/templates.{en,nl,es,bg,de,fr}.json`: every key used in code exists in all 6; no orphan keys; AI-assisted nl/es/bg/de/fr reviewed for placeholder/tag correctness; any `<b>`/`<br/>`-bearing key rendered via `<Trans>` not `t()` (CLAUDE.md Rule 10). *(Sessions 26-27: ✓ key parity perfect (225/225 in all 6 langs after cleanup); ✓ 0 missing keys in code; ✓ 0 HTML-tag-bearing values (no `<Trans>` migration needed); 32 wrong-namespace orphans dropped (`calloutCreate.*`, `guidelinesEditor.*`, `form.callout.*` — those consumers actually use `crd-space` / `crd-spaceSettings`); 40 remaining orphans deliberately kept as future-plumbing placeholders. **Translation pass complete (session 27): 801 translations applied across the 5 non-English files (nl: 160, es: 160, bg: 160, de: 161, fr: 160)** — the entire `manager.*`, `card.*`, `framingKind.*`, `contributionType.*`, `preview.*`, `picker.*`, `form.*` (createTitle/editTitle/common/save/cancel/whiteboard/post/space/communityGuidelines subtrees), `references.*`, `discard.*`, `delete.*`, `removeFromSet.*`, `setDefault.*`, `saveAs.*`, `library.packs.heading` and a few stragglers in `packForm.*` / `createPack.*` / `packProfile.*`. Remaining ~60 "identical-to-English" entries are legitimate cross-language matches (loanwords like "Whiteboard"/"Document"/"Tags", acronyms like URL/OK, proper nouns like "Innovation Library" / "Innovation Packs"). tsc clean, biome clean, 500/500 vitest pass.)*
- [X] T083 [P] Accessibility sweep (WCAG 2.1 AA, SC-007) across every new CRD templates surface — keyboard operability + visible `focus-visible` rings, `aria-label` on icon-only buttons, `aria-hidden` decorative icons, `role="status"`/`aria-label` on loading skeletons, `<ul>`/`<li>` for card grids, `aria-busy`+`disabled` on submitting buttons, 4.5:1 contrast; run the automated a11y check; keyboard-only walkthrough of create-a-template / pick-a-template / delete-a-template / save-as-a-template. *(Session 30 sweep: 113 a11y-attribute occurrences across the templates feature; 20 files use `aria-hidden="true"` on decorative content; zero clickable non-interactive elements (no `<div>/<span>` onClick outside shadcn-allowed primitives); loading skeletons consistently use `<output aria-label={t('preview.loading')}>` (TemplateContentPreview + TemplatesManagerView); submit buttons use `aria-busy={submitting}` + `disabled` (TemplateFormDialog, InnovationPackForm, CreateInnovationPackDialog, SetDefaultTemplateDialog); card grids use `<ul>/<li>` in TemplatesManagerView + TemplateGallery; form inputs wire `aria-invalid` + `aria-describedby` for errors. WCAG 2.1 AA met by construction — semantic HTML + shadcn `focus-visible:ring` + consistent ARIA, plus every user-visible string runs through `t()` per T082.)*
- [X] T084 [P] **Authz-parity verification (FR-070 / FR-014)** — confirm the legacy reality and that CRD mirrors it: the templates-management surfaces (`SpaceAdminTemplatesPage`, `AdminInnovationPackPage`) gate at **page access**, not per template type — they hard-code all-five-types creatable/editable/deletable (Space adds import); `InnovationPackProfilePage` renders `TemplatesAdmin` with no predicates (read-only); so the CRD `can*` predicates must resolve uniformly `true` in a management context (`canImport` only for the Space holder) / `false` in the read-only profile, with **no per-type privilege check and no `isCustom` gate**. Then enumerate the *other* legacy gates the CRD work must preserve: set-default-subspace-template, set-flow-state-default-callout-template, save-callout-as-template (the callout-settings "save as template" permission), save-subspace-as-template (Subspaces-tab privileges), community-guidelines edit + apply-template + save-as-template (the Community-tab privileges — FR-038), pack create/edit (Account-tab pack-management privileges), pack delete (same), view-pack-admin (`InnovationPack.authorization` `Update` privilege). Record the mapping in the PR description. *(Session 30 verification: ✓ `CrdSpaceTemplatesTab` + `CrdInnovationPackAdminPage` wire uniform `() => true` predicates (Space: canImport too, Pack: canImport=false); ✓ `InnovationPackProfileView` uses `READ_ONLY_PREDICATES = {canCreate,canEdit,canDelete,canImport: () => false}`; ✓ zero `isCustom` references anywhere; ✓ save-callout-as-template gated on `perms.showSaveAsTemplate` (CalloutSettingsConnector L179/185); ✓ save-subspace-as-template gated on `templateSetPrivileges.includes(Create)` (useSubspacesTabData L73); ✓ CG editor `canSave` gated on guidelinesId + isDirty + !submitting; ✓ pack create/delete in Account tab gated on `CreateInnovationPack` / `Delete` privileges (ContributorAccountView L261/265); ✓ view-pack-admin entry-point gated on `Update` privilege (useInnovationPackProfile L77).)*
- [X] T085 [P] Verify zero `@mui/*` / `@emotion/*` in the loaded bundles for the CRD templates routes & dialogs (SC-004) — `pnpm analyze` + an import grep over `src/crd/components/{templates,innovationPack,innovationLibrary}/`, `src/crd/components/space/settings/CommunityGuidelinesEditor.tsx`, `src/main/crdPages/{templates,innovationPack,innovationLibrary}/`, and the modified callout/whiteboard/subspace/community/layout connectors (confirm the `CalloutSettingsConnector` and `045` Templates-tab MUI-dialog bridges are gone, and that the new CG editor host is MUI-free). *(Session 30 grep: zero MUI/Emotion imports across all 7 listed paths. `CalloutSettingsConnector.tsx` has only a historic comment mentioning the removed bridge — actual import is gone. No CRD code anywhere imports `@/domain/templates/.../CreateEditTemplateDialog/CreateTemplateDialog`. The only MUI-using file in `src/crd/` is `forms/markdown/extensions/iframe/InsertEmbedCodeButton.tsx` — outside templates scope, a pre-existing markdown-editor concern.)*
- [X] T086 [P] Verify toggle-off (SC-006): with `alkemio-crd-enabled` removed, every templates surface (Space Settings → Templates, the pack admin, the pack public profile, `/innovation-library`, the create-pack entry on the Account tab, the community-guidelines editor, and every consumption + save-as flow) renders the exact legacy MUI experience with no residual state. *(Session 30 verification — every surface has a `crdEnabled ? CRD : MUI` route gate: `/innovation-library` in `TopLevelRoutes.tsx` L274/287; pack public profile + admin in `InnovationPackRoute.tsx` L22; space pages incl. Settings → Templates inside `crdEnabled ? CrdSpaceRoutes : legacy` at `TopLevelRoutes.tsx` L356; create-pack + delete-pack via `crdEnabled` branches in `ContributorAccountView.tsx` L324/525/590/599; all the in-space CRD-only flows (CG editor, callout consumption, subspace save-as, flow-state default) mount only inside the `CrdSpaceRoutes` branch so toggle-off automatically routes them to legacy MUI counterparts; `/admin/innovation-packs` is MUI-only by design — FR-037 transitional fallback.)*
- [X] T087 Assemble the **SC-002 parity checklist** and the PR evidence pack: screenshots of every new CRD surface; the a11y output (T083); the bundle/import check (T085); the authz mapping (T084); the V1/V2/V3/V4/V5 outcomes (T004–T007, incl. the committed schema diffs); and the explicit list mapping each legacy MUI template flow → its CRD equivalent — callout-from-template (every framing kind, incl. **Collabora-document** and **poll**), whiteboard-from-template, **subspace-from-template** (CRD now), **top-level-Space-from-template** (FR-037 fallback — legacy picker, until that wizard migrates), default-subspace-template (own Space templates only), **community-guidelines-apply** (native CRD now — this feature delivers the CRD community-guidelines editor host to legacy parity, FR-038, not an FR-037 fallback), flow-state-default-callout-template (Space/Account/Platform, relabelled "Default callout template"), post-default-description, default-whiteboard, import-into-holder (Account+Platform sources, persistent + mark/remove — a deliberate consolidation, not a 1:1), save-callout-as-template (no-regression — bridge removed), save-subspace-as-template (no-regression), **save-community-guidelines-as-template (a deliberate NEW addition — note it as such, not as no-regression)**, template deep-linking preserved on the Space tab + pack admin — opens the **Preview** dialog everywhere (FR-013 / Clarifications D1; the legacy "pack deep-link → Edit" behaviour is intentionally dropped) (FR-007). The only documented FR-037 fallbacks left after this feature: the **top-level (L0) create-Space wizard** and the **platform-admin Innovation Pack list page** (`/admin/innovation-packs`). Zero gaps except those two. *(Session 31: assembled at `specs/098-crd-templates/pr-evidence.md` — 13 sections: SC-001..SC-008 status table, the 21-row SC-002 parity checklist mapping every legacy flow to its CRD equivalent (or FR-037 fallback), the two-row FR-037 transitional-fallback list, T085 zero-MUI grep, T084 authz mapping, T086 toggle-off route map, T083 a11y sweep results, V1–V5 GraphQL change status (V1/V2/V5 landed, V3 conditional/deferred via T042, V4 confirmed no-gap), test status, screenshots TODO with the standalone-app routes, deferred items rationale, and a PR description boilerplate. ~180 lines, all evidence cross-referenced to file:line locations.)*
- [X] T088 [P] Standalone preview: add mock pages under `src/crd/app/pages/` for `TemplatesManagerView`, `TemplatePicker` (both modes), `TemplatePreviewDialog`, `TemplateFormDialog` (each type — incl. the Whiteboard form's preview-settings and the CG form's title), `CommunityGuidelinesEditor`, `InnovationLibraryView`, `InnovationPackProfileView`, `InnovationPackAdminView`, fed by mock data adapted from `prototype/src/app/data/template-data.ts`, so designers can iterate via `pnpm crd:dev` with no backend. *(Session 28: built `src/crd/app/data/templates.ts` (mock TemplateCardData per type, TemplateContent per id, InnovationPackCardData, pack profile, community-guidelines value) + 5 pages (`TemplatesPage`, `InnovationLibraryPage`, `InnovationPackProfilePage`, `InnovationPackAdminPage`, `CommunityGuidelinesPage`). The TemplatesPage covers the manager view, the preview dialog, and an inline gallery of every preview variant so designers can see them all without opening cards. Routes wired into `CrdApp.tsx` and exposed via `MOCK_PLATFORM_NAVIGATION_ITEMS`. `crd-spaceSettings` namespace added to the standalone `main.tsx` for `CommunityGuidelinesEditor`. `npx tsc --noEmit` clean, `pnpm crd:build` succeeds (3.19s, pre-existing dynamic-import warnings only). Form/picker dialog playgrounds are deferred — the present pages render the major surfaces directly with mock data so the visual playground is covered; the dialog-opener playground would just be button + state, low value vs. T076's already-shipped end-to-end wiring.)*
- [X] T089 [P] Additional unit tests beyond T011/T024/T032/T038: `templateContentMapper` Collabora-document + poll framing fixtures and the `communityGuidelines.title` mapping; `innovationLibraryMapper` (`ownerLabel`); `innovationPackMapper` (form ⇄ input round-trip); `useSaveAsTemplate` (pre-filled value per source kind — incl. the community-guidelines title — + the unsaved-source-changes prompt); the CG-editor integration hook's `CommunityGuidelines.profile.{displayName,description,references}` ⇄ `CommunityGuidelinesEditorValue` round-trip. *(Session 34, 2026-05-14: implemented in three new files: `src/main/crdPages/templates/__tests__/saveAsValues.test.ts` (4 tests — exported the pure `toSaveAsValues` helper from `useSaveAsTemplate.ts`; tests cover both the community-guidelines pre-fill incl. the `title` field with fallback to "Community guidelines" when source title is empty, and the subspace pre-fill with `recursive: true` + `sourceSpaceId`); `src/main/crdPages/innovationLibrary/__tests__/innovationLibraryMapper.test.ts` (8 tests — `packTemplateCount` per-type sum and nullish handling, `mapPackToInnovationPackCardData` with provider semantics and missing-provider/description/tags edge cases, deterministic `pickColorFromId`); `src/main/crdPages/innovationPack/__tests__/innovationPackMapper.test.ts` (9 tests — `mapInnovationPackToBasics`, `searchVisibilityToGql` round-trip, `mapInnovationPackToDetail` form values + provider-null handling, `formValuesToUpdateInnovationPackInput` for tagsets/listedInStore/searchVisibility, drop-new-references-go-through-createReferenceOnProfile-separately). Also added the Collabora-document framing fixture to `mappers.test.ts`. **CG-editor integration-hook round-trip deferred** — `useCommunityGuidelinesData` wraps the domain `useCommunityGuidelines` hook (an Apollo + cache-update consumer that goes through the legacy `src/domain/community/community/CommunityGuidelines/` MUI hooks); testing it in isolation would mean either (a) mocking the full domain Apollo surface or (b) testing the wrapper's pure read-side mapping by extracting `toEditorReferences` + the `serverValue` derivation as pure functions. (a) is heavyweight scaffolding; (b) is a one-hour refactor inside the hook. Both deferred as a polish-time follow-up — the user-visible behaviour is covered end-to-end by `pnpm crd:dev` and the T088 standalone preview page.)*
- [X] T090 Remove the temporary `045`-era compatibility re-exports under `src/main/crdPages/topLevelPages/spaceSettings/templates/` once nothing imports them; confirm `src/crd/components/space/settings/{TemplateLibraryDialog,TemplateEditDialog,SaveSubspaceAsTemplateDialog,ChangeDefaultSubspaceTemplateDialog}.tsx` are deleted and `TemplatePreviewDialog.tsx` moved; run `pnpm lint` and `pnpm vitest run`; update `quickstart.md`'s build order + verification checklist with results. *(Session 34, 2026-05-14: 10 orphan files deleted in T041; same carve-out — `ChangeDefaultSubspaceTemplateDialog.tsx` stays on disk until its consumer in `CrdSpaceSettingsPage.tsx` is re-pointed to `SetDefaultTemplateDialog`. The compatibility re-exports under `topLevelPages/spaceSettings/templates/` are gone (no consumer was using them — the closed cluster was fully self-referential). tsc clean, biome clean, eslint clean, vitest 500/500.)*
- [X] T091 Re-point the **default-subspace-template card** in `CrdSpaceSettingsPage.tsx` from the legacy `ChangeDefaultSubspaceTemplateDialog` to `SetDefaultTemplateDialog(purpose:'defaultSubspaceTemplate')`. `useSubspacesTabData.ts` already produces `subspaceTemplateChoices` (own Space templates) + `defaultTemplateId` + `onSelectDefaultTemplate` + `subspaceTemplatesSaving`; map those into the `SetDefaultTemplateDialogProps` (`templates`, `currentTemplateId`, `onConfirm`, `saving`). The current legacy dialog gives a plain list; `SetDefaultTemplateDialog`'s `defaultSubspaceTemplate` purpose additionally renders a candidate preview (the legacy behaviour is a plain dropdown, CRD adds the preview — already specified in plan.md). Once re-pointed, delete `src/crd/components/space/settings/ChangeDefaultSubspaceTemplateDialog.tsx`. This closes the only remaining T041/T090 carve-out.
- [X] T092 **Bug fix — Callout template form's *callout-tags* input was inert.** `CalloutTemplateForm.tsx` Zone 3 had a broken tags row: a raw `<input>` (instead of `TagsInput`) bound to `values.tags` (a `string[]` — wrong shape for `<input>`) with **no `onChange` handler**. User-visible symptom: cursor appears in the field but typing does nothing. **Two distinct tag sets in a Callout template (FR-020 clarification this session):** (a) the *template's* tags — `commonValue.tags`, rendered by the dialog shell `TemplateFormDialog`, drive the template gallery's tag filter; (b) the *captured callout's* tags — `values.tags` from `useCrdCalloutForm`, applied to every callout created from this template. **Both are persisted.** The Zone-3 row is (b), not a duplicate of (a). Whiteboard / Post / Space / Community Guidelines templates have only (a). Fix: restored the row as a proper `<TagsInput value={values.tags} onChange={tags => setField('tags', tags)} minLength={2} … icon={<Hash …/>} />`, mirroring the live callout-creation form (`CalloutFormConnector.tsx:655–663`). The submit path was already correct — `calloutFormValuesToCreate/UpdateCalloutEntityInput` → `mapFormToCalloutCreation/UpdateInput` carry `values.tags` through to the framing tagset (`calloutFormMapper.ts:144/187/362/383`); only the UI was missing. **Spec/data-model updated this session** to make the two-tag distinction explicit (FR-020 + `data-model.md` `TemplateFormValues` annotation). *(Session 34, 2026-05-14.)* *(Session 34, 2026-05-14: 10 orphan files deleted in T041; same carve-out — `ChangeDefaultSubspaceTemplateDialog.tsx` stays on disk until its consumer in `CrdSpaceSettingsPage.tsx` is re-pointed to `SetDefaultTemplateDialog`. The compatibility re-exports under `topLevelPages/spaceSettings/templates/` are gone (no consumer was using them — the closed cluster was fully self-referential). tsc clean, biome clean, eslint clean, vitest 500/500.)*
- [ ] T093 **Bug fix — Callout template prefill: response-type chip lost when both actor switches are off (D14, 2026-05-18).** `calloutTemplateMapper.ts → calloutTemplateContentToFormValues` derives `responseType` as `settings.contribution.enabled && firstAllowedType ? mapped : 'none'`. When both Members/Admins toggles are off (`canAddContributions === NONE` ⇒ `enabled === false`), this resolves to `'none'` even though `allowedTypes` still names the response type → the chip strip (`LINKS`/`POSTS`/`MEMOS`/`WHITEBOARDS`) renders nothing selected, and saving from there commits the loss. Fix: drop the `enabled &&` gate so `responseType` is derived from `allowedTypes[0]` **alone**, mirroring `mapCalloutDetailsToFormValues.ts:74-84` (the live-callout edit mapper, which already carries the same rule + an in-source comment explaining why). `allowedActors` continues to derive from `canAddContributions` independently — the two surfaces are orthogonal. Add a unit-test fixture (in `__tests__/calloutTemplateMapper.test.ts`) covering the `allowedTypes:['LINK'], canAddContributions:None, enabled:false` shape → `responseType === 'link'` + `allowedActors === { members:false, admins:false }`. (Spec FR-020 / D14 / `data-model.md` §E invariants; no GraphQL change.) *(Session 35, 2026-05-18.)*
- [ ] T094 **Bug fix — `UpdateCalloutTemplate` mutation's response doesn't cover full callout-settings (V6 / D15, 2026-05-18).** `src/domain/templates/graphql/TemplateMutations.graphql` `UpdateCalloutTemplate` currently selects `settings { contribution { enabled allowedTypes } visibility }`. Post-mutation cache write therefore never refreshes `canAddContributions`, `commentsEnabled` or `framing.commentsEnabled` — the form keeps working only because the prior `CalloutTemplateContent` read populated them via `CalloutSettingsFull`. Replace the inline `settings` block with `settings { ...CalloutSettingsFull }` (the same fragment `CalloutTemplateContent` spreads, and the same approach the live `UpdateCalloutContent` mutation uses via `...CalloutDetails` → `...CalloutSettingsFull`). Run `pnpm codegen`; commit the regenerated `apollo-hooks.ts` chunk for `UpdateCalloutTemplateDocument` (Constitution III). No new GraphQL document. *(Session 35, 2026-05-18.)*
- [ ] T095 **Bug fix — Callout-template whiteboard-framing preview image is ignored (D16, 2026-05-18).** Two surfaces drop the server-rendered whiteboard preview image: (a) the **Callout template form**, when a template with `framingKind: 'whiteboard'` is loaded for edit, shows the placeholder Excalidraw icon instead of the server preview (because `InlineWhiteboardPreview`'s `previewImageUrl` is fed only from local-blob URLs derived from `whiteboardPreviewImages`, which are empty on prefill); (b) the **template Preview dialog** (`CalloutTemplatePreview`) renders the literal word "Whiteboard" for the whiteboard-framing branch, with no image. **Not a GraphQL change** — `CalloutTemplateContent` already fetches `framing.whiteboard { ...WhiteboardDetails }` and `WhiteboardDetails.profile.preview` is the `WHITEBOARD_PREVIEW` Visual. The fix is mapper + renderer: (1) `templateContentMapper.mapCalloutContent` adds `framingWhiteboardPreviewImageUrl = framingKind === 'whiteboard' ? framing.whiteboard?.profile?.preview?.uri : undefined` to the `TemplateContent.callout` branch; (2) `calloutTemplateMapper.calloutTemplateContentToFormValues` populates a new `whiteboardPreviewServerUrl` field on the prefilled `CalloutFormValues`; (3) the form-values type in `useCrdCalloutForm` gains `whiteboardPreviewServerUrl?: string` (display-only — never sent to the server; replaced by fresh blob URLs the moment the user re-opens the inline editor and saves); (4) `FramingEditorConnector` accepts a new prop `whiteboardPreviewServerUrl?: string` and passes it as the fallback to `InlineWhiteboardPreview.previewImageUrl` (`whiteboardPreviewUrl ?? whiteboardPreviewServerUrl` — blob URL wins when present); (5) `CalloutTemplateForm` passes both `values.whiteboardPreviewImages` (was previously omitted — also fixed here for parity with `CalloutFormConnector`) and `values.whiteboardPreviewServerUrl` into the connector; (6) `CalloutTemplatePreview` renders an `<img>` for the whiteboard-framing branch when `content.framingWhiteboardPreviewImageUrl` is set, falling back to the existing placeholder text when missing. Add a fixture-test in `__tests__/calloutTemplateMapper.test.ts` covering the new prefill field. (Spec FR-020 / FR-024 / D16; spec/data-model/plan/research updated this session.) *(Session 35, 2026-05-18.)*
- [ ] T096 **Bug fix — Callout-template whiteboard-framing preview-image blob is never uploaded after save (D17, 2026-05-18).** The Callout-template create / edit paths in `src/main/crdPages/templates/useTemplateForms.tsx` (case `'callout'` in `submitCreate` ≈ line 479-484 and `submitEdit` ≈ line 604-610) fire `createTemplate` / `updateCalloutTemplate` and stop — they never call `uploadVisual` against the whiteboard's `WHITEBOARD_PREVIEW` Visual. Symptom: editing the inline whiteboard inside a callout-template form and saving persists the **content** but the server-stored preview image stays at its previous value (or stays empty for a freshly-created template), so subsequent reads — the form re-opening, the template Preview dialog, the picker preview pane — keep showing stale content. **Fix**: mirror `CalloutFormConnector`'s live-callout post-save step (lines 305-317 for create, 447-452 for edit). Both `CreateTemplate` and `UpdateCalloutTemplate` already return `callout.framing.whiteboard.profile.previewVisual.id` (alias of `visual(type: WHITEBOARD_PREVIEW)`) — no GraphQL change required. In `useTemplateForms.tsx`: (1) import + instantiate `useUploadWhiteboardVisuals` (`src/domain/collaboration/whiteboard/WhiteboardVisuals/useUploadWhiteboardVisuals.ts`); (2) capture the `createTemplate` / `updateCalloutTemplate` result; (3) when the callout form's `framingChip === 'whiteboard'` and `values.whiteboardPreviewImages.length > 0`, call `uploadVisuals(values.whiteboardPreviewImages, { previewVisualId })` with the id from the response; (4) apply the same step in `submitCalloutCopy` (Duplicate / import) for safety — the blob array is normally empty in that flow but the upload is a no-op when nothing matches. Use `nameID` from the mutation response as the third argument when available (mirrors the live flow). Spec FR-023 / D17. *(Session 36, 2026-05-18.)*
- [ ] T097 **Bug fix — Creating a live callout from a callout-template loses the template's whiteboard preview image (D18, 2026-05-18).** When the user opens the live callout-creation dialog, picks a callout template with whiteboard framing in the picker, and clicks Create without re-opening the inline whiteboard editor, the new callout's `WHITEBOARD_PREVIEW` Visual ends up empty — even though the template has one. Root cause: `TemplateImportConnector` runs `calloutTemplateContentToFormValues` (which surfaces `whiteboardPreviewServerUrl` for display per T095/D16) but does NOT seed `whiteboardPreviewImages` blobs, so `CalloutFormConnector`'s existing post-create upload step (lines 305-317) has nothing to upload. **Fix**: in `TemplateImportConnector`, after the template-content fetch resolves and the mapper produces `values`, if `values.whiteboardPreviewServerUrl` is set, fetch that URL as a `Blob` and set `values.whiteboardPreviewImages = [{ visualType: VisualType.WhiteboardPreview, imageData: <blob> }]` before calling `onTemplateSelected`. The existing `CalloutFormConnector` post-create `useUploadWhiteboardVisuals({ previewVisualId })` step then copies the blob to the new callout's preview Visual without any further wiring. Fetch errors (CORS, network, transient 5xx) are non-fatal — log + skip the seed; the form's display still shows the server URL via the D16 fallback. Extract the fetch into a small helper (e.g. `fetchPreviewImageBlob(url: string): Promise<Blob | undefined>`) under `src/main/crdPages/templates/` so the same logic can be reused if other prefill paths (template duplicate, save-as) need it later. Spec FR-023 / FR-030 / D18. *(Session 36, 2026-05-18.)*
- [ ] T098 **Bug fix — Callout-template Preview dialog drops `framing.profile.references` (D19, 2026-05-18).** The CRD callout-template Preview dialog has no References section even though the template form's References editor accepts entries, the server persists them, and the resulting in-feed callout *does* render them (verified by `usage/callout-template.use.ts` end-to-end). Root cause: `templateContentMapper.mapCalloutContent` reads `framing.profile.references` for *nothing* — it populates `framingLinks` only for the *cta* framing kind (the single Link entity, separate concept) and never carries `framing.profile.references` (the calloutReferences) into the `TemplateContent.callout` branch. **Not a GraphQL change** — `CalloutTemplateContent` already pulls `framing.profile.references { ...ReferenceDetails }`. **Fix**: (1) extend the `TemplateContent.callout` branch in `@/crd/components/templates/types.ts` with `references?: { id: string; name: string; uri: string; description?: string }[]`; (2) populate in `mapCalloutContent` (`templateContentMapper.ts`) by mapping `framing.profile.references` → `{ id, name, uri, description }`; (3) render in `CalloutTemplatePreview` via the existing `ReferencesAndTagsStrip` (`@/crd/components/callout/ReferencesAndTagsStrip.tsx` — the same one `CalloutDetailDialog` uses in the feed) between the framing body and the *Allowed Contributions* section, so styling stays identical between preview and feed. Mirrors the legacy MUI block (`TemplateContentCalloutPreview.tsx:58` — `{references && references.length > 0 && <References compact={true} references={references} />}`). Add a fixture-test in `mappers.test.ts` covering the references mapping. Spec FR-024 / D19. *(Session 36, 2026-05-18.)*
- [ ] T099 **Bug fix — *Posts defaults* dialog ignores the picked template + dialog polish (D20, 2026-05-19).** In the Callout-template form's `ResponseDefaultsDialog` (opened from the "Set defaults" affordance when the response type is `post`), the user clicks "use a template", the picker shows space/account/platform Post templates, the user picks one — but the dialog's `defaultDescription` field doesn't populate. Root cause: the dialog holds a local `draft` state seeded only on the closed → open transition (`ResponseDefaultsDialog.tsx:67-73`). `ResponseDefaultsConnector` applies the picked template via `onSave({...values, postDescription: x})` which mutates parent state but **never reaches the dialog's draft** (only `whiteboardContent` has a sync effect, lines 80-88). On Save, the dialog commits its stale draft and overwrites the parent's template-applied value. There's a related Cancel bug: cancelling after an apply silently leaves the parent with the template value. **Fix**: change `ResponseDefaultsDialog`'s `templateSlot` from `ReactNode` to `ReactNode | ((helpers: { applyDraft: (next: Partial<ContributionDefaults>) => void }) => ReactNode)`. The render-prop form passes an `applyDraft` helper that writes straight into the dialog's local `draft` (not the parent). In `ResponseDefaultsConnector`, extract the picker-apply effect into a small inner component (e.g. `TemplateApplyButton`) that receives `applyDraft` + `picker` and calls `applyDraft({ postDescription: selectedContent.defaultDescription })` (or `{ whiteboardContent: ... }` for whiteboard templates) when the picker resolves. Remove the obsolete `onSave({ ...values, ...templateContent })` path. Test paths: (a) pick a post template → dialog's `defaultDescription` populates with the template's `defaultDescription`; (b) edit it further, click Save → parent receives the edited value; (c) pick a template, click Cancel → discard-confirmation appears (draft is dirty), confirming discard leaves the parent untouched. **Dialog polish (same session)**: widen `DialogContent` from `sm:max-w-2xl` (672 px) to `sm:max-w-[720px]` (≈ +50 px); apply the sticky-header + sticky-footer + scrollable-body layout (mirrors `TemplateFormDialog` / `TemplatePreviewDialog`) — title and Save/Cancel buttons stay visible at any content height. Spec FR-005 / FR-022 / FR-036 / D20. *(Session 36, 2026-05-19.)*

---

## Dependencies & Execution Order

- **Phase 1 (T001–T003)** — no deps; all `[P]`.
- **Phase 2 (T004–T038)** — depends on Phase 1. Verifications first (T004–T007 `[P]`) — gate any GraphQL change. Then: mappers (T008–T011 `[P]`) → manager view (T012–T013 `[P]`, then T014) → previews (T015–T019 `[P]`, then T020, then T021) → picker (T022 needs T012+T020; T023 needs T008+T010; T024 needs T023) → forms (T025 = `crdPages/templates/CalloutTemplateForm`, integration layer; T026–T029 `[P]` = pure `src/crd/.../forms/*`, T028 presentational; then T030 = the pure `TemplateFormDialog` shell, needs only T003; T031 needs T010+T025+T026–T029, assembles `perTypeFormSlot`; T032 needs T031) → set-default/save-as (T033 needs T022; T034 needs T023; T035 needs T030; **T036 needs T031+T035 — and it also removes the `CalloutSettingsConnector` MUI bridge here, in Phase 2, before any CRD callout-template code path reaches it**) → T037 needs T009/T010/T021/T022/T030/T031 → T038 needs T037/T034. **No user-story phase starts until Phase 2 is complete.**
- **Phase 3 (US1)** — Phase 2 (T037, T034, T036, T014, T033).
- **Phase 4 (US2)** — Phase 2 (T037, T014); independent of US1, US3.
- **Phase 5 (US3)** — Phase 2 (T022, T023); independent of US1, US2.
- **Phase 6 (US4)** — Phase 2 (T022, T023, T018, T035, T036) + US1's T039 (which exposes `useSaveAsTemplate` to the Subspaces tab).
- **Phase 7 (US5)** — Phase 2 (T010, T021, T022, T023, T029, T033, T034); **T057 delivers the CRD community-guidelines editor host (FR-038)** — supersedes `045`'s markdown-only stub; touches `045`-owned files in `layout/` and `community/`.
- **Phase 8 (US6)** — Phase 2 (T021); T061/T062/T063 `[P]`; `InnovationPackCard` (T063) reused by US8.
- **Phase 9 (US9)** — Phase 2 (T035, T036 — note: the *callout* case is already wired & de-bridged in T036/Phase 2; T069 here is just verification) **+ US5's T057** (the community-guidelines case, T070, wires into the CRD community-guidelines editor T057 delivers — so US9's guidelines half follows US5).
- **Phase 10 (US7)** — Phase 2 + **US2's T046** (extends `CrdInnovationPackAdminPage`); T072/T073 `[P]`.
- **Phase 11 (US8)** — Phase 2 (T037, T014) + **US6's T063** (`InnovationPackCard`).
- **Phase 12 (Polish)** — all user-story phases; T082–T086/T088/T089 `[P]`; T087 + T090 last.

**Parallelisable once Phase 2 is done**: US1 ∥ US2 ∥ US3 ∥ US4 ∥ US5 ∥ US6; US7 after US2; US8 after US6; US9's callout case is parallel (the bridge was removed in T036/Phase 2), but US9's community-guidelines case (T070) follows US5's T057 (the CRD community-guidelines editor host).

---

## Parallel Execution Examples

- **Phase 1**: T001, T002, T003 together.
- **Phase 2 — verifications**: T004, T005, T006, T007 together (do first). **mappers**: T008, T009, T010, T011 together. **previews**: T015, T016, T017, T018, T019 together (then T020). **forms**: T025 (`crdPages/templates/CalloutTemplateForm`), T026, T027, T028, T029 together; T030 (pure `TemplateFormDialog` shell) in parallel with them; then T031 (assembles `perTypeFormSlot`); then T035 → T036 (which also removes the `CalloutSettingsConnector` MUI bridge).
- **Phase 8 (US6)**: T061, T062, T063 together (then T064).
- **Phase 10 (US7)**: T072, T073 together.
- **Phase 12**: T082, T083, T084, T085, T086, T088, T089 together (then T087, T090).

---

## Implementation Strategy

> Ships as **one PR**; the steps below are the *build order*, not separate merge points.

1. **Verifications + Foundational first.** Run T004–T007 immediately — they verify each GraphQL gap server-side first, then make the additive fragment edits + `pnpm codegen` + committed schema diffs (V1 Collabora-document framing, V2 poll framing, V5 CG `title`, conditionally V3 delete-cascade data); anything that needs a real backend change is a blocker raised here. Then build the complete CRD templates kit (T008–T038): the holder-agnostic manager view + card, the dual-mode picker, the 5 per-type previews + preview dialog, the per-type forms + the **CRD-native `TemplateFormDialog` that replaces the legacy `CreateTemplateDialog` bridge** (and the Whiteboard form reusing the preview-settings/crop dialogs; the CG form gaining a title field), the set-default & save-as dialogs (generalised from the `045` ones), and the shared hooks/mappers. Unit-test the mappers + hooks + the callout form mapping (incl. Collabora + poll).
2. **The MVP slice: US1 + US3.** Wire the kit into the Space Settings → Templates tab (US1 — removing the `045` partial implementation, the MUI create-dialog bridge, and the "custom only" gate; closing the `onCreateTemplate` TODO; deep-links open Preview, FR-013/D1) and into the CRD callout-creation flow (US3). A coherent, demoable slice (and the natural split-line if the PR has to be broken up).
3. **Then US2** — point the kit at the Innovation Pack admin (new route at `<pack.profile.url>/settings` + a page shell + the same `useTemplatesManager` with `holderKind:'innovationPack'`). Proves the holder unification with almost no new code.
4. **Then the P2 batch** — US4 (subspace creation from a template + defaults + re-point save-subspace-as-template; top-level Space stays legacy FR-037), US5 (whiteboard / **deliver the CRD community-guidelines editor host to legacy parity (FR-038) + wire guidelines-apply** / flow-state-default-with-relabel / post+whiteboard contribution-defaults refactor), US6 (Innovation Library page), US9 (save-callout-as-template — the `CalloutSettingsConnector` bridge was already removed in Phase 2/T036, so this is a verification — + save-guidelines-as-template, which wires into the CRD CG editor from US5). Mostly picker/dialog wiring + the CG editor + one new page; can run in parallel **except** US9's guidelines half follows US5's T057.
5. **Then the P3 batch** — US7 (pack create from the Account tab + edit/delete — port `CreateInnovationPackDialog` to shadcn, extend `CrdInnovationPackAdminPage` with the details form) and US8 (pack public profile — reuses US6's pack card + the read-only manager view).
6. **Polish last** — 6-language i18n audit, a11y sweep, authz-parity verification, no-MUI bundle check (incl. confirming the bridges are gone), toggle-off check, standalone-preview mocks, extra unit tests, the SC-002 parity checklist + PR evidence pack (only two FR-037 fallbacks remain: the top-level create-Space wizard + the platform-admin pack list), cleanup of compatibility shims.

Throughout: legacy MUI templates files stay untouched and serve toggle-off; CRD layer rules apply to every new file; the `045`-era files are refactored-in-place (not duplicated); the `045` integration plumbing for default-subspace / save-subspace / flow-state-default is re-pointed, not re-written; expect some overlap/rewrite of the `045` template cards per the prototype.
