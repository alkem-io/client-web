# Tasks: CRD Templates System

**Input**: `/specs/098-crd-templates/` — `plan.md` (rev. 4 — post-`/speckit.analyze` interview, 2026-05-12), `spec.md` (revised after analyze + the MUI-reality pass + the 2026-05-12 interview — 9 user stories US1–US9, FR-001..FR-071 incl. FR-038 the CRD community-guidelines editor, SC-001..SC-008), `research.md` (refreshed 2026-05-12), `data-model.md`, `contracts/*.ts`, `quickstart.md`, `assumptions.md` (update log = decision status). Note: `analysis.md` is the first-pass point-in-time report and is superseded — the latest clearance state is the 2026-05-12 interview decisions (spec.md "Clarifications" session 2026-05-12 / `assumptions.md` update log); re-run `/speckit.analyze` for a fresh report.

**Tests**: targeted unit tests only — the pure mappers, the key integration hooks, the Callout-template form↔input mapping (incl. Collabora-document + poll framing), the no-dangling-default delete path, the `useSaveAsTemplate` pre-fill (incl. community-guidelines title). No exhaustive component suite; no TDD.

**Delivery**: one PR (the phases below are internal build-order milestones, not separate merge points — natural split-line if it grows unmanageable is Phase 1+2+US1 ∣ the rest).

**Organization**: Phase 1 Setup → Phase 2 Foundational (open verifications + the complete CRD "templates kit" — blocking for every story) → Phases 3–11 one per user story in priority order (US1, US2, US3 = P1; US4, US5, US6, US9 = P2; US7, US8 = P3) → Phase 12 Polish. Task IDs are contiguous (T001…); `[P]` = parallelisable (different files, no incomplete deps); `[USn]` on user-story tasks only.

**Path conventions**: presentational CRD components under `src/crd/components/{templates,innovationPack,innovationLibrary}/` (no MUI/Apollo/router/formik; plain-TS props; callbacks for behaviour; Tailwind + lucide-react + semantic typography; `useTranslation('crd-templates')`; markdown via `MarkdownContent`/`InlineMarkdown`; `date-fns`; no `useMemo`/`useCallback`/`React.memo`). Integration (data/mappers/mutations/routing/toggle/bridge-replacements) under `src/main/crdPages/{templates,innovationPack,innovationLibrary}/` and `src/main/crdPages/topLevelPages/spaceSettings/{templates,subspaces,layout,community}/`. The `045`-era files under `src/crd/components/space/settings/` are refactored-in-place (section view → ≤10-line wrapper; dialogs → removed/moved/generalised). Legacy `src/domain/{templates,templates-manager,InnovationPack,spaceAdmin/SpaceAdminTemplates}/`, `src/domain/platformAdmin/domain/innovationPacks/` (the `/admin/innovation-packs` list — NOT migrated by this feature, FR-037), and `src/main/topLevelPages/InnovationLibraryPage/` are UNCHANGED (serve toggle-off; read for reference). Routing edits land in `src/main/routing/TopLevelRoutes.tsx`, `src/domain/InnovationPack/InnovationPackRoute.tsx` (BOTH pack routes), and `src/domain/community/contributor/Account/ContributorAccountView.tsx` (create + delete pack) — NOT in `AdminInnovationPackRoutes.tsx`. Prototype refs (read-only): `prototype/src/app/components/{space/SpaceSettingsTemplates.tsx,template-library/*}` + `prototype/src/app/pages/Template*Page.tsx` + `prototype/src/app/data/template-data.ts`.

---

## Phase 1: Setup (Shared Infrastructure)

- [ ] T001 [P] Audit the shadcn primitives this feature needs (`collapsible`, `dropdown-menu`, `scroll-area`, `select`, `switch`, `tabs`, `badge`, `card`, `dialog`, `skeleton`, `tooltip`, `separator`, `input`, `textarea`, `popover`, segmented-control / `command` for the type filter) against `src/crd/primitives/`; port any missing one verbatim from `prototype/src/app/components/ui/` into `src/crd/primitives/<name>.tsx` (update `cn()` import to `@/crd/lib/utils`; verify zero MUI imports).
- [ ] T002 [P] Scaffold the `crd-templates` i18n namespace: create empty `src/crd/i18n/templates/templates.{en,nl,es,bg,de,fr}.json` (`{}`); register `'crd-templates'` in `crdNamespaceImports` in `src/core/i18n/config.ts`; add the namespace type entry in `@types/i18next.d.ts`; wire it into `src/crd/app/main.tsx` if that eagerly imports CRD namespaces.
- [ ] T003 [P] Create `src/crd/components/templates/types.ts` — the CRD prop types from `contracts/`: `TemplateType` string union + `TEMPLATE_TYPE_ORDER`; `TemplateCardData`; `TemplateCategorySection`; `TemplateAction`; **`FramingKind` — re-export the existing CRD callout-layer union (`FramingChip` = `'none' | 'whiteboard' | 'memo' | 'document' | 'cta' | 'image' | 'poll'`, from `src/crd/forms/callout/types.ts`); do NOT define a parallel `FramingKind`** (the Callout template form reuses those connectors; `'none'` = plain text, `'document'` = Collabora-document per FR-020/FR-030, `'cta'` = link, `'image'` = media-gallery, `'poll'` = poll — **poll content is in scope** (V2/T005 add it to the `CalloutTemplateContent` fragment), so the type carries it like the others); the `TemplateContent` discriminated union (per `contracts/template-preview.ts` + `data-model.md` §B — incl. `framingCollaboraDoc?` and `framingPoll?` ( `{ question; options? }` ), and the `communityGuidelines` branch's `title: string`); the `TemplateFormValues` discriminated union (incl. the `whiteboard` branch's `previewSettings?` and the `communityGuidelines` branch's `title`); `TemplateFormErrors`; `ReferenceRow`; the `CommunityGuidelinesEditor*` prop types (FR-038, per `data-model.md` §B). Plain TS only.

---

## Phase 2: Foundational — open verifications + the CRD "templates kit" (BLOCKING PREREQUISITE for all user stories)

> **No user-story work may begin until this phase is complete.** Do the V-tasks first — they decide whether a GraphQL change is needed.

### Open verifications (do first; see plan rev. 4 §"Open verifications")

> **All four are additive `.graphql` *fragment* edits (no new documents) + `pnpm codegen` + a committed schema diff (Constitution III). First, for each, verify the field exists server-side on the type the fragment reaches — the schema enum already has `CollaboraDocument` + `Poll`, and `CommunityGuidelines` has a profile displayName/references, so these are expected to be fragment-only changes; anything that needs a real backend change is a BLOCKER raised before the dependent UI is built (not a quiet workaround).** Record every outcome in `research.md`.

- [ ] T004 [P] **V1 + V2** — extend `CalloutTemplateContent` to carry **Collabora-document** framing (V1) and **poll** framing (V2). The fragment currently fetches `framing.{type, whiteboard{content}, link, memo, mediaGallery}` only — no Collabora-document field, no poll field — but a Callout template must store both (spec FR-020/FR-030; both confirmed in scope, see spec Clarifications D2). Add the two fields to `src/domain/templates/graphql/TemplateContent.graphql` (after confirming they exist on `CalloutFraming`/the framing type server-side), run `pnpm codegen`, prepare the committed schema diff. *(Confirmed gaps.)*
- [ ] T005 [P] **V5** — verify whether `CommunityGuidelinesTemplateContent` exposes a distinct **title** for the guidelines (it likely already fetches `profile.displayName`); if not, add it. Also confirm the **live** community-guidelines query (the one the CRD editor host uses — FR-038) exposes `profile.{displayName, description, references}`; add anything missing via codegen + schema diff. *(Likely already present — confirm; gates the CG template `title` field and the CG editor host.)*
- [ ] T006 [P] **V3** — verify whether `deleteTemplate` server-side nulls referencing `TemplateDefault`s and the referencing innovation-flow-state default callout template. If the client must clear them (FR-019), verify it can learn *which* defaults/flow-states reference template X from existing queries; add a field via codegen if not. Record the outcome. *(Conditional.)*
- [ ] T007 [P] **V4** — verify `useInnovationLibraryQuery` exposes everything the CRD gallery card needs (template displayName, description, tags, visual, type, owning-pack name + url); also check whether the legacy `LibraryTemplatesView` has a free-text search box (not just the type filter) — if it does, add it to the Library scope (FR-051) and the Library tasks (T062/T064). Record the outcome.

### Mappers (integration layer, pure functions)

- [ ] T008 [P] `src/main/crdPages/templates/templateCardMapper.ts` — GraphQL `Template` → `TemplateCardData`: `bannerUrl = profile.visual?.uri || undefined`, `color = pickColorFromId(template.id)`, `type` mapped from the GraphQL enum to the CRD string union, `ownerLabel` undefined (set only by library/account callers).
- [ ] T009 [P] `src/main/crdPages/templates/templatesManagerMapper.ts` — GraphQL `TemplatesSet` → `TemplateCategorySection[]` in `TEMPLATE_TYPE_ORDER` (uses `templateCardMapper`).
- [ ] T010 [P] `src/main/crdPages/templates/templateContentMapper.ts` — port the per-type content-shaping logic from `src/domain/templates/components/Previews/*` as **pure functions** → the `TemplateContent` union; handle the **Collabora-document** and **poll** framing kinds (per T004's outcome — `framingCollaboraDoc` / `framingPoll`), and the `communityGuidelines` branch's `title` (= `profile.displayName`, per T005/V5).
- [ ] T011 [P] Unit tests `src/main/crdPages/templates/__tests__/mappers.test.ts` — fixtures per template type (incl. callout-with-each-framing-kind — every kind incl. Collabora-document and poll, space-with-nested-subspaces, guidelines-with-title-and-references, missing-banner → `bannerUrl` undefined) exercising the three mappers.

### Manager view + card + previews

- [ ] T012 [P] `src/crd/components/templates/TemplateCard.tsx` — one template card per `contracts/templates-manager.ts` (reuse / rewrite the `045` card per the prototype): type icon/badge, name, truncated description, tags, banner (real image else `color` gradient), a kebab `dropdown-menu` whose entries are gated by the `can*`-derived flags passed in, body click → `onPreview`. WCAG: `<button>`/`<a>` interactives, `aria-label` on the icon-only kebab. **No `isCustom` gate.**
- [ ] T013 [P] `src/crd/components/templates/TemplateSectionHeader.tsx` — per-type header: icon, localized title + subtitle, template count, optional "Create new" + "Library" (import) buttons, collapse chevron.
- [ ] T014 `src/crd/components/templates/TemplatesManagerView.tsx` — generalise `src/crd/components/space/settings/SpaceSettingsTemplatesView.tsx` to `TemplatesManagerViewProps` (`holderKind`, `categories`, `loading`, `duplicatingId`/`deletingId`, `canCreate/canEdit/canDelete/canImport(type)` predicates, `onCreate/onImport/onTemplateAction`); one collapsible section per type via `TemplateSectionHeader` + a grid of `TemplateCard`; section-visibility rule (`templates.length>0 || canCreate || canImport`); empty-but-addable → empty state + add affordance; inline client-side search box (visual only); per-row spinner on `duplicatingId`/`deletingId`. (Depends on T003, T012, T013.)
- [ ] T015 [P] `src/crd/components/templates/preview/CalloutTemplatePreview.tsx` — renders the `{type:'callout'}` branch for **every framing kind**; **reuse the CRD callout-preview components** (`CalloutWhiteboardPreview`, `CalloutMemoPreview`, `CalloutCollaboraPreview` — read-only title/placeholder for the Collabora-document kind, no live document service, `CalloutMediaGalleryCarousel`, `CalloutLinkAction`) for the framing content; the **poll** framing renders its question + options read-only (reuse the CRD poll-preview component if one exists, else a simple read-only list); framing-kind badge, framing title + description (`MarkdownContent`), allowed-contribution chips, comments-enabled indicator, default post description (`MarkdownContent`), default whiteboard (read-only CRD whiteboard viewer).
- [ ] T016 [P] `src/crd/components/templates/preview/WhiteboardTemplatePreview.tsx` — `{type:'whiteboard'}`: the drawing in the read-only CRD whiteboard viewer; preview-image fallback when content is empty.
- [ ] T017 [P] `src/crd/components/templates/preview/PostTemplatePreview.tsx` — `{type:'post'}`: default description via `MarkdownContent`.
- [ ] T018 [P] `src/crd/components/templates/preview/SpaceTemplatePreview.tsx` — `{type:'space'}`: innovation-flow phase chips (reuse the CRD flow-phase chip if one exists), starter-callout list (name + framing-kind icon), nested subspace-template names.
- [ ] T019 [P] `src/crd/components/templates/preview/CommunityGuidelinesTemplatePreview.tsx` — `{type:'communityGuidelines'}`: the guidelines **title** (heading) + guidelines markdown body (`MarkdownContent`) + references list.
- [ ] T020 `src/crd/components/templates/TemplateContentPreview.tsx` — dispatcher on `content.type` → the matching `preview/*` component; `loading` → skeleton (`role="status"` + `aria-label`); wraps content in a `ScrollArea`. (Depends on T015–T019.)
- [ ] T021 `src/crd/components/templates/TemplatePreviewDialog.tsx` — move/evolve `src/crd/components/space/settings/TemplatePreviewDialog.tsx` to `TemplatePreviewDialogProps`: header from `TemplateCardData` (before the heavy content loads), lazy `content`, an "Edit" affordance shown only when `onEdit` is provided. (Depends on T020.)

### The reusable picker

- [ ] T022 `src/crd/components/templates/TemplatePicker.tsx` (+ `TemplatePickerCard.tsx`) per `contracts/template-picker.ts`. Discriminated `mode`: `'import'` = persistent library manager (gallery, click→preview pane, "Import" adds a copy and the dialog **stays open**, templates in `alreadyInSet` marked "in this set" + removable, admin closes when done) — **Space-holder only; source sections = Account + Platform only (the Space's own set is not a source for importing into itself — matches the legacy `disableSpaceTemplates: true`)**; `'select'` = single-pick-then-apply (filter to `allowedTypes`, click→preview, confirm closes, persistent "No template / start blank"; **Space source section shown when there is a Space context**). Both share: a **free-text search filter** over name/description/tags (mirroring the legacy `ImportTemplatesDialogGallery`), the source sections (each omitted when empty; "no templates available" when all empty), the lazy `onPreview` → `previewContent` pane (uses `TemplateContentPreview`), `TemplatePickerCard`. **Supersedes `src/crd/components/space/settings/TemplateLibraryDialog.tsx`.** (Depends on T012, T020.)
- [ ] T023 `src/main/crdPages/templates/useTemplatePicker.ts` — the `mode:'select'` picker hook for all consumption flows: loads the source lists by reusing `useImportTemplateDialogQuery` (Space) / `useImportTemplateDialogAccountTemplatesQuery` (Account) / `useImportTemplateDialogPlatformTemplatesQuery` (Platform), filtered to `allowedTypes`; `accountId` = the owning Space's account (`useSpace().space.accountId` / `SubspaceContext` at L1/L2); supports the picker's free-text search; lazy content for preview via `useTemplateContentLazyQuery` → `templateContentMapper`; exposes `openPicker()`, `pickerProps: TemplatePickerSelectProps`, `selectedTemplateId`, `selectedTemplateContent`, `clearSelection()`. (Depends on T008, T010.)
- [ ] T024 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplatePicker.test.ts` — source loading + `allowedTypes` filtering + empty-source handling. (Depends on T023.)

### Per-type create/edit forms + the form dialog

- [ ] T025 [P] **`src/main/crdPages/templates/CalloutTemplateForm.tsx`** — note the location: this is the **integration layer**, NOT `src/crd/` (the CRD layer rules forbid Apollo/`@/domain/*`/MUI imports, and this composes the callout-authoring connectors which import all three). A **thin shell over the existing CRD callout-authoring connectors**, not a bespoke editor: a framing-kind picker (the `FramingChip` union) → the matching CRD framing connector (`WhiteboardFramingConnector` / `MemoFramingConnector` / `CollaboraFramingConnector` (+`CollaboraFramingEditorOverlay`) / `MediaGalleryFramingConnector` / `CallToActionFramingConnector` / the poll framing connector if one exists, else a minimal poll editor / plain text), the contribution-type/comment-settings **controls** (the control components used inside `CalloutSettingsConnector` — import the controls, **not** the whole `CalloutSettingsConnector` module, which carries the legacy-MUI `CreateTemplateDialog` bridge), and contribution defaults via `ResponseDefaultsConnector`/`ResponseDefaultsDialog`. Covers **every** framing kind the live callout editor supports, incl. **Collabora-document** and **poll** (poll content persists per V2). Controlled (`value`/`onChange`/`errors`); form state lives in `useTemplateForms`, which passes this component into `TemplateFormDialog` via `perTypeFormSlot`. (Depends on T003; reuses `src/main/crdPages/space/callout/*` connectors + `calloutFormMapper.ts`. Note: `T036` removes the MUI bridge from `CalloutSettingsConnector` in this same Phase 2, so even if the whole connector is transitively reached, no MUI is pulled in.)
- [ ] T026 [P] `src/crd/components/templates/forms/WhiteboardTemplateForm.tsx` — wraps `WhiteboardEditorShell` (the drawing) **plus the full preview-settings UI — `PreviewSettingsDialog` / `PreviewCropDialog` (crop/region), not just an image upload** (FR-020 — "the whiteboard drawing and its preview settings") — and a preview-image upload. Presentational (CRD-layer only — `WhiteboardEditorShell` / `PreviewSettingsDialog` / `PreviewCropDialog` live in `src/crd/components/whiteboard/` and are pure; if `PreviewSettingsDialog` turns out Apollo-bound, its wiring moves to `crdPages/templates/`, or fall back to image-only). Controlled (`value`/`onChange`/`errors`; `value.previewSettings` + `value.previewImageFile`).
- [ ] T027 [P] `src/crd/components/templates/forms/PostTemplateForm.tsx` — wraps `MarkdownEditor` for the default description. Presentational. Controlled.
- [ ] T028 [P] `src/crd/components/templates/forms/SpaceTemplateForm.tsx` — **presentational**: a space-source picker UI that takes search results + `onSearchChange` + `onSelectSource` as props (the Apollo space-search query lives in `crdPages/templates/` — `useTemplateForms` or a tiny `SpaceSourcePicker` connector — NOT here) + "include nested subspaces" toggle + a read-only captured-structure preview (reuse `SpaceTemplatePreview` once content is fetched). Re-selectable on edit. Controlled. (Depends on T018.)
- [ ] T029 [P] `src/crd/components/templates/forms/CommunityGuidelinesTemplateForm.tsx` — a guidelines **title** field [required] + `MarkdownEditor` for the guidelines body + a references-list editor (name/uri/description rows; add/remove). Controlled. (The same title + references-list editor sub-components are reused by the `CommunityGuidelinesEditor` host — T057, FR-038.)
- [ ] T030 `src/crd/components/templates/TemplateFormDialog.tsx` — **the CRD-native create/edit dialog that replaces both the legacy MUI `CreateTemplateDialog` bridge AND `src/crd/components/space/settings/TemplateEditDialog.tsx` (which only did profile+tags)**. Pure `src/crd/` shell — **does NOT import any per-type form**: title from `intent` + `type`; common fields (name [required], description, tags via `tags-input`, banner upload); a **`perTypeFormSlot: ReactNode`** prop into which the consumer (the integration layer / `useTemplateForms`) injects the right per-type form — the ShareDialog `shareOnAlkemioSlot` pattern; Save (disabled + `aria-busy` while `submitting`) / Cancel; closing with unsaved changes → `DiscardChangesDialog` (`ConfirmationDialog` variant). Also the surface a "save X as a template" flow opens, pre-filled. (Depends on T003. Note: it does NOT depend on T025/T026–T029 — those are injected via `perTypeFormSlot` by the integration layer.)
- [ ] T031 `src/main/crdPages/templates/useTemplateForms.ts` — per-type form state + validation + the create/edit mutations (`useCreateTemplateMutation` / `useCreateTemplateFromSpaceMutation` / `useCreateTemplateFromContentSpaceMutation` / `useUpdateTemplateMutation` / `useUpdateCalloutTemplateMutation` / `useUpdateTemplateFromSpaceMutation` / `useUpdateCommunityGuidelinesMutation` / `useUpdateWhiteboardMutation`); reuse the MUI-free `src/domain/templates/components/Forms/common/mappings.ts` for form→input mapping (confirm it has no MUI/Apollo imports); the Apollo **space-search query** for the Space form (results piped into the presentational `SpaceTemplateForm`); image uploads via `useHandlePreviewImages` + `useUploadMediaGalleryVisuals`; mutations in `useTransition`; exposes the `form` sub-state shape from `contracts/data-mapper.ts` (incl. the WB branch's `previewSettings` and the CG branch's required `title`), **assembles the `perTypeFormSlot` per `type`** (the pure `src/crd/.../forms/{Whiteboard,Post,CommunityGuidelines}TemplateForm` + the presentational `SpaceTemplateForm` for those types; `crdPages/templates/CalloutTemplateForm` for callout) and hands it to `TemplateFormDialog`, **plus** a helper to produce pre-filled `value` from a callout (every framing kind incl. Collabora-document + poll) / the current guidelines (title + body + references) / a subspace (for save-as). (Depends on T010, T025, T026–T029.)
- [ ] T032 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplateForms.test.ts` — the Callout-template form↔mutation-input mapping (callout-with-each-framing-kind, incl. Collabora-document and poll) round-trips correctly; the CG-template form↔input mapping carries the `title`. (Depends on T031.)

### Set-default & save-as-template dialogs

- [ ] T033 `src/crd/components/templates/SetDefaultTemplateDialog.tsx` — generalise `src/crd/components/space/settings/ChangeDefaultSubspaceTemplateDialog.tsx` to `SetDefaultTemplateDialogProps`: `purpose: 'defaultSubspaceTemplate' | 'flowStateDefaultCalloutTemplate'`, current default shown selected + a clear option, `onConfirm(templateId | null)`. **The two purposes have different source scopes (matching legacy)**: for `defaultSubspaceTemplate` the candidates are the holder's **own Space templates only** (the default must reference a template that is in the set — the legacy `SelectDefaultSpaceTemplateDialog` is a plain dropdown of those; CRD adds a candidate preview) — do **not** host the 3-source picker here; for `flowStateDefaultCalloutTemplate` host `TemplatePicker` in `mode:'select'` with the full Space/Account/Platform sources (the legacy innovation-flow `SetDefaultTemplateDialog` uses `ImportTemplatesDialog` with `selectedTemplateId` + `onRemoveTemplate`). **Remove `ChangeDefaultSubspaceTemplateDialog.tsx`.** (Depends on T022.)
- [ ] T034 `src/main/crdPages/templates/useSetDefaultTemplate.ts` — wraps `useUpdateTemplateDefaultMutation` (Space per-type defaults) and `useSetDefaultCalloutTemplateOnInnovationFlowStateMutation` / `useRemoveDefaultCalloutTemplateOnInnovationFlowStateMutation` (flow-state default); exposes confirm + in-flight state. (Depends on T023.)
- [ ] T035 `src/crd/components/templates/SaveAsTemplateDialog.tsx` — = `TemplateFormDialog` opened in `intent:'create'` with `value` **pre-filled** from a source entity: callout → Callout form (no-regression — replaces the CRD→MUI bridge), current community guidelines → Community Guidelines form (a deliberate **new** flow — legacy only *applies* a guidelines template), subspace → Space form with `sourceSpaceId` = the subspace (no-regression). Generalised from `src/crd/components/space/settings/SaveSubspaceAsTemplateDialog.tsx`; **remove `SaveSubspaceAsTemplateDialog.tsx`** once consumers are re-pointed. (Depends on T030.)
- [ ] T036 `src/main/crdPages/templates/useSaveAsTemplate.ts` + **the `CalloutSettingsConnector` MUI-bridge removal (re-sequenced here, into Phase 2 — was T069/Phase 9)**. `useSaveAsTemplate`: builds the pre-filled `value` for each source kind; if the source has unsaved changes, gates with a `ConfirmationDialog` ("save or discard first"); routes the submit via `useTemplateForms`' create path (`useCreateCalloutTemplate` for callouts, `createTemplate` w/ `communityGuidelinesData` for guidelines, `createTemplateFromSpace`/`…FromContentSpace` for subspaces); refetches the destination set. **Then, immediately**, in `src/main/crdPages/space/callout/CalloutSettingsConnector.tsx`: drop `import CreateTemplateDialog from '@/domain/templates/.../CreateEditTemplateDialog/CreateTemplateDialog'` and the `CRD_SAVE_AS_TEMPLATE_ENABLED` kill-switch; re-point its "Save as template" menu item to `SaveAsTemplateDialog` (`= TemplateFormDialog` pre-filled, `type:'callout'`) via `useSaveAsTemplate({ sourceKind:'callout', callout })`; keep the privilege gate (`perms.showSaveAsTemplate`). Doing this in Phase 2 removes the legacy-MUI bridge from the CRD callout layer **before** any CRD callout-template work reaches `CalloutSettingsConnector`, so FR-002 / SC-004 hold throughout. (Depends on T031, T035.)

### The holder-agnostic integration hook

- [ ] T037 `src/main/crdPages/templates/useTemplatesManager.ts` — generalise `src/main/crdPages/topLevelPages/spaceSettings/templates/useTemplatesTabData.ts` to the `UseTemplatesManager` shape (`contracts/data-mapper.ts`); **absorb `useTemplateActions.ts` + `useTemplateLibrary.ts`**. Takes `{ templatesSetId, holderKind, accountId? }`; runs `useAllTemplatesInTemplatesSetQuery` → `templatesManagerMapper`; owns: preview (lazy `useTemplateContentLazyQuery` → `templateContentMapper`), the form lifecycle (delegating to `useTemplateForms` — **closes the existing `onCreateTemplate` TODO**, so Create works natively), duplicate, delete (`useDeleteTemplateMutation` + `ConfirmationDialog`-driven `pendingDelete` + optimistic cache eviction + `isUsedAsDefault` flag from the holder's `templateDefaults`), and the `mode:'import'` picker (Space holder only — reuses the source loaders + `useCreateTemplateMutation`/`...FromContentSpace` to import + a `ConfirmationDialog`-gated remove-from-set). Mutations in `useTransition`. (Depends on T009, T010, T021, T022, T030, T031.)
- [ ] T038 [P] Unit test `src/main/crdPages/templates/__tests__/useTemplatesManager.test.ts` — the delete confirmation path + the post-delete default-clear (FR-019, T034 hook). (Depends on T037, T034.)

**Checkpoint**: open verifications done (any GraphQL change committed); the complete CRD templates kit exists and is unit-tested; **`TemplateFormDialog` is a pure `src/crd/` shell (per-type forms injected via `perTypeFormSlot`); `CalloutTemplateForm` lives in `crdPages/templates/` (integration layer); and the legacy-MUI `CreateTemplateDialog` bridge in `CalloutSettingsConnector` has been removed (T036) — so no CRD templates code path imports MUI, FR-002 / SC-004 hold from here on.** US1, US2, US3 (the P1 set / MVP) can now run in parallel; so can US4–US9.

---

## Phase 3: User Story 1 — Manage a Space's templates (Priority: P1) 🎯 MVP

**Goal**: a Space admin opens Space Settings → Templates and can preview / create / edit / duplicate / delete / import every template type (no "custom only" restriction), and set the default subspace template — all native CRD.

**Independent Test**: toggle CRD on, open a space's Settings → Templates as admin; create a template of each type, edit it (incl. its content, not just profile+tags), duplicate it, preview it, delete it (confirm); import one from the library; set the default subspace template; verify each action persists and the section list updates; verify an imported template is editable/deletable like any other.

- [ ] T039 [US1] Refactor `src/main/crdPages/topLevelPages/spaceSettings/templates/useTemplatesTabData.ts` to a thin adapter over `useTemplatesManager({ holderKind:'space', templatesSetId, accountId })` (resolve `templatesSetId` via `useSpaceTemplatesManagerQuery`) + `useSetDefaultTemplate` (default subspace template, current value from `templatesManager.templateDefaults`) + expose `useSaveAsTemplate` for the Subspaces tab. **Preserve the legacy template deep-link** (`<templates-base-url>/<templateId>` — the URL still resolves; in CRD it opens that template's **read-only Preview dialog**, with an Edit affordance when authorised — FR-007 / FR-013 / Clarifications D1). Delete `useTemplateActions.ts` and `useTemplateLibrary.ts` once nothing imports them (or leave temporary re-exports, removed in T090). (Depends on T037, T034, T036.)
- [ ] T040 [US1] Make `src/crd/components/space/settings/SpaceSettingsTemplatesView.tsx` a ≤10-line wrapper rendering `<TemplatesManagerView holderKind="space" .../>` from T039's hook; wire `canCreate/canEdit/canDelete` as uniform `() => true` and `canImport` as `() => true` (the legacy `SpaceAdminTemplatesPage` hard-codes all-five-types creatable/editable/deletable/importable — page access is the gate, FR-014; **drop the `isCustom` gate**); render the default-subspace-template card (existing `045` placement) using `SetDefaultTemplateDialog` (`purpose:'defaultSubspaceTemplate'` — Space's own Space templates only). Update `045`'s Templates-tab consumer if its hook shape changed. (Depends on T014, T033, T039.)
- [ ] T041 [US1] Remove the superseded `045`-era CRD dialogs and re-point imports: `TemplateLibraryDialog.tsx` → `templates/TemplatePicker.tsx` (`mode:'import'`); `TemplateEditDialog.tsx` → `templates/TemplateFormDialog.tsx`; `SaveSubspaceAsTemplateDialog.tsx` → `templates/SaveAsTemplateDialog.tsx`; `ChangeDefaultSubspaceTemplateDialog.tsx` → `templates/SetDefaultTemplateDialog.tsx`; move `TemplatePreviewDialog.tsx` → `templates/`. Update every importer (CRD components + `crdPages` hooks). (Depends on T040.)
- [ ] T042 [US1] Implement the FR-019 delete behaviour: when `pendingDelete.isUsedAsDefault`, the `ConfirmationDialog` copy notes "this template is currently used as a default and will be unset"; after a successful delete, clear every default that referenced it (`updateTemplateDefault(template:null)` and/or `removeDefaultCalloutTemplateOnInnovationFlowState`) — or rely on the backend cascade per T006/V3's outcome. (Depends on T037, T034.)
- [ ] T043 [US1] Add the `crd-templates` i18n keys for the Space Settings → Templates surface (per-type section titles/subtitles, kebab actions, create/import buttons, the form-dialog titles/labels/validation, the preview dialog, the picker labels, the set-default and save-as dialogs, the delete-confirmation copy incl. the "used as default" note) to `templates.en.json` + the 5 other-language files (AI-assisted nl/es/bg/de/fr).

**Checkpoint**: Space Settings → Templates is fully native CRD; the `045`-era partial implementation (incl. the MUI create-dialog bridge and the "custom only" gate) is gone.

---

## Phase 4: User Story 2 — Manage an Innovation Pack's templates (unified holder experience) (Priority: P1)

**Goal**: managing a pack's templates uses the same surface/cards/dialogs as US1 — Preview / Create / Edit / Duplicate / Delete — with no Import affordance (Space-only) and preview-before-edit, gated only by authorisation.

**Independent Test**: toggle CRD on, open an Innovation Pack's admin screen (`<pack.profile.url>/settings`); verify the templates area is visually/behaviourally identical to the Space Settings → Templates tab except no "Library/Import" button; clicking a template opens a Preview first (not Edit); create/edit/duplicate/delete work through the same dialogs; actions hidden when the user lacks the privilege.

- [ ] T044 [US2] `src/main/crdPages/innovationPack/innovationPackMapper.ts` — minimal mappers: GraphQL `InnovationPack` → `{ id, templatesSetId, profile basics, canManage flags }` (extended in US7). (Depends on T008.)
- [ ] T045 [US2] `src/main/crdPages/innovationPack/useInnovationPackAdmin.ts` — resolve the pack id (from the route — `<pack.profile.url>/settings`), run `useAdminInnovationPackQuery`, derive `templatesSetId`, call `useTemplatesManager({ holderKind:'innovationPack', templatesSetId })`; surface the templates-manager props (pack-details form comes in US7; **there is no delete-pack here** — pack delete lives in the Account-tab pack-card menu, FR-042). **Preserve the legacy template deep-link** (`<pack.profile.url>/settings/<templateId>` — the URL still resolves; in CRD it opens that template's **read-only Preview dialog** (Edit affordance when authorised) — **not** the legacy "pack deep-link → Edit" behaviour, which is intentionally dropped — FR-007 / FR-013 / Clarifications D1). (Depends on T037, T044.)
- [ ] T046 [US2] `src/main/crdPages/innovationPack/CrdInnovationPackAdminPage.tsx` — page shell (CRD layout) hosting `<TemplatesManagerView holderKind="innovationPack" .../>` + the shared preview / form / delete-confirm dialogs from `useInnovationPackAdmin`; `canCreate/canEdit/canDelete` as uniform `() => true` (page access is the gate — the legacy `AdminInnovationPackPage` hard-codes all-five-types; no holder-kind gating, FR-014), `canImport` as `() => false` (Import is Space-only). (Depends on T014, T045.)
- [ ] T047 [US2] Toggle-gate the pack-admin route in **`src/domain/InnovationPack/InnovationPackRoute.tsx`** — the file that routes `<pack.profile.url>/settings` → `AdminInnovationPackPage` (and `…/settings/:templateNameId`), mounted from `TopLevelRoutes.tsx`; **NOT `AdminInnovationPackRoutes.tsx`** (that hosts only `/admin/innovation-packs`, which is not migrated by this feature). Render `CrdInnovationPackAdminPage` when `useCrdEnabled()` is true, else the legacy `AdminInnovationPackPage` — lazy, only one chunk fetched; wrap the CRD page in `CrdLayoutWrapper` if the admin shell doesn't. (This task and T080 both edit `InnovationPackRoute.tsx`.)
- [ ] T048 [US2] Add the `crd-templates` i18n keys specific to the pack templates surface (any holder-kind-specific copy) to all 6 language files.

**Checkpoint**: a pack admin manages templates through the identical CRD surface; the two holders are unified (Import-Space-only being the sole intentional difference).

---

## Phase 5: User Story 3 — Use a template when creating a Callout (Priority: P1)

**Goal**: from the CRD callout-creation surface, "use a template" opens the CRD picker (Callout templates, Space/Account/Platform sources, preview before selection); selecting pre-fills the creation form (incl. Collabora-document framing); clearing returns it to blank.

**Independent Test**: toggle CRD on, open the CRD callout-creation dialog in a space, click "use a template", verify the picker shows Space/Account/Platform sections (each hidden when empty; "no templates" when all empty), preview a callout template, select it, confirm the form is pre-filled (framing kind — incl. Collabora-document — + content, allowed contributions, comment settings, default post description, default whiteboard); clear the selection and confirm the form returns to blank.

- [ ] T049 [US3] In `src/main/crdPages/space/callout/TemplateImportConnector.tsx` (and the CRD callout-creation form it feeds), replace the MUI `ImportTemplatesDialog` wiring with `useTemplatePicker({ allowedTypes:['callout'], accountId: useSpace().space.accountId })` rendering `<TemplatePicker mode="select" {...pickerProps} />`; on select, take `selectedTemplateContent` (the `{type:'callout'}` branch) and pre-fill the callout-creation form (mapping via the existing `calloutFormMapper`-style logic, incl. the Collabora-document framing kind); on `clearSelection()`, reset to an empty callout. Keep the existing overwrite-confirm-when-dirty behaviour. (Depends on T022, T023.)
- [ ] T050 [US3] Add the `crd-templates` i18n keys for the callout-creation picker entry point ("Use a template", "No template / start blank"; source-section labels reused from the shared set) to all 6 language files.
- [ ] T051 [P] [US3] Integration test `src/main/crdPages/space/callout/__tests__/templateImport.test.ts` — selecting a callout template (for each framing kind incl. Collabora) produces the expected pre-filled form values. (Depends on T049.)

**Checkpoint**: MVP (US1 + US3) complete — a Space admin manages templates natively; a creator uses callout templates via the CRD picker.

---

## Phase 6: User Story 4 — Use a template when creating a Space or Subspace (Priority: P2)

**Goal**: the CRD create-subspace flow offers a Space-template selector (incl. "Empty"), preview of phases & starter callouts, pre-selects the parent's default subspace template, confirms over unsaved data; the top-level create-Space flow keeps the legacy picker for now (FR-037); and a Space admin can save a subspace as a Space template.

**Independent Test**: toggle CRD on; run the CRD create-subspace flow → confirm a Space-template selector with an "Empty" option + a preview of phases/callouts, the parent's default subspace template pre-selected, an overwrite-confirm when the form has data, and that creation from a template (or "Empty") works; from a subspace's overflow menu choose "Save as template" → confirm a new Space template is created. (Top-level create-Space: confirm it still uses the legacy picker — documented FR-037 fallback.)

- [ ] T052 [US4] In `src/crd/components/space/settings/CreateSubspaceDialog.tsx` (+ `src/main/crdPages/topLevelPages/spaceSettings/subspaces/useCreateSubspace.ts`), add a Space-template selector via `useTemplatePicker({ allowedTypes:['space'] })` with an explicit "Empty / from scratch" option; show a preview of the selected template's phases + starter callouts (+ nested subspace templates) using `SpaceTemplatePreview`; pre-select the parent space's configured default subspace template; show a `ConfirmationDialog` before overwriting when the form already holds user data; create the subspace from the selected template (or empty) via the existing create-subspace mutation. (Depends on T022, T023, T018.)
- [ ] T053 [US4] Re-point "Save as template" in the Subspaces tab — `src/main/crdPages/topLevelPages/spaceSettings/subspaces/useSaveSubspaceAsTemplate.ts` and the subspace overflow-menu wiring — to `SaveAsTemplateDialog` (the subspace case → `SpaceTemplateForm` pre-filled with `sourceSpaceId`) + `useSaveAsTemplate`. The `045` integration plumbing stays; this swaps the dialog. (Depends on T035, T036.)
- [ ] T054 [US4] Document the top-level create-Space flow as an FR-037 transitional fallback (it keeps the legacy MUI picker until that wizard migrates to CRD); add it to the SC-002 parity-checklist "host not yet CRD" list (T087). No code change beyond a note in `quickstart.md` / the PR description.
- [ ] T055 [US4] Add the `crd-templates` i18n keys for the subspace-creation selector ("Empty / start from scratch", overwrite-confirm copy) and the save-subspace-as-template flow to all 6 language files.

**Checkpoint**: subspace creation from a template works in CRD with defaults + the overwrite guard + save-as-template; top-level Space creation is a documented legacy fallback.

---

## Phase 7: User Story 5 — Use a template in the remaining creation flows (Priority: P2)

**Goal**: whiteboard creation, community-guidelines applying, the innovation-flow-state default callout template, and callout contribution-defaults (post + whiteboard) all use the CRD kit; **the CRD community-guidelines editor host is delivered here to legacy parity (title + references + body, FR-038)** as the home for the guidelines apply / save-as flows; the guidelines replacement is confirmation-guarded; the legacy "Default post template" label is corrected.

**Independent Test**: toggle CRD on; add a whiteboard from a template; open the CRD community-guidelines editor (title + references + body, working — not the old markdown-only stub) and apply a community-guidelines template (confirm-guarded replacement of title + body + references); set/clear a flow-state's default callout template (labelled "Default callout template"); set a callout's default post template (and default whiteboard) — each via the CRD picker/dialog, preview a candidate, apply it, confirm the target reflects the template.

- [ ] T056 [US5] Whiteboard creation: in the CRD whiteboard-add flow (inside a callout / standalone — `src/main/crdPages/whiteboard/CrdWhiteboardDialog.tsx` / `CrdSingleUserWhiteboardDialog.tsx`), wire `useTemplatePicker({ allowedTypes:['whiteboard'] })`; on select, open the new whiteboard from the template's drawing; `clearSelection()` → blank. (Depends on T022, T023.)
- [ ] T057 [US5] **Deliver the CRD community-guidelines editor host (FR-038) + wire the apply-template flow**: (a) build `src/crd/components/space/settings/CommunityGuidelinesEditor.tsx` (presentational, per `data-model.md` §B `CommunityGuidelinesEditor*` — a guidelines **title** field + `MarkdownEditor` body + references-list editor, reusing the title/references sub-components from T029; `value`/`onChange`/`errors`/`onSave`/`saving` + `onApplyTemplate`/`onSaveAsTemplate`/`can*` callbacks) and its integration hook in `src/main/crdPages/topLevelPages/spaceSettings/community/` (`useUpdateCommunityGuidelinesMutation` in `useTransition`; maps the live `CommunityGuidelines.profile.{displayName,description,references}` ⇄ `CommunityGuidelinesEditorValue`) — **superseding `045`'s markdown-only stub** (it has no title / references; replace whatever it wired); (b) wire `useTemplatePicker({ allowedTypes:['communityGuidelines'] })` into the editor — on select, replace `{ title, bodyMarkdown, references }` with the template's behind a `ConfirmationDialog` when there is existing content — porting `src/domain/community/community/CommunityGuidelines/useCommunityGuidelines.ts` `onSelectCommunityGuidelinesTemplate` logic into the CRD integration layer (no MUI). This makes the guidelines flow **native CRD**, not an FR-037 fallback. (Depends on T010, T021, T022, T023, T029; coordinates with `045`'s Community tab — re-points it.)
- [ ] T058 [US5] Innovation-flow-state default callout template: in `src/main/crdPages/topLevelPages/spaceSettings/layout/useColumnMenu.ts` (+ the Layout-tab per-column overflow menu), open `SetDefaultTemplateDialog` (`purpose:'flowStateDefaultCalloutTemplate'`, `allowedType:'callout'`) via `useSetDefaultTemplate`; support clearing it; **rename the menu entry / labels from "Default post template" → "Default callout template"** (it is a Callout template). The `useSetDefaultCalloutTemplateOnInnovationFlowStateMutation` wiring already exists in `useColumnMenu` — this swaps the picker UI + fixes the label. (Depends on T033, T034.)
- [ ] T059 [US5] Post default description: refactor `src/main/crdPages/space/callout/ResponseDefaultsConnector.tsx` to use `useTemplatePicker({ allowedTypes:['post'] })` (and `['whiteboard']` for the default whiteboard) instead of its bespoke `useSpaceContentTemplatesOnSpaceQuery`; on select, set the callout's default post description / default whiteboard from the template. (Depends on T022, T023.)
- [ ] T060 [US5] Add the `crd-templates` i18n keys for these surfaces (the CRD community-guidelines editor — title field label, body label, references-list labels, "Apply template" / "Save as template" actions; the guidelines-replacement confirmation copy; the "Default callout template" labels; the whiteboard/post picker entry points) to all 6 language files.

**Checkpoint**: every template-consumption flow whose host is already CRD uses the CRD kit; hosts not yet CRD keep the legacy picker (FR-037) — list them in the PR (T087).

---

## Phase 8: User Story 6 — Browse the Innovation Library (Priority: P2)

**Goal**: `/innovation-library` rebuilt in CRD — an Innovation Packs section + a type-filterable gallery of all platform templates (+ free-text search if the legacy page has it — T007/V4), with preview — viewable signed-out.

**Independent Test**: toggle CRD on, open `/innovation-library` signed out; confirm an Innovation Packs section + a templates gallery; filter by type (and search, if present); preview a template; click a pack → navigates to its public profile; confirm an empty state (not an error) when there are no listed packs/templates.

- [ ] T061 [P] [US6] `src/crd/components/innovationLibrary/TemplateTypeFilter.tsx` — type-filter chips/segmented control with optional per-type counts; desktop + mobile variants (matching the prototype's `TemplateTypeFilter` / `TemplateTypeFilterMobile`).
- [ ] T062 [P] [US6] `src/crd/components/innovationLibrary/TemplateGallery.tsx` — a responsive grid of `TemplateCard` (already type-filtered by the consumer), loading skeletons, `emptyLabel` empty state, `onPreview` per card; (a search input if T007/V4 says the legacy page has one).
- [ ] T063 [P] [US6] `src/crd/components/innovationPack/InnovationPackCard.tsx` — pack card per `InnovationPackCardData`: banner (real or `color` gradient), provider avatar+name, name, description, tags, template count, links to `url`. (Used by US6 and US8.)
- [ ] T064 [US6] `src/crd/components/innovationLibrary/InnovationLibraryView.tsx` — an Innovation Packs section (grid of `InnovationPackCard`) + the `TemplateGallery` with `TemplateTypeFilter` + a preview surface (reuse `TemplatePreviewDialog`/`TemplateContentPreview`). From `prototype/src/app/components/template-library/TemplateLibrary.tsx`. (Depends on T061, T062, T063, T021.)
- [ ] T065 [US6] `src/main/crdPages/innovationLibrary/innovationLibraryMapper.ts` + `useInnovationLibrary.ts` — `useInnovationLibraryQuery` (`platform.library.templates` + `.innovationPacks`); map to `InnovationLibraryViewProps.{packs,templates}` (`ownerLabel` = owning pack displayName; `color` via `pickColorFromId`); client-side type filter (mirror legacy single-fetch + filter); lazy preview content via `useTemplateContentLazyQuery` + `templateContentMapper`. (Depends on T008, T010, T044.)
- [ ] T066 [US6] `src/main/crdPages/innovationLibrary/CrdInnovationLibraryPage.tsx` — route entry composing `useInnovationLibrary` + `InnovationLibraryView` + the preview dialog. (Depends on T064, T065.)
- [ ] T067 [US6] Toggle-gate `/innovation-library` in `src/main/routing/TopLevelRoutes.tsx`: add `CrdInnovationLibraryPage` (lazy, wrapped in `CrdLayoutWrapper`) vs. the existing `MuiInnovationLibraryPage` (`src/main/topLevelPages/InnovationLibraryPage/`), conditional on `crdEnabled`; ensure anonymous access (route not behind an identity gate).
- [ ] T068 [US6] Add the `crd-templates` i18n keys for the Library page (section titles, filter labels, gallery empty state) to all 6 language files.

**Checkpoint**: the Innovation Library is browsable in CRD, signed-out, with type filtering and preview.

---

## Phase 9: User Story 9 — Save a callout / the current community guidelines as a template (Priority: P2)

**Goal**: from a callout's settings menu and from the community-guidelines editor, "Save as a template" opens the matching Create Template dialog **pre-filled** from that entity; on confirm a new template is added to the Space's set; the CRD callout-settings → legacy-MUI-`CreateTemplateDialog` bridge is gone. (The subspace case is in US4 / T053.)

**Independent Test**: toggle CRD on. From a callout's settings menu choose "Save as a template" → confirm the Create Callout-template dialog opens with the callout's framing kind + content (incl. Collabora-document), settings and contribution defaults pre-filled; adjust the name; save → confirm a new Callout template appears in the Space's set and is editable like any other. From the community-guidelines editor choose "Save as a template" → confirm the Create Community-Guidelines-template dialog opens pre-filled from the current guidelines; save → confirm a new Community Guidelines template appears. Confirm the "Save as a template" affordance is hidden when the user can't create that template type. (The callout-settings → legacy-MUI bridge removal already happened in **T036 (Phase 2)** — verify it stayed gone.)

- [ ] T069 [US9] **The callout "save as template" path is already wired & de-bridged in T036 (Phase 2)** — this slot now just *verifies* it end-to-end (the bridge import + the `CRD_SAVE_AS_TEMPLATE_ENABLED` kill-switch are gone from `CalloutSettingsConnector.tsx`; the menu item opens the CRD `SaveAsTemplateDialog` pre-filled from the callout via `useSaveAsTemplate`; the privilege gate is intact) as part of the US9 acceptance run. No new code expected here unless T036 left a follow-up.
- [ ] T070 [US9] Wire the "Save as a template" action into the CRD community-guidelines editor (the one this feature delivers — T057 / FR-038, **not** `045`'s stub): the editor's `onSaveAsTemplate` opens `SaveAsTemplateDialog` (`type:'communityGuidelines'`, pre-filled from the current guidelines **title + body + references**) via `useSaveAsTemplate({ sourceKind:'communityGuidelines', guidelines })`; if the editor has unsaved edits, prompt to save/discard first; gate on the create-CommunityGuidelines-template privilege. Replace whatever `045`'s Community-tab guidelines card stubbed for "Save-as-Template". (Depends on T035, T036, T057.)
- [ ] T071 [US9] Add the `crd-templates` i18n keys for the save-as-a-template flows ("Save as a template", the "save or discard first" prompt copy) to all 6 language files.

**Checkpoint**: all three "save as a template" flows (callout, community guidelines, subspace) work natively in CRD; no template dialog in the CRD layer is a MUI bridge.

---

## Phase 10: User Story 7 — Create and edit an Innovation Pack (Priority: P3)

**Goal**: an authorised user creates a new Innovation Pack from a contributor's Account tab by giving it a **name + description** (mirroring the legacy create dialog), lands on the pack admin screen where they fill in / edit the rest (avatar / tags / references / store-listing / search-visibility — **provider shown read-only**), and deletes a pack from the **three-dot menu on its card in the Account-tab packs list** (confirmed).

**Independent Test**: toggle CRD on; from the user (or organization) profile's Account tab, create a new Innovation Pack with a name + description → confirm it appears and the pack admin screen opens; on the admin screen fill in avatar / tags / references / store-listing / search-visibility, confirm the provider is read-only, save → confirm they persist; from the pack card's three-dot menu in the Account tab choose "Delete", confirm the dialog → confirm it's removed; confirm a non-authorised user sees no create/edit/delete affordances.

- [ ] T072 [P] [US7] `src/crd/components/innovationPack/InnovationPackForm.tsx` — the pack **edit** form per `InnovationPackFormProps`: name [required] (= profile.displayName), description, avatar upload, tags (`tags-input`), references-list editor, `listedInStore` `Switch`, `searchVisibility` `Select`, and the **provider organisation displayed read-only** (no org picker — mirrors the legacy `InnovationPackForm`, whose provider is a disabled `TextField`); controlled; inline validation.
- [ ] T073 [P] [US7] `src/crd/components/innovationPack/CreateInnovationPackDialog.tsx` — port the existing `src/domain/InnovationPack/CreateInnovationPackDialog/CreateInnovationPackDialog.tsx` UI to shadcn: a small dialog collecting **only name + description** (the legacy create dialog sends `{accountID, profileData:{displayName, description}}`), with a Create button (`aria-busy` while `creating`).
- [ ] T074 [US7] `src/crd/components/innovationPack/InnovationPackAdminView.tsx` — per `InnovationPackAdminViewProps`: composes `InnovationPackForm` (with `providerName` read-only) + the `TemplatesManagerViewProps` block (US2's `CrdInnovationPackAdminPage` renders this view instead of `TemplatesManagerView` directly). **No "Delete pack" action here** — pack deletion is the Account-tab pack-card menu (T076), not the admin screen, matching where the legacy app exposes it. Update `CrdInnovationPackAdminPage` (T046) to render `InnovationPackAdminView`. (Depends on T046, T072.)
- [ ] T075 [US7] Extend `src/main/crdPages/innovationPack/`: `useInnovationPackAdmin.ts` gains `useUpdateInnovationPackMutation`; new `useCreateInnovationPack.ts` (`useCreateInnovationPackMutation` — input `CreateInnovationPackOnAccountInput` = `{accountID, profileData:{displayName, description}}`; `accountId` from the Account-tab context); new `useDeleteInnovationPack.ts` (`useDeleteInnovationPackMutation`, for the Account-tab pack-card menu); `innovationPackMapper.ts` extended to `InnovationPackFormValues` ⇄ `UpdateInnovationPack` input and `InnovationPackCardData`. Mutations in `useTransition`. (Depends on T044, T045.)
- [ ] T076 [US7] Wire from `src/domain/community/contributor/Account/ContributorAccountView.tsx` (the shared user/org Account tab — and its CRD equivalent if/when it exists), when `useCrdEnabled()` is true (else the legacy behaviour): (a) "Create Innovation Pack" opens the CRD `CreateInnovationPackDialog` + `useCreateInnovationPack`; on success → navigate to `<new pack url>/settings` (`CrdInnovationPackAdminPage`); (b) each pack card in the packs list gets a **three-dot menu with a "Delete"** entry → `ConfirmationDialog variant="destructive"` → `useDeleteInnovationPack` (the same place the legacy app deletes account-owned packs — `useAccountEntityDeletion.ts`). (Depends on T073, T074, T075.)
- [ ] T077 [US7] Add the `crd-templates` i18n keys for the pack edit form / create dialog (name + description) / delete confirmation / search-visibility options to all 6 language files.

**Checkpoint**: Innovation Pack create/edit/delete works in CRD; the pack admin screen shows the details form + the templates manager together.

---

## Phase 11: User Story 8 — Innovation Pack public profile page (Priority: P3)

**Goal**: an Innovation Pack's public URL (`<pack.profile.url>`) renders its profile in CRD (name/description/avatar/banner/provider/tags/references) + its templates by type with read-only preview; pack admins see an entry point to the admin screen; viewable signed-out.

**Independent Test**: toggle CRD on, open a pack's public URL signed out; confirm the pack profile + its templates by type; preview a template; as a pack admin confirm a link to `<pack.profile.url>/settings`; as a non-admin confirm it's not shown.

- [ ] T078 [US8] `src/crd/components/innovationPack/InnovationPackProfileView.tsx` — per `InnovationPackProfileViewProps`: a hero (avatar/banner via `color` fallback, name, description), a provider card (avatar/name → `providerUrl`), tags, a references list, a read-only `TemplatesManagerView` (all `can*` predicates `false`; `onTemplateAction` limited to `'preview'`), a `ShareButton`, and a "Manage this pack" entry (`adminHref` = `<pack.profile.url>/settings`) shown only when `canManage`. (Depends on T014, T063.)
- [ ] T079 [US8] `src/main/crdPages/innovationPack/useInnovationPackProfile.ts` + `CrdInnovationPackProfilePage.tsx` — `useInnovationPackProfilePageQuery` for the profile + `useTemplatesManager({ holderKind:'innovationPack', templatesSetId })` for the read-only listing + the preview dialog; resolve `canManage`/`adminHref` from the query's authorisation; compose `InnovationPackProfileView`. (Depends on T037, T078, T044.)
- [ ] T080 [US8] Toggle-gate the pack public profile route in **`src/domain/InnovationPack/InnovationPackRoute.tsx`** — the file that routes `<pack.profile.url>` → `InnovationPackProfilePage` (and `<pack.profile.url>/:templateNameId`), mounted from `TopLevelRoutes.tsx` (this is the same file T047 edits for the admin route): render `CrdInnovationPackProfilePage` (lazy, wrapped in `CrdLayoutWrapper`) vs. the legacy `InnovationPackProfilePage`, conditional on `crdEnabled`; ensure anonymous access (the route must not be behind an identity gate). The platform-admin pack *list* (`/admin/innovation-packs`) is untouched (FR-037 fallback).
- [ ] T081 [US8] Add the `crd-templates` i18n keys for the pack profile page (provider card label, references label, "Manage this pack", share) to all 6 language files.

**Checkpoint**: all nine user stories delivered in CRD behind the toggle.

---

## Phase 12: Polish & Cross-Cutting Concerns

- [ ] T082 [P] Full 6-language audit of `src/crd/i18n/templates/templates.{en,nl,es,bg,de,fr}.json`: every key used in code exists in all 6; no orphan keys; AI-assisted nl/es/bg/de/fr reviewed for placeholder/tag correctness; any `<b>`/`<br/>`-bearing key rendered via `<Trans>` not `t()` (CLAUDE.md Rule 10).
- [ ] T083 [P] Accessibility sweep (WCAG 2.1 AA, SC-007) across every new CRD templates surface — keyboard operability + visible `focus-visible` rings, `aria-label` on icon-only buttons, `aria-hidden` decorative icons, `role="status"`/`aria-label` on loading skeletons, `<ul>`/`<li>` for card grids, `aria-busy`+`disabled` on submitting buttons, 4.5:1 contrast; run the automated a11y check; keyboard-only walkthrough of create-a-template / pick-a-template / delete-a-template / save-as-a-template.
- [ ] T084 [P] **Authz-parity verification (FR-070 / FR-014)** — confirm the legacy reality and that CRD mirrors it: the templates-management surfaces (`SpaceAdminTemplatesPage`, `AdminInnovationPackPage`) gate at **page access**, not per template type — they hard-code all-five-types creatable/editable/deletable (Space adds import); `InnovationPackProfilePage` renders `TemplatesAdmin` with no predicates (read-only); so the CRD `can*` predicates must resolve uniformly `true` in a management context (`canImport` only for the Space holder) / `false` in the read-only profile, with **no per-type privilege check and no `isCustom` gate**. Then enumerate the *other* legacy gates the CRD work must preserve: set-default-subspace-template, set-flow-state-default-callout-template, save-callout-as-template (the callout-settings "save as template" permission), save-subspace-as-template (Subspaces-tab privileges), community-guidelines edit + apply-template + save-as-template (the Community-tab privileges — FR-038), pack create/edit (Account-tab pack-management privileges), pack delete (same), view-pack-admin (`InnovationPack.authorization` `Update` privilege). Record the mapping in the PR description.
- [ ] T085 [P] Verify zero `@mui/*` / `@emotion/*` in the loaded bundles for the CRD templates routes & dialogs (SC-004) — `pnpm analyze` + an import grep over `src/crd/components/{templates,innovationPack,innovationLibrary}/`, `src/crd/components/space/settings/CommunityGuidelinesEditor.tsx`, `src/main/crdPages/{templates,innovationPack,innovationLibrary}/`, and the modified callout/whiteboard/subspace/community/layout connectors (confirm the `CalloutSettingsConnector` and `045` Templates-tab MUI-dialog bridges are gone, and that the new CG editor host is MUI-free).
- [ ] T086 [P] Verify toggle-off (SC-006): with `alkemio-crd-enabled` removed, every templates surface (Space Settings → Templates, the pack admin, the pack public profile, `/innovation-library`, the create-pack entry on the Account tab, the community-guidelines editor, and every consumption + save-as flow) renders the exact legacy MUI experience with no residual state.
- [ ] T087 Assemble the **SC-002 parity checklist** and the PR evidence pack: screenshots of every new CRD surface; the a11y output (T083); the bundle/import check (T085); the authz mapping (T084); the V1/V2/V3/V4/V5 outcomes (T004–T007, incl. the committed schema diffs); and the explicit list mapping each legacy MUI template flow → its CRD equivalent — callout-from-template (every framing kind, incl. **Collabora-document** and **poll**), whiteboard-from-template, **subspace-from-template** (CRD now), **top-level-Space-from-template** (FR-037 fallback — legacy picker, until that wizard migrates), default-subspace-template (own Space templates only), **community-guidelines-apply** (native CRD now — this feature delivers the CRD community-guidelines editor host to legacy parity, FR-038, not an FR-037 fallback), flow-state-default-callout-template (Space/Account/Platform, relabelled "Default callout template"), post-default-description, default-whiteboard, import-into-holder (Account+Platform sources, persistent + mark/remove — a deliberate consolidation, not a 1:1), save-callout-as-template (no-regression — bridge removed), save-subspace-as-template (no-regression), **save-community-guidelines-as-template (a deliberate NEW addition — note it as such, not as no-regression)**, template deep-linking preserved on the Space tab + pack admin — opens the **Preview** dialog everywhere (FR-013 / Clarifications D1; the legacy "pack deep-link → Edit" behaviour is intentionally dropped) (FR-007). The only documented FR-037 fallbacks left after this feature: the **top-level (L0) create-Space wizard** and the **platform-admin Innovation Pack list page** (`/admin/innovation-packs`). Zero gaps except those two.
- [ ] T088 [P] Standalone preview: add mock pages under `src/crd/app/pages/` for `TemplatesManagerView`, `TemplatePicker` (both modes), `TemplatePreviewDialog`, `TemplateFormDialog` (each type — incl. the Whiteboard form's preview-settings and the CG form's title), `CommunityGuidelinesEditor`, `InnovationLibraryView`, `InnovationPackProfileView`, `InnovationPackAdminView`, fed by mock data adapted from `prototype/src/app/data/template-data.ts`, so designers can iterate via `pnpm crd:dev` with no backend.
- [ ] T089 [P] Additional unit tests beyond T011/T024/T032/T038: `templateContentMapper` Collabora-document + poll framing fixtures and the `communityGuidelines.title` mapping; `innovationLibraryMapper` (`ownerLabel`); `innovationPackMapper` (form ⇄ input round-trip); `useSaveAsTemplate` (pre-filled value per source kind — incl. the community-guidelines title — + the unsaved-source-changes prompt); the CG-editor integration hook's `CommunityGuidelines.profile.{displayName,description,references}` ⇄ `CommunityGuidelinesEditorValue` round-trip.
- [ ] T090 Remove the temporary `045`-era compatibility re-exports under `src/main/crdPages/topLevelPages/spaceSettings/templates/` once nothing imports them; confirm `src/crd/components/space/settings/{TemplateLibraryDialog,TemplateEditDialog,SaveSubspaceAsTemplateDialog,ChangeDefaultSubspaceTemplateDialog}.tsx` are deleted and `TemplatePreviewDialog.tsx` moved; run `pnpm lint` and `pnpm vitest run`; update `quickstart.md`'s build order + verification checklist with results.

---

## Dependencies & Execution Order

- **Phase 1 (T001–T003)** — no deps; all `[P]`.
- **Phase 2 (T004–T038)** — depends on Phase 1. Verifications first (T004–T007 `[P]`) — gate any GraphQL change. Then: mappers (T008–T011 `[P]`) → manager view (T012–T013 `[P]`, then T014) → previews (T015–T019 `[P]`, then T020, then T021) → picker (T022 needs T012+T020; T023 needs T008+T010; T024 needs T023) → forms (T025 = `crdPages/templates/CalloutTemplateForm`, integration layer; T026–T029 `[P]` = pure `src/crd/.../forms/*`, T028 presentational; then T030 = the pure `TemplateFormDialog` shell, needs only T003; T031 needs T010+T025+T026–T029, assembles `perTypeFormSlot`; T032 needs T031) → set-default/save-as (T033 needs T022; T034 needs T023; T035 needs T030; **T036 needs T031+T035 — and it also removes the `CalloutSettingsConnector` MUI bridge here, in Phase 2, before any CRD callout-template code path reaches it**) → T037 needs T009/T010/T021/T022/T030/T031 → T038 needs T037/T034. **No user-story phase starts until Phase 2 is complete.**
- **Phase 3 (US1)** — Phase 2 (T037, T034, T036, T014, T033).
- **Phase 4 (US2)** — Phase 2 (T037, T014); independent of US1, US3.
- **Phase 5 (US3)** — Phase 2 (T022, T023); independent of US1, US2.
- **Phase 6 (US4)** — Phase 2 (T022, T023, T018, T035, T036) + US1's T039 (which exposes `useSaveAsTemplate` to the Subspaces tab).
- **Phase 7 (US5)** — Phase 2 (T010, T021, T022, T023, T029, T033, T034); **T057 delivers the CRD community-guidelines editor host (FR-038)** — supersedes `045`'s markdown-only stub; touches `045`-owned files in `layout/` and `community/`.
- **Phase 8 (US6)** — Phase 2 (T021); T061/T062/T063 `[P]`; `InnovationPackCard` (T063) reused by US8.
- **Phase 9 (US9)** — Phase 2 (T035, T036 — note: the *callout* case is already wired & de-bridged in T036/Phase 2; T069 here is just verification) **+ US5's T057** (the community-guidelines case, T070, wires into the CRD community-guidelines editor T057 delivers — so US9's guidelines half follows US5).
- **Phase 10 (US7)** — Phase 2 + **US2's T046** (extends `CrdInnovationPackAdminPage`); T072/T073 `[P]`.
- **Phase 11 (US8)** — Phase 2 (T037, T014) + **US6's T063** (`InnovationPackCard`).
- **Phase 12 (Polish)** — all user-story phases; T082–T086/T088/T089 `[P]`; T087 + T090 last.

**Parallelisable once Phase 2 is done**: US1 ∥ US2 ∥ US3 ∥ US4 ∥ US5 ∥ US6; US7 after US2; US8 after US6; US9's callout case is parallel (the bridge was removed in T036/Phase 2), but US9's community-guidelines case (T070) follows US5's T057 (the CRD community-guidelines editor host).

---

## Parallel Execution Examples

- **Phase 1**: T001, T002, T003 together.
- **Phase 2 — verifications**: T004, T005, T006, T007 together (do first). **mappers**: T008, T009, T010, T011 together. **previews**: T015, T016, T017, T018, T019 together (then T020). **forms**: T025 (`crdPages/templates/CalloutTemplateForm`), T026, T027, T028, T029 together; T030 (pure `TemplateFormDialog` shell) in parallel with them; then T031 (assembles `perTypeFormSlot`); then T035 → T036 (which also removes the `CalloutSettingsConnector` MUI bridge).
- **Phase 8 (US6)**: T061, T062, T063 together (then T064).
- **Phase 10 (US7)**: T072, T073 together.
- **Phase 12**: T082, T083, T084, T085, T086, T088, T089 together (then T087, T090).

---

## Implementation Strategy

> Ships as **one PR**; the steps below are the *build order*, not separate merge points.

1. **Verifications + Foundational first.** Run T004–T007 immediately — they verify each GraphQL gap server-side first, then make the additive fragment edits + `pnpm codegen` + committed schema diffs (V1 Collabora-document framing, V2 poll framing, V5 CG `title`, conditionally V3 delete-cascade data); anything that needs a real backend change is a blocker raised here. Then build the complete CRD templates kit (T008–T038): the holder-agnostic manager view + card, the dual-mode picker, the 5 per-type previews + preview dialog, the per-type forms + the **CRD-native `TemplateFormDialog` that replaces the legacy `CreateTemplateDialog` bridge** (and the Whiteboard form reusing the preview-settings/crop dialogs; the CG form gaining a title field), the set-default & save-as dialogs (generalised from the `045` ones), and the shared hooks/mappers. Unit-test the mappers + hooks + the callout form mapping (incl. Collabora + poll).
2. **The MVP slice: US1 + US3.** Wire the kit into the Space Settings → Templates tab (US1 — removing the `045` partial implementation, the MUI create-dialog bridge, and the "custom only" gate; closing the `onCreateTemplate` TODO; deep-links open Preview, FR-013/D1) and into the CRD callout-creation flow (US3). A coherent, demoable slice (and the natural split-line if the PR has to be broken up).
3. **Then US2** — point the kit at the Innovation Pack admin (new route at `<pack.profile.url>/settings` + a page shell + the same `useTemplatesManager` with `holderKind:'innovationPack'`). Proves the holder unification with almost no new code.
4. **Then the P2 batch** — US4 (subspace creation from a template + defaults + re-point save-subspace-as-template; top-level Space stays legacy FR-037), US5 (whiteboard / **deliver the CRD community-guidelines editor host to legacy parity (FR-038) + wire guidelines-apply** / flow-state-default-with-relabel / post+whiteboard contribution-defaults refactor), US6 (Innovation Library page), US9 (save-callout-as-template — the `CalloutSettingsConnector` bridge was already removed in Phase 2/T036, so this is a verification — + save-guidelines-as-template, which wires into the CRD CG editor from US5). Mostly picker/dialog wiring + the CG editor + one new page; can run in parallel **except** US9's guidelines half follows US5's T057.
5. **Then the P3 batch** — US7 (pack create from the Account tab + edit/delete — port `CreateInnovationPackDialog` to shadcn, extend `CrdInnovationPackAdminPage` with the details form) and US8 (pack public profile — reuses US6's pack card + the read-only manager view).
6. **Polish last** — 6-language i18n audit, a11y sweep, authz-parity verification, no-MUI bundle check (incl. confirming the bridges are gone), toggle-off check, standalone-preview mocks, extra unit tests, the SC-002 parity checklist + PR evidence pack (only two FR-037 fallbacks remain: the top-level create-Space wizard + the platform-admin pack list), cleanup of compatibility shims.

Throughout: legacy MUI templates files stay untouched and serve toggle-off; CRD layer rules apply to every new file; the `045`-era files are refactored-in-place (not duplicated); the `045` integration plumbing for default-subspace / save-subspace / flow-state-default is re-pointed, not re-written; expect some overlap/rewrite of the `045` template cards per the prototype.
