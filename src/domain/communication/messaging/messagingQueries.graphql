query UserMessagingEnabled ($id: UUID!) {
  lookup {
    user(ID: $id) {
      id
      settings {
        id
        communication {
          allowOtherUsersToSendMessages
        }
      }
    }
  }
}