query askVirtualPersonaQuestion($question: String!, $virtualPersonaID: UUID!) {
  askVirtualPersonaQuestion(
    chatData: {
      question: $question
      virtualPersonaID: $virtualPersonaID
    }
  ) {
    id
    question
    answer
    sources {
      title
      uri
    }
  }
}
