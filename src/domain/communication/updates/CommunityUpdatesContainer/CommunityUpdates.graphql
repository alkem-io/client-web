query communityUpdates($communityId: UUID!) {
  lookup {
    community(ID: $communityId) {
      id
      displayName
      communication {
        id
        updates {
          id
          messages {
            id
            ...MessageDetails
          }
          messagesCount
        }
      }
    }
  }
}
