query JourneyBreadcrumbsInnovationHub {
  platform {
    innovationHub {
      id
      profile {
        id
        displayName
        avatar: visual(type: BANNER_WIDE) {
          id
          ...VisualUri
        }
      }
    }
  }
}