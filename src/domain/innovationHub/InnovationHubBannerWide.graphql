query InnovationHubBannerWide {
  platform {
    innovationHub {
      id
      profile {
        id
        displayName
        bannerWide: visual(type: BANNER_WIDE) {
          id
          ...VisualModel
        }
      }
    }
  }
}