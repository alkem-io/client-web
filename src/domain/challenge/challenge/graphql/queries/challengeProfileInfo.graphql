query challengeProfileInfo($hubId: UUID_NAMEID!, $challengeId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenge(ID: $challengeId) {
      id
      nameID
      profile {
        id
        displayName
        tagset {
          id
          name
          tags
        }
        visuals {
          ...VisualFull
        }
      }
      lifecycle {
        state
      }
      context {
        ...ContextDetails
      }
    }
  }
}
