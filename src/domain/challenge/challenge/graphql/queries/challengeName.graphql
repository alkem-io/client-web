query challengeName($hubId: UUID_NAMEID!, $challengeId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    nameID
    challenge(ID: $challengeId) {
      id
      nameID
      profile {
        id
        displayName
      }
    }
  }
}
