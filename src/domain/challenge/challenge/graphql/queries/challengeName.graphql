query challengeName($spaceId: UUID_NAMEID!, $challengeId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    nameID
    challenge(ID: $challengeId) {
      id
      nameID
      profile {
        id
        displayName
      }
    }
  }
}
