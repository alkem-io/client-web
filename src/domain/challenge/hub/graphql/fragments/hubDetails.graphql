fragment HubDetails on Hub {
  id
  nameID
  profile {
    id
    displayName
    tagset {
      id
      name
      tags
    }
    visuals {
      ...VisualUri
    }
  }

  authorization {
    id
    anonymousReadAccess
  }
  host {
    id
    nameID
    profile {
      id
      displayName
    }
  }
  context {
    ...ContextDetails
  }
}
