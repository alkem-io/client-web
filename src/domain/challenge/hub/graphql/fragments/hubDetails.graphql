fragment HubDetails on Hub {
  id
  nameID
  displayName
  tagset {
    id
    name
    tags
  }
  authorization {
    id
    anonymousReadAccess
  }
  host {
    id
    nameID
    profile {
        id
        displayName
    }
  }
  context {
    ...ContextDetails
  }
}
