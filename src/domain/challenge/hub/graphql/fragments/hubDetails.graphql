fragment HubDetails on Hub {
  id
  nameID
  profile {
    id
    displayName
    description
    tagline
    tagset {
      id
      name
      tags
    }
    references {
      id
      name
      description
      uri
    }
    visuals {
      ...VisualFull
    }
  }

  authorization {
    id
    anonymousReadAccess
  }
  host {
    id
    nameID
    profile {
      id
      displayName
    }
  }
  context {
    ...ContextDetails
  }
}
