query hubInnovationFlowTemplates($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    templates {
      id
      innovationFlowTemplates {
        definition
        id
        type
        profile {
          id
          displayName
        }
      }
    }
  }
}
