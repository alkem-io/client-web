query hubApplicationTemplate($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    community {
      id
      applicationForm {
        id
        description
        questions {
          required
          question
          explanation
          sortOrder
        }
      }
    }
  }
}
