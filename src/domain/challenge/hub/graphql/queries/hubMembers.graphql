query hubMembers ($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    community {
      id
      memberUsers {
        id
        displayName
        firstName
        lastName
        email
      }
    }
  }
}
