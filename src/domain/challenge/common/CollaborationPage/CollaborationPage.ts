/**
 * Move to domain/challenge/common if other Journeys have Collaboration page as well
 */
import { CalloutPageProps } from '../../../collaboration/CalloutPage/CalloutPage';

export interface CollaborationPageProps {
  children?: CalloutPageProps['children'];
}
