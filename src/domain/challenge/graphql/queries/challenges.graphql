query challenges($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenges {
      id
      displayName
      nameID
      context {
        id
        tagline
        references {
          name
          uri
        }
        visuals {
          ...VisualUri
        }
      }
    }
  }
}
