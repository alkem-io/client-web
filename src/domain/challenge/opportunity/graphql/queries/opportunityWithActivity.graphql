query opportunityWithActivity($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    opportunities {
      id
      profile {
        id
        displayName
        tagline
        visuals {
          ...VisualUri
        }
        tagset {
          ...TagsetDetails
        }
      }
      nameID
      metrics {
        id
        name
        value
      }
    }
  }
}
