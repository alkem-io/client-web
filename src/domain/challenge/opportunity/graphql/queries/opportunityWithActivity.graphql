query opportunityWithActivity($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    opportunities {
      id
      profile {
        id
        displayName
        tagline
        visuals {
          ...VisualUri
        }
        tagset {
          name
          tags
        }
      }
      nameID
      metrics {
        name
        value
      }
    }
  }
}
