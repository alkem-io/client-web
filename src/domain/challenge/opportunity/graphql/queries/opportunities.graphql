query opportunities($spaceId: UUID_NAMEID!, $challengeId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    challenge(ID: $challengeId) {
      id
      opportunities {
        id
        nameID
        profile {
          id
          displayName
        }
      }
    }
  }
}
