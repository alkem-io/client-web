query platformAdminSpacesList {
  platformAdmin {
    spaces(filter: { visibilities: [ACTIVE, DEMO] }) {
      id
      nameID
      visibility
      about {
        id
        profile {
          id
          displayName
          url
        }
      }
      authorization {
        id
        myPrivileges
      }
    }
  }
}
