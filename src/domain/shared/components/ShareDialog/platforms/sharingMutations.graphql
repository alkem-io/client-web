# TODO: Proper mutation with $link, $entityType [space, ch, opp, callout, card ...], $message
# For now it's just a direct message to a user
mutation shareLinkWithUser($messageData: CommunicationSendMessageToUserInput!) {
  sendMessageToUser(messageData: $messageData)
}

# TODO: Mutation to send a message to an email address
# ....
