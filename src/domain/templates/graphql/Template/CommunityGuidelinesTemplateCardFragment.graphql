fragment CommunityGuidelinesTemplateCard on Template {
  id
  profile {
    ...TemplateCardProfileInfo
  }
  communityGuidelines {
    id
    profile {
      id
      displayName
      description
      references {
        ...ReferenceDetails
      }
    }
  }
}
