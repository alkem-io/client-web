query SpaceTemplatesSetId($spaceNameId: UUID!) {
  space(ID: $spaceNameId) {
    id
    templatesManager {
      id
      templatesSet {
        id
        authorization {
          myPrivileges
        }
      }
    }
  }
}
