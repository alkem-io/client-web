query SpaceTemplatesSetId($spaceId: UUID!) {   #//!! Remove this query?
  lookup {
    space(ID: $spaceId) {
      id
      templatesManager {
        id
        templatesSet {
          id
          authorization {
            myPrivileges
          }
        }
      }
    }
  }
}
