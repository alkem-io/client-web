query SpaceTemplatesSetId($spaceNameId: UUID_NAMEID!) {
  space(ID: $spaceNameId) {
    id
    templatesManager {
      id
      templatesSet {
        id
        authorization {
          myPrivileges
        }
      }
    }
  }
}
