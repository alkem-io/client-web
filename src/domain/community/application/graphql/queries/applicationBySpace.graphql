query applicationBySpace($spaceId: UUID_NAMEID!, $appId: UUID!) {
  space(ID: $spaceId) {
    id
    application(ID: $appId) {
      id
      createdDate
      updatedDate
      questions {
        id
        name
        value
      }
    }
  }
}
