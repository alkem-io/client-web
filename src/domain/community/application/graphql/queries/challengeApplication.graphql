query challengeApplication($challengeId: UUID!) {
  lookup {
    challenge(ID: $challengeId) {
      id
      profile {
        id
        displayName
      }
      community {
        id
      }
    }
  }
}
