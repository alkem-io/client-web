query hubApplication($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    profile {
      id
      displayName
      tagline
      visuals {
        ...VisualUri
      }
    }
    community {
      id
      displayName
    }
  }
}
