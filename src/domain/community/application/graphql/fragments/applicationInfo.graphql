fragment ApplicationInfo on Application {
  id
  createdDate
  updatedDate
  lifecycle {
    id
    state
    nextEvents
  }
  user {
    id
    email
    profile {
      id
      displayName
      visual(type: AVATAR) {
        ...VisualUri
      }
    }
  }
  questions {
    id
    name
    value
  }
}
