query InviteUsersDialog($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      level
      about {
        id
        profile {
          id
          displayName
        }
        membership {
          roleSetID
        }
      }
    }
  }
}