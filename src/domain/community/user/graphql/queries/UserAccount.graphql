query UserAccount($userId: UUID_NAMEID_EMAIL!) {
  user(ID: $userId) {
    id
    profile {
      id
      displayName
    }
    agent {
      id
      credentials {
        id
        type
      }
    }
    account {
      id
    }
  }
}
