query UserProvider {
  me {
    user {
      ...UserDetails
      ...UserAgent
    }
    applications(states: ["new"]) {
      id
      communityID
      displayName
      state
      spaceID
      spaceLevel
    }
    invitations(states: ["invited"]) {
      id
      spaceID
      spaceLevel
      welcomeMessage
      createdBy
      createdDate
      state
    }
  }
}
