fragment DashboardContributingVirtualContributor on VirtualContributor {
  id
  nameID
  profile {
    id
    displayName
    url
    avatar: visual(type: AVATAR) {
      id
      uri
    }
    tagsets {
      ...TagsetDetails
    }
  }
}
