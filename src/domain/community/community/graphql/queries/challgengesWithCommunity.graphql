query challengesWithCommunity($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenges {
      id
      nameID
      profile {
        id
        displayName
      }
      community {
        id
        displayName
      }
    }
  }
}
