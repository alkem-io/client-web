query challengesWithCommunity($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    challenges {
      id
      nameID
      profile {
        id
        displayName
      }
      community {
        id
        displayName
      }
    }
  }
}
