query communityMembers($spaceId: UUID_NAMEID!, $communityId: UUID!) {
  space(ID: $spaceId) {
    id
    community(ID: $communityId) {
      id
      memberUsers {
        ...UserDisplayName
      }
    }
  }
}
