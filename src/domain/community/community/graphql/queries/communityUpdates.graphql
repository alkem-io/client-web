query communityMessages($spaceId: UUID_NAMEID!, $communityId: UUID!) {
  space(ID: $spaceId) {
    id
    community(ID: $communityId) {
      id
      communication {
        id
        updates {
          id
          messages {
            ...MessageDetails
          }
          messagesCount
        }
      }
    }
  }
}
