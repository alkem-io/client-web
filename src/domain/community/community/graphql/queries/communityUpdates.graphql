query communityMessages($hubId: UUID_NAMEID!, $communityId: UUID!) {
  hub(ID: $hubId) {
    id
    community(ID: $communityId) {
      id
      communication {
        id
        updates {
          id
          messages {
            ...MessageDetails
          }
          messagesCount
        }
      }
    }
  }
}
