query VirtualContributorKnowledgeBase($id: UUID!) {
  virtualContributor(ID: $id) {
    id
    knowledgeBase {
      id
      profile {
        id
        displayName
        description
      }
      calloutsSet {
        id
      }
    }
  }
}
