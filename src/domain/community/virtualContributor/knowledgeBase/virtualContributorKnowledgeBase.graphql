query VirtualContributorKnowledgeBase($id: UUID_NAMEID!) {
  virtualContributor(ID: $id) {
    id
    knowledgeBase {
      id
      profile {
        id
        displayName
        description
      }
      calloutsSet {
        id
      }
    }
  }
}
