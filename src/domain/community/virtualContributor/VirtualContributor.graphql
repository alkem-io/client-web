query VirtualContributor ($id: UUID_NAMEID!) {
  virtualContributor (ID: $id) {
    id
    nameID
    profile {
      id
      displayName
      description
      tagline
      tagsets {
        ...TagsetDetails
      }
      url
      avatar: visual(type: AVATAR) {
        ...VisualFull
      }
    }
  }
}
