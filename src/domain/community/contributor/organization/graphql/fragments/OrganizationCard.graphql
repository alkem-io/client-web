fragment OrganizationCard on Organization {
  id
  nameID
  displayName
  metrics {
    id
    name
    value
  }
  profile {
    id
    visual(type: AVATAR) {
      ...VisualUri
    }
    description
  }
  verification {
    id
    status
  }
}
