fragment GroupInfo on UserGroup {
  id
  name
  profile {
    id
    avatar {
      ...VisualFull
    }
    description
    references {
      id
      uri
      name
      description
    }
    tagsets {
      id
      name
      tags
    }
  }
}
