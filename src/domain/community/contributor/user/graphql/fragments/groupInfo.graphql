fragment GroupInfo on UserGroup {
  id
  name
  profile {
    id
    visual(type: AVATAR) {
      ...VisualFull
    }
    description
    references {
      id
      uri
      name
      description
    }
    tagsets {
      id
      name
      tags
    }
  }
}
