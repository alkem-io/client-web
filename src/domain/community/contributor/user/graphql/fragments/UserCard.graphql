fragment UserCard on User {
  id
  nameID
  displayName
  isContactable
  agent {
    id
    credentials {
      id
      type
      resourceID
    }
  }
  profile {
    id
    location {
      country
      city
    }
    visual(type: AVATAR) {
      ...VisualUri
    }
    tagsets {
      id
      name
      tags
    }
  }
}
