mutation updateGroup($input: UpdateUserGroupInput!) {
  updateUserGroup(userGroupData: $input) {
    id
    name
    profile {
      id
      avatar {
        ...VisualUri
      }
      description
      references {
        uri
        name
        description
      }
      tagsets {
        name
        tags
      }
    }
  }
}
