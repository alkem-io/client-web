query userAvatars($ids: [UUID!]!) {
  usersById(IDs: $ids) {
    id
    nameID
    displayName
    profile {
      id
      location {
        country
        city
      }
      avatar {
        ...VisualUri
      }
      tagsets {
        id
        name
        tags
      }
    }
  }
}
