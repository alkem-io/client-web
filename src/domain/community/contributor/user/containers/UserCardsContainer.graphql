query userCardsContainer($ids: [UUID!]!) {
  usersById(IDs: $ids) {
    id
    nameID
    displayName
    profile {
      id
      location {
        city
        country
      }
      visual(type: AVATAR) {
        ...VisualUri
      }
      tagsets {
        id
        name
        tags
      }
    }
    agent {
      id
      credentials {
        id
        resourceID
        type
      }
    }
  }
}
