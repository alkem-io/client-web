query userCardsContainer($ids: [UUID!]!) {
  usersById(IDs: $ids) {
    id
    nameID
    profile {
      id
      displayName
      location {
        city
        country
      }
      visual(type: AVATAR) {
        ...VisualUri
      }
      tagsets {
        id
        name
        tags
      }
    }
  }
}
