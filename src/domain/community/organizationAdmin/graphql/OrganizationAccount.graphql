query OrganizationAccount($organizationId: UUID!) {
  lookup {
    organization(ID: $organizationId) {
      id
      profile {
        id
        displayName
      }
      account {
        id
      }
    }
  }
}
