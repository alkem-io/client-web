query UsersInPlatformRole ($role: RoleName) {
  usersInPlatformRole ($role: $role){
    id
    firstName
    lastName
    email
    profile {
      id
      displayName
      avatar: visual(type: AVATAR) {
        ...VisualUri
      }
      url
    }
    isContactable
  }
}
