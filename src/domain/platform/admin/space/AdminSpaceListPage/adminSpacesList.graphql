query adminSpacesList {
  spaces(filter: { visibilities: [ARCHIVED, ACTIVE, DEMO] }) {
    ...AdminSpace
  }
}

fragment AdminSpace on Space {
  id
  account {
    id
    license {
      id
      visibility
      featureFlags {
        name
        enabled
      }
    }
    host {
      id
      profile {
        id
        displayName
      }
    }
  }
  profile {
    id
    displayName
    url
  }
  authorization {
    id
    myPrivileges
  }
}
