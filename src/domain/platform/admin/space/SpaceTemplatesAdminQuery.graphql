query SpaceTemplatesAdmin($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      profile {
        id
        url
      }
      templatesManager {
        id
        templatesSet {
          id
        }
      }
    }
  }
}
