query organizationGroup($organizationId: UUID_NAMEID!, $groupId: UUID!) {
  organization(ID: $organizationId) {
    id
    members {
      ...GroupMembers
    }
    group(ID: $groupId) {
      ...GroupInfo
    }
  }
}
