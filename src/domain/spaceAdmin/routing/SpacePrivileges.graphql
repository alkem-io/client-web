query SpacePrivileges($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      about {
        id
        profile {
          id
          url
        }
      }
      authorization {
        id
        myPrivileges
      }
    }
  }
}