mutation updateAspect($input: UpdateAspectInput!) {
  updateAspect(aspectData: $input) {
    id
    displayName
    type
    profile {
      id
      description
      tagset {
        id
        name
        tags
      }
      references {
        id
        name
        description
        name
        uri
      }
    }
  }
}
