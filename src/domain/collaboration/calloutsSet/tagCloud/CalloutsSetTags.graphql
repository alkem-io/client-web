query CalloutsSetTags(
  $calloutsSetId: UUID!
  $classificationTagsets: [TagsetArgs!] = [],
) {
  lookup {
    calloutsSet(ID: $calloutsSetId) {
      id
      tags(classificationTagsets: $classificationTagsets)
    }
  }
}