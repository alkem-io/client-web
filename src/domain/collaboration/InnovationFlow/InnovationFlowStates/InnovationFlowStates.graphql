query InnovationFlowStatesAllowedValues($id: UUID!) {
  lookup {
    innovationFlow(ID: $id) {
      id
      authorization {
        id
        myPrivileges
      }
      profile {
        id
        tagsets {
          id
          name
          allowedValues
        }
      }
    }
  }
}
