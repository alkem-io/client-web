query SpaceInnovationFlows($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      library {
        id
        innovationFlowTemplates {
          id
          profile {
            id
            displayName
          }
        }
      }
    }
  }
}
