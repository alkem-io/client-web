query SpaceInnovationFlows($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    account {
      id
      library {
        id
        innovationFlowTemplates {
          id
          profile {
            id
            displayName
          }
        }
      }
    }
  }
}
