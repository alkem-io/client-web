query CalloutPageCallout(
  $calloutId: UUID!
) {
  lookup {
    callout(ID: $calloutId) {
      classification {
        id
        flowState: tagset(tagsetName: FLOW_STATE) {
          ...TagsetDetails
        }
      }
      ...CalloutDetails
    }
  }
}
