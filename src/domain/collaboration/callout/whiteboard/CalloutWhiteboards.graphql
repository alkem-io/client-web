query CalloutWhiteboards($calloutId: UUID!) {
  lookup {
    callout(ID: $calloutId) {
      id
      contributions {
        whiteboard {
          ...WhiteboardDetails
        }
      }
    }
  }
}
