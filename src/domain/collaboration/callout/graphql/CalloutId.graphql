query CalloutId(
  $calloutNameId: UUID_NAMEID!
  $spaceId: UUID_NAMEID!
) {
  space(ID: $spaceId)  {
    id
    collaboration {
      id
      callouts(IDs: [$calloutNameId]) {
        id
      }
    }
  }
}
