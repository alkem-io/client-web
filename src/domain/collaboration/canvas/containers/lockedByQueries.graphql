query CanvasLockedByDetails($ids: [UUID!]!) {
  usersById(IDs: $ids) {
    ...LockedByDetails
  }
}

fragment LockedByDetails on User {
  id
  profile {
    id
    displayName
    visual(type: AVATAR) {
      ...VisualUri
    }
  }
}
