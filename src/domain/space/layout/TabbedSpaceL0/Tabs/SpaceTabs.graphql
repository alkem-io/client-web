query SpaceTabs($collaborationId: UUID!) {
  lookup {
    collaboration(ID: $collaborationId) {
      id
      innovationFlow {
        id
        states {
          displayName
          description
        }
      }
    }
  }
}