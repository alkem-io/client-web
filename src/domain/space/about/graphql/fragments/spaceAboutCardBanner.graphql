fragment SpaceAboutCardBanner on SpaceAbout {
  id
  profile {
    id
    displayName
    url
    tagline
    avatar: visual(type: AVATAR) {
      ...VisualModel
    }
    cardBanner: visual(type: CARD) {
      ...VisualModel
    }
    tagset {
      ...TagsetDetails
    }
  }
}
