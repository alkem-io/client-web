query SubspacesInSpace($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      settings {
        sortMode
      }
      subspaces {
        id
        about {
          ...SpaceAboutCardBanner
        }
        level
        sortOrder
        pinned
      }
    }
  }
}
