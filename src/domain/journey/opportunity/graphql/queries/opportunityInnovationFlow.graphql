query opportunityInnovationFlow($opportunityId: UUID!) {
  lookup {
    opportunity(ID: $opportunityId) {
      id
      collaboration {
        id
        innovationFlow {
          id
          states {
            displayName
            description
            sortOrder
          }
        }
      }
    }
  }
}
