query PlansTable {
  platform {
    id
    licensing {
      id
      plans {
        id
        name
        enabled
        sortOrder
        pricePerMonth
        isFree
        trialEnabled
        requiresPaymentMethod
        requiresContactSupport
      }
    }
  }
}

query AccountsHosts {
  accounts {
    hosts {
      id
      nameID
    }
  }
}

query ContactSupportLocation {
  platform {
    configuration {
      locations {
        contactsupport
      }
    }
  }
}
