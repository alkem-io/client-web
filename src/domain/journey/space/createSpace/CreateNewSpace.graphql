mutation CreateSpace($spaceData: CreateSpaceOnAccountInput!) {
  createSpace(spaceData: $spaceData) {
    id
    about {
      id
      profile {
        id
        url # Used to redirect to the new space
      }
    }
  }
}
