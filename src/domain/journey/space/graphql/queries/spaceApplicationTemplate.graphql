query SpaceApplicationTemplate($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      community {
        id
        roleSet {
          applicationForm {
            description
            questions {
              required
              question
              sortOrder
              explanation
              maxLength
            }
          }
        }
      }
    }
  }
}
