query spaceApplicationTemplate($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    community {
      id
      applicationForm {
        id
        description
        questions {
          required
          question
          explanation
          sortOrder
          maxLength
        }
      }
    }
  }
}
