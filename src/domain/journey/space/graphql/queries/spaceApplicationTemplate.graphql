query spaceApplicationTemplate($spaceId: UUID_NAMEID!) {
  space(ID: $spaceId) {
    id
    community {
      id
      applicationForm {
        description
        questions {
          required
          question
          sortOrder
          explanation
          maxLength
        }
      }
    }
  }
}
