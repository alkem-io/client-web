query SubspacesInSpace($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      subspaces {
        id
        nameID
        profile {
          id
          displayName
        }
      }
    }
  }
}
