query challengeInnovationFlow($challengeId: UUID!) {
  lookup {
    challenge(ID: $challengeId) {
      id
      collaboration {
        id
        innovationFlow {
          id
          states {
            displayName
            description
          }
          currentState {
            displayName
          }
        }
      }
    }
  }
}
