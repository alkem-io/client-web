query challengeInnovationFlow($challengeId: UUID!) {
  lookup {
    challenge(ID: $challengeId) {
      id
      collaboration {
        id
        innovationFlow {
          id
          states {
            displayName
            description
            sortOrder
          }
          currentState {
            displayName
          }
        }
      }
    }
  }
}
