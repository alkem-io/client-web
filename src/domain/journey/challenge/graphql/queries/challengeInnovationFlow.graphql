query challengeInnovationFlow($challengeId: UUID!) {
  lookup {
    challenge(ID: $challengeId) {
      id
      innovationFlow {
        id
        states {
          displayName
          description
        }
        currentState {
          displayName
        }
      }
    }
  }
}
