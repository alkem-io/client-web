query userProfile($input: UUID_NAMEID_EMAIL!) {
  user(ID: $input) {
    ...UserDetails
    ...UserAgent
  }
  rolesUser(rolesData: { userID: $input }) {
    id
    ...UserRolesDetails
  }
  authorization {
    ...MyPrivileges
  }
}
