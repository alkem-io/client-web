query userProfileApplications($input: RolesUserInput!) {
  rolesUser(rolesData: $input) {
    applications {
      id
      state
      displayName
      hubID
      challengeID
      opportunityID
    }
  }
}
