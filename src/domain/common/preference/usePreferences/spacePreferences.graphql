query spacePreferences($spaceNameId: UUID_NAMEID!) {
  space(ID: $spaceNameId) {
    id
    preferences {
      id
      value
      definition {
        id
        description
        displayName
        group
        type
        valueType
      }
    }
  }
}
