mutation updateAspect($input: UpdateAspectInput!) {
  updateAspect(aspectData: $input) {
    id
    displayName
    type
    profile {
      description
      tagset {
        id
        name
        tags
      }
      references {
        id
        name
        description
        name
        uri
      }
    }
  }
}
