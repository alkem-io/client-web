mutation updateAspect($input: UpdateAspectInput!) {
  updateAspect(aspectData: $input) {
    id
    defaultDescription
    typeDescription
    displayName
    tagset {
      id
      name
      tags
    }
    references {
      id
      name
      description
      name
      uri
    }
  }
}
