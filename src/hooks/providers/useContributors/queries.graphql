query contributingUsers($limit: Float, $shuffle: Boolean) {
  users(limit: $limit, shuffle: $shuffle) {
    id
    displayName
    nameID
    profile {
      id
      location {
        city
        country
      }
      avatar {
        id
        uri
      }
      tagsets {
        id
        tags
      }
    }
  }
}
