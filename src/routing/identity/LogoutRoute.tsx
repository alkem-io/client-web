import React, { FC } from 'react';
import { LogoutPage } from '../../core/auth/authentication/pages/LogoutPage';

export const LogoutRoute: FC = () => {
  return <LogoutPage />;
};
export default LogoutRoute;
