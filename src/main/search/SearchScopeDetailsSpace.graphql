query SearchScopeDetailsSpace($spaceNameId: UUID!) {
  space(ID: $spaceNameId) {
    id
    profile {
      id
      displayName
      avatar: visual(type: AVATAR) {
        id
        uri
      }
    }
    visibility
  }
}
