query RecentSpaces($limit: Float) {
  me {
    mySpaces(limit: $limit) {
      space {
        id
        about {
          ...SpaceAboutCardBanner
          isContentPublic
        }
        level
        __typename # do not remove
      }
    }
  }
}