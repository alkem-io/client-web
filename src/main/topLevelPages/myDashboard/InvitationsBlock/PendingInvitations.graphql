query PendingInvitations {
  me {
    communityInvitations(states: ["invited"]) {
      id
      spacePendingMembershipInfo {
        id
        level
        about {
          ...SpaceAboutLight
        }
      }
      invitation {
        id
        welcomeMessage
        actor {
          type
        }
        createdBy {
          id
        }
        state
        createdDate
      }
    }
  }
}
