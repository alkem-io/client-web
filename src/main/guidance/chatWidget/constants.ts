/**
 * Chat widget configuration constants
 */

/**
 * Timeout in milliseconds for hiding the loader if no server response is received
 * Default: 45000ms (45 seconds) - easily configurable for testing/production
 */
export const CHAT_LOADER_TIMEOUT_MS = 45000;
