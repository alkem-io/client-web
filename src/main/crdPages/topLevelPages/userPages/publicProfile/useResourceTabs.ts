/**
 * Backwards-compatible re-export shim. The implementation moved to
 * `@/main/crdPages/topLevelPages/common/useResourceTabs` (Phase 10 — the hook
 * is now used by both the User and Organization profile pages).
 */
export { default } from '../../common/useResourceTabs';
