query DefaultVisualTypeConstraints(
  $visualType: VisualType!
) {
  platform {
    id
    configuration {
      defaultVisualTypeConstraints(type: $visualType) {
        maxHeight
        maxWidth
        minHeight
        minWidth
        aspectRatio
        allowedTypes
      }
    }
  }
}
