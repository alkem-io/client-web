// `@excalidraw/element/types` shim — re-exports the element type surface the
// binding's `.d.ts` references (currently `BoundElement`) from the published
// `@alkemio/excalidraw` bundle, which carries the identical declarations under
// its `./element/*` subpath export.
export type * from '@alkemio/excalidraw/dist/types/element/src/types';
