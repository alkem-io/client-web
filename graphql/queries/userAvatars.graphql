query userAvatars($ids: [UUID_NAMEID_EMAIL!]!) {
  usersById(IDs: $ids) {
    id
    nameID
    displayName
    city
    country
    profile {
      id
      avatar {
        ...VisualUri
      }
      tagsets {
        id
        name
        tags
      }
    }
  }
}
