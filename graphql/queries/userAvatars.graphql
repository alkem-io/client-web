query userAvatars($ids: [UUID_NAMEID_EMAIL!]!) {
  usersById(IDs: $ids) {
    id
    nameID
    displayName
    city
    country
    profile {
      id
      avatar2 {
        id
        uri
      }
      tagsets {
        id
        name
        tags
      }
    }
  }
}
