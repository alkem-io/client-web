query ecoverseApplication($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    displayName
    context{
      tagline
      visuals {
        ...VisualUri
      }
    }
    community {
      id
      displayName
    }
  }
}
