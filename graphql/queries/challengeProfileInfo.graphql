query challengeProfileInfo($id: UUID_NAMEID!) {
  ecoverse {
    id
    challenge(ID: $id) {
      id
      nameID
      displayName
      lifecycle {
        state
      }
      context {
        ...ContextDetails
      }
    }
  }
}
