query projects($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    projects {
      id
      nameID
      displayName
      description
      lifecycle {
        state
      }
    }
  }
}
