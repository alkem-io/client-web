query ecoverseMembers ($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    community {
      id
      members {
        id
        displayName
        firstName
        lastName
        email
      }
    }
  }
}
