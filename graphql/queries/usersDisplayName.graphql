query usersDisplayName($first: Int, $after: UUID) {
  users2(first: $first, after: $after) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        id
        displayName
      }
    }
  }
}
