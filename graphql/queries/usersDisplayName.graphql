query usersDisplayName($first: Int, $after: UUID) {
  usersPaginated(first: $first, after: $after) {
    pageInfo {
      endCursor
      hasNextPage
    }
    edges {
      node {
        id
        displayName
      }
    }
  }
}
