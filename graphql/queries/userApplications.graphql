query userApplications($input: RolesUserInput!) {
  rolesUser(rolesData: $input) {
    applications {
      id
      state
      communityID
      displayName
      createdDate
      hubID
      challengeID
      opportunityID
    }
  }
}
