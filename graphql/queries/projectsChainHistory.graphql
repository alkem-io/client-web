query projectsChainHistory($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    challenges {
      displayName
      nameID
      opportunities {
        nameID
        projects {
          nameID
        }
      }
    }
  }
}
