query projectsChainHistory($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenges {
      displayName
      nameID
      opportunities {
        nameID
        projects {
          nameID
        }
      }
    }
  }
}
