query organizationCard($id: UUID_NAMEID!) {
  organisation(ID: $id) {
    id
    displayName
    groups {
      name
    }
    members {
      id
    }
    profile {
      id
      description
      avatar
    }
  }
}
