query userCardData($ids: [String!]!) {
  usersById(IDs: $ids) {
    __typename
    memberof {
      communities {
        name
      }
      organisations {
        name
      }
    }
    ...UserDetails
  }
}
