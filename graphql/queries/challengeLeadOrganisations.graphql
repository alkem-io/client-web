query challengeLeadOrganizations($hubId: UUID_NAMEID!, $challengeID: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenge(ID: $challengeID) {
      id
      leadOrganizations {
        ...OrganizationDetails
      }
    }
  }
  organizations {
    ...OrganizationDetails
  }
}