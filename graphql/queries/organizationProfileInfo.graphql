query organizationProfileInfo($id: UUID_NAMEID!) {
  organisation(ID: $id) {
    id
    nameID
    displayName
    profile {
      id
      avatar
      description
      references {
        id
        name
        uri
      }
      tagsets {
        id
        name
        tags
      }
    }
  }
}
