query hubLifecycleTemplates($hubId: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    templates {
      lifecycleTemplates {
        definition
        id
        type
        info {
          id
          title
        }
      }
    }
  }
}
