query challengesWithCommunity($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    challenges {
      id
      nameID
      displayName
      community {
        id
        displayName
      }
    }
  }
}
