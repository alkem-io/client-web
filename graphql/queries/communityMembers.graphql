query communityMembers($hubId: UUID_NAMEID!, $communityId: UUID!) {
  hub(ID: $hubId) {
    id
    community(ID: $communityId) {
      id
      memberUsers {
        ...UserDisplayName
      }
    }
  }
}
