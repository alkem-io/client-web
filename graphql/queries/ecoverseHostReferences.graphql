query ecoverseHostReferences($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    host {
      profile {
        id
        references {
          name
          uri
        }
      }
    }
  }
}
