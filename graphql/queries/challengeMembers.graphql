query challengeMembers($hubId: UUID_NAMEID!, $challengeID: UUID_NAMEID!) {
  hub(ID: $hubId) {
    id
    challenge(ID: $challengeID) {
      community {
        members {
          id
          displayName
          firstName
          lastName
          email
        }
      }
    }
  }
}
