query challengeMembers($challengeID: UUID_NAMEID!) {
  ecoverse {
    id
    challenge(ID: $challengeID) {
      community {
        members {
          id
          displayName
          firstName
          lastName
          email
        }
      }
    }
  }
}
