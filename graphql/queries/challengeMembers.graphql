query challengeMembers($challengeID: String!) {
  ecoverse {
    id
    challenge(ID: $challengeID) {
      community {
        members {
          id
          name
          firstName
          lastName
          email
        }
      }
    }
  }
}
