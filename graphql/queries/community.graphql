query community($communityId: UUID!) {
  community(ID: $communityId) {
    id
    displayName
    groups {
      id
      name
      profile {
        id
        avatar
        description
        tagsets {
          id
          name
          tags
        }
      }
    }
    updatesRoom {
      id
      messages {
        id
        message
        sender
        timestamp
      }
    }
  }
}