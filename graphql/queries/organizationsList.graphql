query organizationsList($limit: Float, $shuffle: Boolean) {
  organizations(limit: $limit, shuffle: $shuffle) {
    id
    nameID
    displayName
    profile {
      id
      avatar {
        ...VisualUri
      }
    }
  }
}
