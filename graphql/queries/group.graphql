query group($id: String!) {
  ecoverse {
    id
    group(ID: $id) {
      id
      name
      profile {
        id
        avatar
        description
        references {
          name
          description
        }
        tagsets {
          name
          tags
        }
      }
    }
  }
}
