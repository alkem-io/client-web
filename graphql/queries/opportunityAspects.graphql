query opportunityAspects($id: UUID_NAMEID!) {
  ecoverse {
    id
    opportunity(ID: $id) {
      context {
        aspects {
          title
          framing
          explanation
        }
      }
    }
  }
}
