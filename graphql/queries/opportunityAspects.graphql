query opportunityAspects($id: String!) {
  ecoverse {
    id
    opportunity(ID: $id) {
      context {
        aspects {
          title
          framing
          explanation
        }
      }
    }
  }
}
