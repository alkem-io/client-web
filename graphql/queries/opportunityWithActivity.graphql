query opportunityWithActivity($ecoverseId: UUID_NAMEID!) {
  ecoverse(ID: $ecoverseId) {
    id
    opportunities {
      id
      displayName
      nameID
      activity {
        name
        value
      }
      context {
        tagline
        visuals {
          ...VisualUri
        }
      }
      tagset {
        name
        tags
      }
    }
  }
}
