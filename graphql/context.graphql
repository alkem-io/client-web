import { gql } from '@apollo/client';

export const CONTEXT_DETAL_FRAGMENT = gql`
  fragment ContextDetails on Context {
    id
    tagline
    background
    vision
    impact
    who
    references {
      id
      name
      uri
      description
    }
  }
`;
