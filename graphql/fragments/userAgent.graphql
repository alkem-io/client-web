fragment UserAgent on User {
  agent {
    id
    did
    credentials {
      id
      resourceID
      type
    }
    verifiedCredentials {
      claim
      context
      issued
      expires
      issuer
      name
      type
    }
  }
}