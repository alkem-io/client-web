kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: default
  name: ct-web-deployment
  labels:
    app: ct-web

spec:
  replicas: 2
  selector:
    matchLabels:
      app: ct-web
  template:
    metadata:
      labels:
        app: ct-web
    spec:
      containers:
        - name: ct-web
          image: ctdev.azurecr.io/ct-web:e1c6f2e518053a7e6dafd27840b53130927cbb3b
          envFrom:
          - secretRef:
              name: server-secret
          ports:
            - name: web
              containerPort: 80

